{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://skillgraph.dev/schemas/skill-audit-state.schema.json",
  "title": "Skill Graph Audit-State Sidecar",
  "type": "object",
  "additionalProperties": false,
  "description": "The audit-state sidecar (`audit-state.json`, skill-folder root, sibling of SKILL.md) carries the Skill Audit Loop's records ABOUT a skill — provenance, audit/eval verdicts, drift bookkeeping, distribution-internal tier, and runtime telemetry. These are SYSTEM-mode output, written only by `/audit:*`, never read by the everyday agent finding/understanding/executing the skill (those fields live in SKILL.md frontmatter). The manifest compiler joins frontmatter + sidecar so the router's quality/staleness gates keep working. Two of the gates that used to live in the frontmatter schema are now cross-file lint checks in scripts/skill-lint.js, NOT here: (1) `comprehension_state: present` (sidecar) ⇒ the five flat Understanding fields in frontmatter; (2) `stability: deprecated` (frontmatter) ⇒ `superseded_by` (frontmatter). The two `eval_state ⇒ eval_artifacts` gates are intra-sidecar and kept below. Authorizing decision: docs/adr/0019-audit-state-sidecar-separation.md. Field placement basis: benchmarks/field-relevance/field-placement.json.",
  "required": [
    "schema_version",
    "owner",
    "freshness",
    "drift_check",
    "eval_artifacts",
    "eval_state",
    "routing_eval"
  ],
  "properties": {
    "schema_version": {
      "oneOf": [
        {
          "type": "integer",
          "enum": [8]
        },
        {
          "type": "string",
          "enum": ["8"]
        }
      ],
      "description": "Major contract shape version. Integer 8 is the only accepted current value. v7 is a prior contract shape retrievable from git history, not an accepted live-schema value. Bumps when shape changes break consumers. Moved from SKILL.md frontmatter to this sidecar in the audit-state split (ADR-0019): which contract a skill conforms to is a system/audit concern, not part of the public Agent-Skills frontmatter. Prior versions live in git history — see `git tag --list 'schema-*'`."
    },
    "version": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$",
      "description": "Skill content version (semver). Bumps when the SKILL.md body or contract changes meaningfully. Distinct from `schema_version` (the contract shape). Used by `relations.depends_on` for `min_version` constraints. Governance/provenance — moved to the sidecar in ADR-0019. Optional in the sidecar (the only currently-required audit field NOT carried into the sidecar `required` set)."
    },
    "skill_graph_protocol": {
      "type": "string",
      "pattern": "^Skill Metadata Protocol v[0-9]+(\\.[0-9]+)*$",
      "description": "Optional content-label claim — the protocol version whose SUBSTANTIVE content bar this skill's body and metadata have actually earned (e.g. `Skill Metadata Protocol v8`). Distinct from `schema_version` (the mechanical shape integer a codemod can bump): the content label is earned only when the version's semantic content — classification, free-text scope, Understanding fields, gradeable eval artifacts, audited verdicts — is present and reviewed per `AGENTS.md § Version Labels Are Earned, Not Bumped`. History: this token previously existed ONLY as export-provenance frontmatter that `parse-frontmatter.js::normalizeFrontmatter()` strips, leaving the doctrine governed by human discipline alone; making it a first-class sidecar field (2026-06-10) gives the earned-not-bumped rule a deterministic, schema-validated surface the audit loop and status tooling can read. Written by the audit loop when a content migration completes — never advanced by find-replace.",
      "examples": ["Skill Metadata Protocol v8"]
    },
    "urn": {
      "type": "string",
      "pattern": "^urn:skill:[a-z0-9][a-z0-9-]*:[a-z0-9][a-z0-9-/:]*$",
      "description": "Optional globally-unique persistent identifier in the `urn:skill:<repo>:<skill-name>` form (RFC 8141). Consumers treat the URN as the stable identity across repos and federated registries; the frontmatter `name` is the display-layer handle. The `<skill-name>` segment MUST equal the frontmatter `name` field. Registry identity / provenance — moved to the sidecar in ADR-0019 (0 acting consumer today)."
    },
    "owner": {
      "type": "string",
      "description": "Maintainer or team accountable for keeping this skill correct. Free-form string; conventional values: `skill-graph-maintainer`, GitHub team handles, individual usernames. Used by drift-check workflows to route review requests. Maintenance accountability is a human-curator/audit concern, not agent-facing — moved to the sidecar in ADR-0019."
    },
    "repo": {
      "type": "array",
      "description": "Repos this skill is linked to. Each entry has a kebab-case `handle` and a canonical `url`. Optional; plural even though most skills today have one source repo, so federation is structurally ready without a future schema bump. Belonging-entity / provenance reference — moved to the sidecar in ADR-0019.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["handle"],
        "properties": {
          "handle": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9-]*$",
            "description": "Kebab-case repo handle (e.g. `skill-graph`)."
          },
          "url": {
            "type": "string",
            "format": "uri",
            "description": "Canonical repository URL."
          }
        }
      }
    },
    "freshness": {
      "type": "string",
      "format": "date",
      "description": "ISO date (YYYY-MM-DD) of the last meaningful content review. The author's claim that the skill was current as of this date. Complemented by `drift_check.truth_source_hashes` for grounded skills. `reviewed_at` is the supported alias. Audit freshness timestamp — moved to the sidecar in ADR-0019."
    },
    "reviewed_at": {
      "type": "string",
      "format": "date",
      "description": "ISO date (YYYY-MM-DD) of the last meaningful content review (v3.1 preferred alias for `freshness`). When both are present they must match. "
    },
    "drift_check": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "last_verified"
      ],
      "properties": {
        "last_verified": {
          "type": "string",
          "format": "date",
          "description": "ISO date of the last verification against truth sources. Authored claim. `verified_at` is the supported alias."
        },
        "verified_at": {
          "type": "string",
          "format": "date",
          "description": "ISO date of the last verification against truth sources (v3.1 preferred alias for `last_verified`). When both are present they must match. "
        },
        "truth_source_hashes": {
          "type": "object",
          "description": "Map of normalized truth source key to SHA-256 hex digest at the time of last verification. Keys are `path` for whole-file sources, `path#Lstart-Lend` for line ranges, and `path#anchor` for anchor-only sources. Evidence that complements the `last_verified` claim. The truth_sources themselves live in frontmatter `grounding.truth_sources`; the hashes (the audit facet) live here per ADR-0019. Computed by `node scripts/skill-graph-drift.js --record --apply <skill-path>`.",
          "additionalProperties": {
            "type": "string",
            "pattern": "^[a-f0-9]{64}$"
          }
        }
      },
      "description": "Drift-detection record for grounded skills. `last_verified` is the author's claim; `truth_source_hashes` is content-addressable evidence keyed by each normalized frontmatter `grounding.truth_sources` entry. Whole-file sources hash normalized file content; line-range sources hash only the cited slice; anchor-only sources hash the resolved Markdown section or literal text. The combination lets `scripts/skill-graph-drift.js` detect when underlying truth has changed without an accompanying review. Audit machinery — moved to the sidecar in ADR-0019 (grounding stays frontmatter; only the hashes move)."
    },
    "eval_artifacts": {
      "type": "string",
      "enum": [
        "none",
        "planned",
        "present"
      ],
      "description": "Are eval artifacts present on disk for this skill? `none` (no evals planned), `planned` (eval intent declared but not yet shipped), `present` (eval JSON exists at `evals/<skill>.json` or similar). The `present` claim requires a real artifact and audit/eval receipt."
    },
    "eval_state": {
      "type": "string",
      "enum": [
        "unverified",
        "passing",
        "monitored"
      ],
      "description": "What does the eval say about content quality? `unverified` (no eval has run), `passing` (last run passed), `monitored` (eval runs on a cadence and is currently passing). Independent of `routing_eval` (the routing-coverage axis). Use to express content-level quality grading orthogonal to routing coverage."
    },
    "routing_eval": {
      "type": "string",
      "enum": [
        "absent",
        "present"
      ],
      "description": "Is routing / trigger coverage explicitly evaluated? `absent` (router behaviour is not part of the eval set), `present` (the skill's frontmatter `examples[]` and `anti_examples[]` pass `scripts/skill-graph-routing-eval.js`). Honesty over green checkmarks — flip to `present` only after the harness PASSes. The nested `eval.routing_coverage` is the v3.1 preferred alias."
    },
    "last_audited": {
      "type": "string",
      "format": "date",
      "description": "ISO date (YYYY-MM-DD) the `audit` command last ran against this skill. Written by `scripts/skill/skill-audit.js`. Independent of `freshness` (which is the author's claim of content-level review) and `drift_check.last_verified` (which is the truth-source verification). Loop priority uses `last_audited` to pick the stalest skill."
    },
    "last_changed": {
      "type": "string",
      "format": "date",
      "description": "ISO date (YYYY-MM-DD) the SKILL.md body or frontmatter was last edited. Written automatically by `improve` operations. Distinct from `freshness` (review claim) — `last_changed` is the editor's footprint, `freshness` is the reviewer's footprint."
    },
    "structural_verdict": {
      "type": "string",
      "enum": [
        "PASS",
        "PASS_WITH_FIXES",
        "FAIL",
        "UNVERIFIED"
      ],
      "description": "Structural-layer verdict produced by gates 1–2 and 7 of the skill-audit loop (schema lint, manifest census, concept-card shape). `PASS` (clean), `PASS_WITH_FIXES` (warnings present but no errors), `FAIL` (lint or schema errors), `UNVERIFIED` (no structural audit has run yet). Independent of `lint_verdict` (per-script signal); this is the audit-loop roll-up. See docs/adr/0011-split-audit-verdict-into-four-verdicts.md."
    },
    "truth_verdict": {
      "type": "string",
      "enum": [
        "PASS",
        "DRIFT",
        "BROKEN",
        "UNVERIFIED"
      ],
      "description": "Truth-layer verdict produced by gates 3–6 of the skill-audit loop (truth-source catalog, drift sentinel, test coverage, claim verification). `PASS` (truth sources align with declared `last_verified` and hashes), `DRIFT` (truth sources changed since last_verified), `BROKEN` (declared truth sources missing or unreadable), `UNVERIFIED` (no truth audit has run yet). Independent of `drift_status` (per-script signal); this is the audit-loop roll-up. See docs/adr/0011-split-audit-verdict-into-four-verdicts.md."
    },
    "comprehension_verdict": {
      "type": "string",
      "enum": [
        "PASS",
        "SHALLOW",
        "REDUNDANT",
        "UNVERIFIED",
        "PROVISIONAL",
        "SKIPPED_BASELINE_HIGH",
        "NA"
      ],
      "description": "Comprehension-layer verdict produced by gate 8 (the comprehension grader on `evals/comprehension.json`). `PASS` (with-skill answers measurably deeper than baseline; dual-run grader earned), `PROVISIONAL` (a single competent model ran the comprehension assessment and recorded a real result — lower confidence than `PASS` because not yet confirmed by the independent dual-run grader, but distinct from `UNVERIFIED` which means no assessment has run), `SHALLOW` (skill recites the concept but does not deepen agent understanding), `REDUNDANT` (baseline already saturated — skill adds no comprehension lift on this concept), `SKIPPED_BASELINE_HIGH` (early-skip — `avg_primary_baseline >= 1.0` after the first 2 evals so the dual-run was aborted), `NA` (skill carries no `evals/comprehension.json`), `UNVERIFIED` (initial state before any grader run). Confidence hierarchy: `PASS (grader) > PROVISIONAL (single model) > UNVERIFIED (none)`. Comprehension never alone certifies a skill as useful — `application_verdict` is the aggregate-quality field. See docs/adr/0011-split-audit-verdict-into-four-verdicts.md and .claude/rules/version-schema-contract.md § 5."
    },
    "application_verdict": {
      "type": "string",
      "enum": [
        "APPLICABLE",
        "REDUNDANT",
        "NOT_DISCRIMINATED_CEILING",
        "EQUIVALENT_ON_FRONTIER",
        "HARMFUL",
        "MIXED",
        "FALSE_POSITIVE",
        "UNVERIFIED",
        "PROVISIONAL"
      ],
      "description": "Application-layer verdict produced by gate 9 (the application grader on `evals/application.json`). `APPLICABLE` (loading the skill changes agent behavior on real artifacts in the expected direction — flags, fixes, generative trajectory), `NOT_DISCRIMINATED_CEILING` (baseline saturated on the real cases, so the eval had no measurement headroom; inconclusive, not a deprecation signal), `EQUIVALENT_ON_FRONTIER` (baseline had measurement headroom but the skill produced no marginal lift for the measured frontier model on this case set), `REDUNDANT` (legacy no-delta bucket retained for old receipts; new runner output should prefer the two scoped no-lift values), `HARMFUL` (negative delta — agent makes worse decisions with the skill loaded; SkillsBench arXiv 2602.12670 found 19% of evaluated skills exhibit this), `MIXED` (delta varies across cases — some applicable, some equivalent, or false-positive), `FALSE_POSITIVE` (skill over-triggers — applies on cases where its expertise does not apply), `UNVERIFIED` (no application assessment has run), `PROVISIONAL` (single-model self-assessment audit found useful behavior but the independent application grader has not confirmed it). This is the aggregate-quality field: a skill is only behaviorally certified when this verdict is `APPLICABLE`. See docs/adr/0011-split-audit-verdict-into-four-verdicts.md."
    },
    "lint_verdict": {
      "type": "string",
      "enum": [
        "PASS",
        "FAIL",
        "UNKNOWN"
      ],
      "description": "Result of the most recent canonical-source schema lint pass against this skill (`scripts/skill-lint.js`). The current lint gate checks valid frontmatter, validation against `schemas/SKILL_METADATA_PROTOCOL_schema.json` (and this sidecar schema), identifier shape, non-empty description, and parent-directory/name alignment. Relation targets, routing quality, drift, export, and eval checks are separate tools. `PASS` means zero lint errors. `UNKNOWN` is the initial state."
    },
    "drift_status": {
      "type": "string",
      "enum": [
        "OK",
        "DRIFT",
        "BROKEN",
        "STALE",
        "NO_BASELINE",
        "EXTERNAL_UNHASHED",
        "UNKNOWN"
      ],
      "description": "Current truth-source drift status, mirroring the `scripts/skill-graph-drift.js` sentinel verdicts. `OK` (live hashes match recorded), `DRIFT` (mismatch), `BROKEN` (declared truth source missing), `STALE` (older than `lifecycle.stale_after_days`), `NO_BASELINE` (local truth sources declared but no hashes recorded), `EXTERNAL_UNHASHED` (URL truth sources not fetched), `UNKNOWN` (no drift check has run). Written by the drift sentinel; read by the loop to prioritise re-grounding work."
    },
    "eval_score": {
      "type": "number",
      "minimum": 0,
      "maximum": 5,
      "description": "Latest aggregate eval grade on a 0.0–5.0 scale, written by `scripts/skill/evaluate-skill.js`. Replaces the read-the-log dance for knowing how this skill scored. When `evals/comprehension.json` exists, the comprehension grader's score lands here; otherwise the standard eval-suite score. Use `eval_failed_ids` to inspect the failing cases."
    },
    "eval_failed_ids": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Eval IDs that failed in the most recent run. Empty array when clean. Populated alongside `eval_score` by the eval runner. Surfaces failures without forcing readers to crawl `eval-history.jsonl`."
    },
    "eval_last_run": {
      "type": "object",
      "additionalProperties": false,
      "description": "Optional receipt for the most recent eval run. Complements `eval_state` so `passing` and `monitored` claims can point at evidence instead of remaining self-attested.",
      "required": [
        "at",
        "status"
      ],
      "properties": {
        "at": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp for the eval run that supports the current eval_state claim."
        },
        "status": {
          "type": "string",
          "enum": [
            "pass",
            "fail",
            "mixed"
          ]
        },
        "runner": {
          "type": "string",
          "description": "Eval runner or command used, e.g. `scripts/skill-audit.js --graded`."
        },
        "model": {
          "type": "string",
          "description": "Optional grader/model identifier when an LLM grader was used."
        },
        "receipt": {
          "type": "string",
          "description": "Path or URL to the eval receipt, scorecard, grader history, or CI run."
        },
        "receipt_hash": {
          "type": "string",
          "pattern": "^[a-f0-9]{64}$",
          "description": "Optional SHA-256 digest of the receipt artifact."
        },
        "bidirectional": {
          "type": "object",
          "additionalProperties": false,
          "description": "Eval provenance from lib/audit/run-bidirectional-eval.js. The normal certifying shape is the two-frontier Opus/GPT bidirectional receipt. An explicit single-available-frontier degraded run may record one direction only; that shape is non-certifying, must set `regrade_required: true`, and can support at most PROVISIONAL until a later full two-frontier re-grade runs. A graded verdict (PASS/APPLICABLE) is honest only when `parity_ok` AND `certifying_clean` are both true; otherwise the verdict was capped to PROVISIONAL. See docs/skill-audit-loop-philosophy.md and lib/audit/eval-execution-profile.js.",
          "properties": {
            "frontier_pair": {
              "type": "array",
              "items": { "type": "string" },
              "minItems": 1,
              "maxItems": 2,
              "description": "The frontier model role/aliases used. Two items is the normal certifying pair (e.g. [\"opus\", \"gpt-5.5\"]); one item is allowed only for explicit single-available-frontier degraded runs. Aliases, never dated versions (model-identity discipline)."
            },
            "reconciliation": {
              "type": "string",
              "enum": ["conservative", "single-frontier-provisional"],
              "description": "How directions were reconciled. `conservative` is the two-frontier rule (more-skeptical verdict wins; APPLICABLE/PASS requires BOTH directions). `single-frontier-provisional` records an explicit degraded run with one available frontier and may never certify."
            },
            "agreement": {
              "type": "boolean",
              "description": "True when both directions produced the same verdict."
            },
            "parity_ok": {
              "type": "boolean",
              "description": "True when both directions ran under an identical tools-ON execution profile. false means either profile parity failed or the run had only one direction; it may never certify."
            },
            "certifying_clean": {
              "type": "boolean",
              "description": "True when parity_ok AND both directions are cross-family certifying. A strong verdict (PASS/APPLICABLE) stands only when this is true; otherwise it was capped to PROVISIONAL."
            },
            "synthesized_verdict": {
              "type": "string",
              "description": "The reconciled verdict after the conservative rule and the certifying-clean cap."
            },
            "applicable_for": {
              "type": "string",
              "enum": ["anthropic", "openai", "both", "neither"],
              "description": "Per-family applicability (SH-6682). The generator is the measured agent, so each direction measures whether the skill helps THAT generator's model family. A family is listed only when the run is certifying-clean AND its direction reached the certifying verdict (APPLICABLE/PASS). The conservative `synthesized_verdict` stays THE verdict; this records the per-family nuance it hides — e.g. a skill that helps Anthropic's model but not OpenAI's is `applicable_for: 'anthropic'` while the aggregate caps below APPLICABLE. 'neither' = no family certified (or run not certifying-clean)."
            },
            "registry_version": {
              "type": "string",
              "description": "Model-registry epoch (REGISTRY_VERSION). Scores compare only within the same epoch."
            },
            "merge_ledger_ref": {
              "type": ["string", "null"],
              "description": "Path/ref to the enrichment merge-ledger that produced the skill version this eval graded, so enrichment and eval provenance are one record. Null when the eval ran standalone (not after an enrich)."
            },
            "provisional_reason": {
              "type": "string",
              "description": "Reason a degraded/non-certifying receipt is capped at PROVISIONAL, e.g. the unavailable frontier or unresolved model provenance."
            },
            "missing_frontiers": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Frontier aliases that did not run in an explicit single-available-frontier degraded receipt."
            },
            "regrade_required": {
              "type": "boolean",
              "description": "True when this receipt must be replaced by a later full two-frontier re-grade before certification."
            },
            "execution_profile": {
              "type": "object",
              "additionalProperties": false,
              "description": "The tools-ON parity profile both directions ran under.",
              "properties": {
                "tools": { "type": "string", "description": "Tool-access level, e.g. 'full'." },
                "research": { "type": "string", "description": "Research allowance, e.g. 'repo+web'." },
                "repoScope": { "type": "string", "description": "Repo scope fence, e.g. 'skill-graph + skills ONLY' (the public-content boundary)." },
                "cwd": { "type": "string", "description": "Working directory both directions ran from (the skill-graph repo root)." }
              }
            },
            "directions": {
              "type": "array",
              "minItems": 1,
              "maxItems": 2,
              "description": "Eval directions, named by which model GENERATES (the measured agent). Direction 'Claude': Claude (opus) generates → Codex (gpt) grades. Direction 'Codex': the swap. One direction is allowed only for an explicit single-available-frontier degraded run.",
              "items": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "role": { "type": "string", "enum": ["Claude", "Codex", "Single"] },
                  "generator_model": { "type": "string", "description": "Role/alias of the model that produced the answer." },
                  "grader_model": { "type": "string", "description": "Role/alias of the model that graded it (cross-family)." },
                  "generator_family": { "type": "string" },
                  "grader_family": { "type": "string" },
                  "resolved_model": { "type": "string", "description": "Resolved concrete model when knowable, else the literal 'latest-alias-unresolved' (an unresolved alias caps the run at PROVISIONAL)." },
                  "verdict": { "type": "string", "description": "This direction's standalone verdict before reconciliation." },
                  "certification_tier": { "type": "string", "enum": ["certifying", "provisional"] }
                }
              }
            }
          }
        }
      }
    },
    "eval": {
      "type": "object",
      "additionalProperties": false,
      "description": "Nested Evaluation Status record (v3.1 preferred alias for the sibling fields `eval_artifacts` / `eval_state` / `routing_eval`). When both the top-level and nested forms are present they must match. ",
      "properties": {
        "artifacts": {
          "type": "string",
          "enum": [
            "none",
            "planned",
            "present"
          ],
          "description": "Are eval artifacts present on disk for this skill? Mirrors top-level `eval_artifacts`."
        },
        "content_state": {
          "type": "string",
          "enum": [
            "unverified",
            "passing",
            "monitored"
          ],
          "description": "What does the eval say about content quality? Mirrors top-level `eval_state`. Renamed to `content_state` here to disambiguate from `routing_coverage`."
        },
        "routing_coverage": {
          "type": "string",
          "enum": [
            "absent",
            "present"
          ],
          "description": "Is routing / trigger coverage explicitly evaluated? Mirrors top-level `routing_eval`. Renamed to `routing_coverage` here to remove the head-first compound ambiguity."
        },
        "comprehension_state": {
          "type": "string",
          "enum": [
            "absent",
            "present"
          ],
          "description": "Mirrors top-level `comprehension_state`. When `present`, the five flat Understanding fields in SKILL.md frontmatter are required by the cross-file lint check in scripts/skill-lint.js (the gate moved out of the frontmatter schema's allOf in the ADR-0019 sidecar split)."
        }
      }
    },
    "comprehension_state": {
      "type": "string",
      "enum": [
        "absent",
        "present"
      ],
      "description": "Does this skill carry a comprehension eval (typically `evals/comprehension.json`) and the Understanding fields authored for the comprehension grader? `absent` (no comprehension grading), `present` (comprehension evals exist; the five flat Understanding fields `mental_model`, `purpose`, `boundary`, `analogy`, `misconception` in SKILL.md frontmatter are required by the cross-file lint check in scripts/skill-lint.js). The gate that ties this flag to the Understanding fields is CROSS-FILE (sidecar flag ⇒ frontmatter prose) and so lives in skill-lint.js, not in this schema's allOf. Authored-vs-measured boundary: the Understanding fields are AUTHORED CONTENT — the comprehension grader's INPUT, never the measurement itself. The measurement is the eval artifact (`comprehension.json`) plus the `comprehension_verdict` it produces. Independent of `routing_eval` (router-level) and `eval_state` (content-level). The nested `eval.comprehension_state` is the v3.1 preferred alias."
    },
    "marketplace_tier": {
      "type": "string",
      "enum": [
        "S",
        "A",
        "B",
        "C"
      ],
      "description": "Publication priority for the public marketplace at `github.com/jacob-balslev/skills` / `skills.sh`. `S` = featured (top-of-README, individual hero copy). `A` = high-demand (named in collection tables). `B` = standard utility (included in collection tables). `C` = niche (collapsed 'More' section). Omit entirely for skills that should not be published. Sourced from `marketplace-publication-priority-*.md` and authored per skill. Lint validates the enum; consumers (export-marketplace-skills.js, generate-marketplace-readmes.js) filter and group on this field. Distribution-internal — a marketplace consumer reads it at publish time, not the everyday agent; moved to the sidecar in ADR-0019."
    },
    "portability": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "readiness",
        "targets"
      ],
      "properties": {
        "readiness": {
          "type": "string",
          "enum": [
            "declared",
            "scripted",
            "verified"
          ]
        },
        "targets": {
          "type": "array",
          "description": "Supported export destinations. `export_targets` is the supported alias.",
          "items": {
            "type": "string",
            "enum": [
              "skill-md"
            ]
          }
        },
        "export_targets": {
          "type": "array",
          "description": "Supported export destinations (v3.1 preferred alias for `targets`). Distinguishes from bare `targets` which is generic. When both are present they must match. ",
          "items": {
            "type": "string",
            "enum": [
              "skill-md"
            ]
          }
        }
      },
      "description": "Portability execution signal. `readiness` declares whether portability is only declared (`declared`), covered by export tooling (`scripted`), or verified with a target-runtime receipt (`verified`). `targets` lists supported export destinations, currently `skill-md`. Manifest-only pass-through, not agent-read — moved to the sidecar in ADR-0019."
    },
    "lifecycle": {
      "type": "object",
      "additionalProperties": false,
      "description": "Per-skill maintenance policy consumed by the drift sentinel. Audit/freshness scheduling — moved to the sidecar in ADR-0019. NOTE: `stale_after_days` feeds the router staleness gate; the manifest-join carries it into the manifest so the gate keeps working with the field living in the sidecar.",
      "properties": {
        "stale_after_days": {
          "type": "integer",
          "minimum": 1,
          "description": "Days after `drift_check.last_verified` at which the skill is flagged stale. Integration skills rot faster than pure concept skills; pick per skill."
        },
        "review_cadence": {
          "type": "string",
          "enum": [
            "per-commit",
            "weekly",
            "quarterly",
            "on-truth-source-change"
          ],
          "description": "How frequently the skill should be re-verified. Drives scheduled drift checks."
        }
      }
    },
    "runtime_telemetry": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "feedback_source"
      ],
      "description": "Optional pointer to a real-world success/failure feed. Consumers may use telemetry to corroborate or override `eval_state`. Runtime-feedback that corroborates audit verdicts — moved to the sidecar in ADR-0019.",
      "properties": {
        "feedback_source": {
          "type": "string",
          "description": "Path or URL to a JSONL of run receipts. Each receipt is expected to carry `{ timestamp, skill, outcome }` at minimum."
        },
        "last_updated": {
          "type": "string",
          "format": "date"
        },
        "metrics": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "sample_size": {
              "type": "integer",
              "minimum": 0
            },
            "success_rate": {
              "type": "number",
              "minimum": 0,
              "maximum": 1
            }
          }
        }
      }
    },
    "model_run_coverage": {
      "type": "object",
      "additionalProperties": false,
      "description": "Per-model Skill Audit Loop participation matrix. This is not a verdict and never certifies quality; it records which model aliases ran which loop operation for this skill, which phase they reached, and where the receipt/failure evidence lives. Used to answer coverage questions such as 'has the free advisory tier run on this skill yet?' separately from `schema_version`, `skill_graph_protocol`, and the four Audit Status verdicts.",
      "required": [
        "schema_version",
        "updated_at",
        "registry_version",
        "models"
      ],
      "properties": {
        "schema_version": {
          "type": "integer",
          "enum": [1],
          "description": "Shape version for this coverage sub-record. Independent of the skill metadata protocol version."
        },
        "updated_at": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when this coverage matrix was last updated."
        },
        "registry_version": {
          "type": "string",
          "description": "Model registry epoch from `lib/audit-shared/model-provider.js`. Coverage comparisons across epochs must account for alias target drift."
        },
        "models": {
          "type": "object",
          "description": "Map keyed by stable model alias (`opus`, `gpt-5.5`, `gemini`, `deepseek-flash`, etc.). Aliases come from the canonical model registry; concrete dated IDs belong in historical receipts, not durable prompts.",
          "additionalProperties": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "model",
              "provider",
              "backend",
              "tier",
              "operations"
            ],
            "properties": {
              "model": {
                "type": "string",
                "description": "Stable model alias from the model registry."
              },
              "provider": {
                "type": "string",
                "description": "Provider family resolved by the model registry."
              },
              "backend": {
                "type": "string",
                "description": "CLI/backend used to run this model alias."
              },
              "tier": {
                "type": "string",
                "enum": [
                  "mandatory",
                  "advisory",
                  "orchestrator"
                ],
                "description": "`mandatory` means quality-frontier/certifying tier; `advisory` means breadth/novelty tier; `orchestrator` means the model supervised the runtime wrapper rather than acted as a panel participant."
              },
              "operations": {
                "type": "object",
                "description": "Latest participation record per operation. Current writer uses `panel`; future single-model audit/evaluate wrappers may add `audit`, `improve`, `evaluate`, or `evolve` keys.",
                "additionalProperties": {
                  "type": "object",
                  "additionalProperties": false,
                  "required": [
                    "at",
                    "operation",
                    "eval_mode",
                    "tier",
                    "status",
                    "registry_version",
                    "phase_status",
                    "certifying"
                  ],
                  "properties": {
                    "at": {
                      "type": "string",
                      "format": "date-time"
                    },
                    "operation": {
                      "type": "string",
                      "description": "Loop operation or composite operation this record describes."
                    },
                    "eval_mode": {
                      "type": "string",
                      "enum": [
                        "application",
                        "comprehension"
                      ]
                    },
                    "tier": {
                      "type": "string",
                      "enum": [
                        "mandatory",
                        "advisory",
                        "orchestrator"
                      ]
                    },
                    "status": {
                      "type": "string",
                      "enum": [
                        "completed",
                        "degraded",
                        "failed",
                        "skipped",
                        "blocked"
                      ],
                      "description": "Participation status for this model on this operation. `completed` means the model participated as expected; `degraded` means it contributed but some phase/finality was non-certifying; `failed`/`skipped` name distinct non-participation modes."
                    },
                    "registry_version": {
                      "type": "string"
                    },
                    "phase_status": {
                      "type": "object",
                      "additionalProperties": false,
                      "description": "Phase-by-phase state for the model in this operation.",
                      "properties": {
                        "budget": { "$ref": "#/$defs/phaseStatus" },
                        "claim": { "$ref": "#/$defs/phaseStatus" },
                        "propose": { "$ref": "#/$defs/phaseStatus" },
                        "cross_review": { "$ref": "#/$defs/phaseStatus" },
                        "revise": { "$ref": "#/$defs/phaseStatus" },
                        "curate": { "$ref": "#/$defs/phaseStatus" },
                        "verify": { "$ref": "#/$defs/phaseStatus" },
                        "evaluate": { "$ref": "#/$defs/phaseStatus" },
                        "apply": { "$ref": "#/$defs/phaseStatus" },
                        "record": { "$ref": "#/$defs/phaseStatus" }
                      }
                    },
                    "verdict": {
                      "type": "string",
                      "description": "Reconciled eval verdict seen by this operation when present. This mirrors evidence; it does not replace the canonical four verdict fields."
                    },
                    "certifying": {
                      "type": "boolean",
                      "description": "True only when the overall run produced certifying-clean evidence for the evaluated dimension."
                    },
                    "regrade_required": {
                      "type": "boolean",
                      "description": "True when a degraded/single-frontier/non-certifying run must be replaced by a full certifying run."
                    },
                    "failure_reason": {
                      "type": "string",
                      "description": "Machine-readable reason when the model failed, skipped, or degraded."
                    },
                    "failure_detail": {
                      "type": "string",
                      "description": "Short human-readable detail from the failure or certifying-block reason."
                    },
                    "receipt": {
                      "type": "string",
                      "description": "Path or URL to the merge ledger, eval receipt, run root, or other evidence for this model's participation."
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
  },
  "$defs": {
    "phaseStatus": {
      "type": "string",
      "enum": [
        "completed",
        "degraded",
        "failed",
        "skipped",
        "not_applicable"
      ]
    }
  },
  "allOf": [
    {
      "_comment": "Eval-state coherence (audit M5, 2026-05-27; lifted intra-sidecar in the ADR-0019 split): a skill can only claim `eval_state: passing` when there are real evals on disk (`eval_artifacts: present`). `passing` without present artifacts is the same doc-lie shape as `application_verdict: APPLICABLE` without an eval_last_run receipt — the schema refuses it. Both fields are sidecar-local, so this gate stays here (not cross-file).",
      "if": {
        "properties": {
          "eval_state": { "const": "passing" }
        },
        "required": ["eval_state"]
      },
      "then": {
        "properties": {
          "eval_artifacts": { "const": "present" }
        },
        "required": ["eval_artifacts"]
      }
    },
    {
      "_comment": "Eval-state coherence (audit M5, 2026-05-27; lifted intra-sidecar in the ADR-0019 split): `eval_state: monitored` is a forward state that means continuous cadenced runs are succeeding. It implies an eval suite exists; `eval_artifacts` must be `present`.",
      "if": {
        "properties": {
          "eval_state": { "const": "monitored" }
        },
        "required": ["eval_state"]
      },
      "then": {
        "properties": {
          "eval_artifacts": { "const": "present" }
        },
        "required": ["eval_artifacts"]
      }
    }
  ]
}
