# Changelog

All notable changes to Skill Graph are recorded here.

The format is based on [Keep a Changelog 1.1.0](https://keepachangelog.com/en/1.1.0/) and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

Skill Graph versions describe the contract at each checkpoint in the git history and are how consumers pin. Beginning with 0.5.0, the repository is package-ready as `@skill-graph/cli` with the `skill-graph` binary. `schema_version` in the JSON Schemas tracks contract shape (currently `8` — owned by `@skill-graph/cli` post-consolidation; see [ADR 0009](docs/adr/0009-sibling-repo-deprecation.md) for the consolidation addendum and the post-2026-05-18 source-of-truth model, [ADR 0011](docs/adr/0011-split-audit-verdict-into-four-verdicts.md) for the v6→v7 four-verdict Audit Status split, and [ADR 0017](docs/adr/0017-five-axis-classification-model.md) for the v7→v8 classification model. The dual-versioning rationale historically lived in `skill-metadata-protocol/docs/adr/0007-version-source-of-truth.md`, which is preserved in the now docs-only mirror at `jacob-balslev/skill-metadata-protocol` for inbound-link stability — that mirror is **frozen / read-only** per ADR-0009 § Update and is referenced here for historical context only, not as an active ADR series entry); this changelog tracks the library as a whole (tooling + starters + docs).

## [Unreleased]

### Changed

- **v8 doc reconciliation `deployment_target → public` — reference docs (Part 2C, 2026-06-08T12:00Z).** Fixed `docs/manifest-field-mapping.md` (projection row 9 → `public`; `by_deployment_target` → `by_public`; YAML/JSON examples → `public: false`; the v8-shape prose + the v2→v8 history line), `docs/CONFORMANCE.md` (L2 row → `public: false` + `project[]`), `docs/positioning-vs-marketplaces.md` (project-scoping rows), `docs/publishing-vs-quality.md` (the publishability explanation — `public` IS the publishability gate), `docs/marketplace-syndication.md` (classification field lists + the exporter gate reframed `public: false` primary / `deployment_target: project` back-compat), and `docs/grounding-policy.md` (semantic — re-keyed the grounding-obligation Axis-1 from the retired `deployment_target: project` to non-empty `project[]`, matching schema `allOf[0]`; kept "portable" as a conceptual descriptor and the dated 2026-06-07 corpus snapshot as a historical measurement). Correct deprecation/rename notes (e.g. "renamed from `domain_object`", "replaces `workspace_tags`") left intact. Verified: `check-markdown-links` OK (1323). Part 2C of the v8-doc-reconciliation plan. SYSTEM-only; no skill content touched.
- **v8 doc reconciliation `deployment_target → public` — semantic rewrites (Part 2B, 2026-06-08T11:45Z).** Rewrote the three docs that explain `deployment_target`'s *meaning* (not just a field-name swap): `docs/field-rationale.md` (`## deployment_target` section → `## public`, reframed around the publishability gate with project-anchoring/project-fit moved to `project[]`; fixed the `portability.readiness` rationale + decision-tree row), `docs/glossary.md` (`### deployment_target` entry → `### public`; the `repo_specific`/`repo_code_first` defaults re-keyed to project-anchored skills), `docs/publish-workflow.md` (the marketplace-exporter exclusion list — the code's primary gate is `public === false` per `export-marketplace-skills.js`; `deployment_target: project` reframed as the back-compat fallback). Grounded against the actual code: exporter filters `public: false` (fail-closed), manifest emits `by_public`. Verified: `check-markdown-links` OK (1323); no broken anchors to the renamed sections. Part 2B of the v8-doc-reconciliation plan. SYSTEM-only; no skill content touched.
- **v8 doc reconciliation `deployment_target → public` — authoring + structural docs (Part 2A, 2026-06-08T11:30Z).** The ADR-0017 amendment retired the `deployment_target` enum (→ boolean `public`) and moved project-anchoring/grounding-trigger to `project[]`, but ~24 active docs still taught `deployment_target` as the current required field. Fixed the authoring-facing + structural docs to the current contract: `docs/QUICKSTART-30MIN.md`, `docs/ADOPTION.md`, `docs/AUTHORING-QUICKSTART.md`, `docs/concept-map.md`, `docs/field-state-matrix.md` — required-field lists/tables → `public` (boolean); YAML examples `deployment_target: portable/project` → `public: true/false`; the grounding-trigger prose re-keyed from "`deployment_target: project`" to "non-empty `project[]`" (matching schema `allOf[0]`); the manifest summary facet `by_deployment_target` → `by_public` (matching `generate-manifest.js`). Correct deprecation-notes (e.g. "renamed from `domain_object`", "replaces old `workspace_tags`") were left intact. Verified: `check-markdown-links` OK (1323 files); zero `deployment_target` remains in these 5 files. Part 2A of `docs/plans/v8-doc-tooling-reconciliation-2026-06-08.md`. SYSTEM-only; no skill content touched.

### Removed

- **Retired the obsolete v7→v8 frontmatter codemod `lib/audit/migrate-frontmatter.js` to git history (2026-06-08T11:10Z).** Under `audit --fix` it wrote the RETIRED `deployment_target` field and DROPPED the now-required free-text `scope`, so it could no longer emit a valid current-v8 skill (the ADR-0017 amendment replaced `deployment_target` with the boolean `public` + free-text `scope`). Per `AGENTS.md § Major Version Is a Clean Cut` ("run the codemod, then delete it; the `--fix` framework stays, the map entry goes"), `git rm`'d the codemod and rewired `lib/audit/skill-audit.js`'s two `--fix` blocks (dry-run + real) to report an EMPTY deterministic repair catalog; remaining shape-violation lint errors now route to `improve`/manual. Also removed the now-dead `runBackfill` wrapper in `skill-audit.js` (the `scripts/backfill-field-purpose-comments.js` script itself is untouched — only the unused in-audit wrapper went). Updated the `--fix` help in `bin/skill-graph.js` (+ the `init` scaffold that told users to fill in `deployment_target` → `public`), `commands/audit/audit.md`, and the runbook step-1 note. Recover via `git show <sha>^:lib/audit/migrate-frontmatter.js` / `git tag schema-v7`. Conservative-scope cleanup (user 2026-06-08): the corpus-coupled deprecated ALIASES in the normalizer/lint/router stay (gated on SKI-317). Verified: `node --check` both JS; `audit <skill> --dry-run --fix` reports the empty catalog; no test imports the deleted module; `check-markdown-links` OK. Part 1 of `docs/plans/v8-doc-tooling-reconciliation-2026-06-08.md`. SYSTEM-only; no skill content touched.

### Fixed

- **Authoring template scaffolded the deprecated routing edge + an inconsistent heading name (C1 + C2, 2026-06-08T10:07Z).** `examples/skill-metadata-template.md` scaffolded `relations.boundary: []` and told authors to "Pair with `relations.boundary`", so every skill authored from the template inherited the deprecated edge (perpetuating the very `boundary`→`suppresses` corpus backlog the audit loop drains). Changed the scaffold to author `relations.suppresses` as primary (with the inverse-mechanic ownership-wording warning) and mention `boundary` only as the deprecated alias; updated the commented example + the relations checklist item. Separately, the TEMPLATE NOTE (line 182) told authors to rewrite `## Philosophy`, but the lint-enforced (and actual) heading is `## Philosophy of the skill` — corrected so copying the guidance no longer produces a heading that fails lint. Verified: `node scripts/skill-lint.js examples/skill-metadata-template.md` clean (0 errors / 0 warnings); no bare `## Philosophy` remains; `suppresses: []` scaffolded. SYSTEM-only; no skill content touched.
- **Audit/eval docs were ambiguous about the two eval schemas, the `audit --graded` scope, and the certifying run (B2 + B3 + B4, 2026-06-08T10:07Z).**
  - **B2 (two eval schemas + criticality enum):** `comprehension.json` (`evals[]`) and `application.json` (`cases[]`) are different schemas with a different `criticality` enum — comprehension `critical/high/medium/low`, application `critical/high/normal/low` (`check-application-evals.js` rejects `medium`). Added a side-by-side comparison table (both shapes + both enums + validators + graders) to `docs/comprehension-eval-spec.md` and `docs/application-eval-spec.md`, cross-linked, and linked from the runbook's evaluate section. (The recurring authoring error is `criticality: medium` in an application case.)
  - **B3 (audit --graded scope) + discovered drift:** the runbook, `bin/skill-graph.js audit --help`, and `lib/audit/skill-audit.js`'s own comments/messages claimed `audit --graded` runs the Behavior Gate and stamps `comprehension_verdict` / `application_verdict`. Verified against the code: `skill-audit.js`'s only sidecar write (`writeSidecarFields`) stamps `lint_verdict` / `structural_verdict` / `truth_verdict` / `last_audited` — it never writes the behavior verdicts. Corrected the runbook audit-pipeline phases + added an explicit scope note (`audit --graded` grades the seven scorecard dimensions; behavior verdicts come from `evaluate --mode`), the bin `audit` help, the `skill-graph/AGENTS.md` "what each command writes" table row, and the three misleading `skill-audit.js` comments/console messages (incl. the user-facing "run --graded to populate" message, which was actively wrong).
  - **B4 (certifying-run recipe):** added a copy-paste cross-family certifying-run one-liner to `docs/application-eval-spec.md` (`--certifying` + differing `--generator-family`/`--grader-family`; omitting silently caps at `PROVISIONAL`), model families by role/alias per Model Identity Discipline; linked from the runbook.
  - Verified: `node --check` both JS files; `bin/skill-graph.js audit --help` renders; `check-markdown-links.js` OK (1322 files, new cross-links resolve). SYSTEM-only; no skill content touched.

- **Protocol doc claimed a landed field rename was "future work" (B1, 2026-06-08T10:07Z).** `skill-metadata-protocol/SKILL_METADATA_PROTOCOL.md` stated the Understanding-field rename `boundary` → `concept_boundary` (ADR-0018) "is NOT yet landed and remains future work" and framed both renames as "v8.1" — but the SYSTEM half has landed: the schema's canonical Understanding field is `concept_boundary` (line 109) with top-level `boundary` retained only as a DEPRECATED alias (line 113), `skill-lint.js:85` requires `concept_boundary`, and `parse-frontmatter.js:404-408` normalizes the deprecated alias. An agent reading the canonical doc would conclude a landed field "isn't available" and could revert a correct migration. Corrected the five-flat-Understanding-fields lists (conditional table, optional-field YAML, comprehension_state description, the field section, and the all-fields appendix) to `concept_boundary`; rewrote the `boundary` field section + the § Relations note to state `concept_boundary`/`relations.suppresses` are canonical (SYSTEM half landed, normalizer maps the deprecated alias, corpus rename drains per-skill via the audit loop). The legacy nested `concept` block's `boundary` sub-field (normalizer-untouched) is preserved as-is. Sibling `field-reference.md` already documented `concept_boundary` correctly (no edit). Verified: `node scripts/check-protocol-consistency.js` PASS (C7 generated-parity green); zero remaining "v8.1 / not yet landed / future work" framing for the rename. SYSTEM-only; no skill content touched.
- **`status` showed a graded verdict without flagging a missing backing artifact (A4, 2026-06-08T10:07Z).** `skill-graph status <skill>` (`lib/audit/skill-status.js`) derived assessment from `application_verdict` but never checked whether the gradeable eval file existed on disk — so a skill carrying `application_verdict: PROVISIONAL`/`APPLICABLE` (or a graded `comprehension_verdict`) with no `evals/application.json` / `evals/comprehension.json` displayed as if the verdict were backed. Added `detectVerdictArtifactGaps()`: a graded verdict (anything other than `UNVERIFIED` / `NA` / `SKIPPED_BASELINE_HIGH`) with its eval artifact absent now prints a loud `(!) Verdict present, artifact ABSENT — honest state is UNVERIFIED` block (table) and a `verdictArtifactGaps[]` array (`--json`). Symmetric to the per-run gate in `scripts/check-audit-manifest.js`, applied to the live sidecar. Verified: graded verdict + no file → gap reported; `UNVERIFIED` / `SKIPPED_BASELINE_HIGH` → silent; existing `test-skill-status-sidecar.js` + `test-lib-audit-smoke.js` green. SYSTEM-only; no skill content touched.
- **Graded audit emitted false "no eval file exists" + "no portability block" on correctly-authored skills (A1 + A2, 2026-06-08T10:07Z).** `scripts/lib/audit-prompt-builder.js` built the grader context from raw `parseFrontmatter` only and located evals at the legacy `examples/evals/*.json` path. After ADR-0019 (audit/eval/provenance fields moved to the sibling `audit-state.json`) and the move to per-skill `skills/<name>/evals/`, this meant: (A1) a skill with `evals/comprehension.json` + `evals/application.json` on disk graded "Eval quality FAIL — no eval file exists"; (A2) `eval_artifacts` / `portability` / verdicts living in the sidecar were invisible to the grader, so it false-reported "eval_artifacts not present" / "no portability block". Fixes: `collectEvalArtifacts` now resolves per-skill `skills/<name>/evals/{comprehension,application}.json` FIRST (on-disk truth, flag-independent), falling back to `examples/evals/*.json` gated on the **joined** (sidecar-aware) `eval_artifacts` flag; `collectContext` reads+joins the sidecar (`readSidecar`/`joinSidecar`) and threads it through; `buildDimensionPrompt` embeds an `<audit-state>` block on the `eval` and `portability` dimensions so the grader judges those claims against the sidecar, not the SKILL.md body. Verified: `collectContext('design/component-architecture')` now returns both per-skill eval artifacts + `auditState.eval_artifacts=present`, and the eval/portability prompts embed `<audit-state>`. `test-collect-neighbor-summaries.js` still green. SYSTEM-only; no skill content touched.
- **Preflight false-reported Understanding 4/5 on correctly-migrated skills + emitted a stale `deployment_target` label (2026-06-08T10:07Z).** `scripts/skill-audit-preflight.js` hardcoded the deprecated top-level Understanding field name `boundary` in `UNDERSTANDING_FIELDS`, but the normalizer (`scripts/lib/parse-frontmatter.js:404-408`) canonicalizes `boundary`→`concept_boundary` (ADR-0018) before preflight sees the frontmatter — so a v8 skill that authored the canonical `concept_boundary` always scored 4/5 and an agent could "fix" a non-gap or revert a correct migration. Changed `UNDERSTANDING_FIELDS` to `concept_boundary` to mirror `scripts/skill-lint.js:85` (the alias is handled by the shared normalizer, exactly as lint relies on it). Separately, the `fmt()` v8 GAP row referenced `o.v8.deployment_target_valid` — a field `assess()` stopped returning when the `deployment_target` enum was replaced by the boolean `public` gate (ADR-0017 amendment) — so the GAP branch always appended a bogus ` [deployment_target invalid]`; corrected to `o.v8.public_valid` / ` [public invalid]`. Verified: `node scripts/skill-audit-preflight.js component-architecture --for comprehension` now reports understanding **5/5** + READY (was 4/5). SYSTEM-only; no skill content touched.

### Changed

- **Named one canonical `evaluate` command surface; labeled the others wrappers (E, 2026-06-08T10:07Z).** The same operation was invokable three ways (`bin/skill-graph.js evaluate`, `scripts/skill/evaluate-skill.js --comprehension`, `node lib/audit/evaluate-skill.js --mode`), with the runbook Quick start using the bare `lib/audit/evaluate-skill.js` implementation path. Declared `skill-graph evaluate` / `node bin/skill-graph.js evaluate` the canonical surface: switched the Quick start to it and added a note that it wraps the implementation `lib/audit/evaluate-skill.js`, and that the workspace `scripts/skill/evaluate-skill.js` is a divergent-fork wrapper (SH-6603). (The PRIMARY-loop + drain-appendix canonical-vs-wrapper notes landed in the Part 3 reorg, commit above.) No working wrapper deleted (code-preservation) — authority labeled only. `docs/reference/command-manifest.json` carries no `evaluate` entries (slash-commands only), so it was not applicable. Verified: `node bin/skill-graph.js evaluate --help` renders; `check-markdown-links.js` OK (1322 files). SYSTEM-only; no skill content touched.
- **Reorganized Part 3 of `SKILL_AUDIT_LOOP.md` into three audience-labeled parts (D, 2026-06-08T10:07Z).** Part 3 served four audiences at once (npm consumer, multi-agent drain, migration historian, harness-troubleshooter) with non-linear step numbering, which made it impossible for an executing agent to tell which path applied to it. Restructured, preserving 100% of operational content (verified: harness block moved not duplicated, all commands/steps/spot-checks survive, file grew 1045→1062 lines): (a) **promoted the minimal npm-consumer path** to a top-level `## The PRIMARY per-skill loop (single skill · default · START HERE)` — the clean linear 1-9 sequence; (b) gathered the lane-claim/ledger/census/worklist machinery + the workspace→npm mapping table under `## Appendix — Drain orchestration (MULTI-AGENT BATCH ONLY)`; (c) moved the inline heading-rename migration history (`## Concept Card`→`## Concept of the skill`, `## Philosophy`→`## Philosophy of the skill`, the garbled "renamed 2026-05-26" 4b parenthetical) here to the CHANGELOG, leaving a pointer in the step; (d) moved the SH-6353 subagent-`.md`-write harness workaround to `## Troubleshooting (reference)` with a pointer left in Step 7; (e) added a "Three audiences" map + per-part audience lines, and named the ONE canonical `evaluate` invocation (`skill-graph evaluate` → wraps `lib/audit/evaluate-skill.js`; `scripts/skill/evaluate-skill.js` is a divergent-fork wrapper, SH-6603). **Scope note (transparency, not silent reduction):** the `4b`/`4c`/`6-displacement`/`6b` sub-numbering remains in the drain appendix as orchestration detail — the PRIMARY loop is the clean linear sequence, so promoting it (not renumbering the detailed drain block, whose ~20 internal "Step N" cross-references that renumber would risk breaking) satisfies "linear primary loop." Verified: `check-markdown-links.js` OK (1322 files). SYSTEM-only; no skill content touched.
- **Width before verdict — advisory cross-review made CONTRACTUAL + new Phase 3.1 mandatory-verification gate (2026-06-07T19:20Z).** Operationalizes the workspace `no-lesser-models-for-quality § "Width before verdict"` companion rule in the multi-agent panel. Phase 3 curate: the frontier curator no longer "MAY fold in advisory cross-review where it adds value" (discretionary) — it **MUST examine and disposition every advisory cross-review finding** in the merge-ledger as incorporated / deferred-to-eval / rejected, each with a reason (silence ≠ permission to ignore). New **Phase 3.1 (mandatory verification gate)**: before eval, each mandatory frontier (Opus + GPT-5.5) independently verifies the merge represents its contribution, the advisory dispositions are honest, and no relied-on claim is load-bearing without reproduced evidence — bidirectionally (Opus checks GPT, GPT checks Opus, both check advisory) by re-running the command / reading the `file:line`; flagged gaps trigger a curator revision + re-verify (max 2 rounds); a ~2-min pass, not a re-propose. Phase 4 note clarified: the opt-in `--advisory` panel is breadth-only (graded by a frontier, recorded in `advisory_panel`, never feeds reconciliation); only the two mandatory frontier models cast the deciding verdict and must agree (else PROVISIONAL). Edited the five-phase list + the per-step table in `skill-audit-loop/SKILL_AUDIT_LOOP.md`. Prompted by user request 2026-06-07 for "most width and quality": advisory does its own research + cross-evaluates everyone; frontier Opus/GPT hold the final verdict and verify all claims. SYSTEM-only; no skill content touched. Companion: workspace `.claude/rules/no-lesser-models-for-quality.md` (commit `a8c02539e`).
- **CORRECTION — panel PRIMARY surface is the native Task panel (`TaskCreate`/`TaskUpdate`), NOT the Agent-tool subagent panel (2026-06-07T18:49Z).** The 2026-06-07T15:30 entry below (commit `4d826a5`) wrongly named the PRIMARY panel surface as the "native Claude Code subagent panel (the `↑/↓ · Enter to view` list, like `/manage`)" via Agent-tool dispatch, and treated `watch-panel`/`Monitor` as a co-equal main-area surface. Per user confirmation 2026-06-07, the pinned, interactive panel is the native **Task panel = the harness `TaskCreate` / `TaskUpdate` tools** (the pinned checklist at the bottom of the main conversation): ONE in_progress header task whose `activeForm` is the live rollup (`⟳ Skill Audit Loop · <skill> · <phase> · N/M done · 2 QUALITY/4 advisory`) + one task per phase whose `subject` encodes the participating models + per-model state (`✓Opus[Q] ⟳GPT-5.5[Q] ·Nemotron …`), flipped live by `TaskUpdate`; true parent/child UI nesting is not a Task-tool primitive, so the hierarchy is encoded in the subjects. Corrected `commands/audit/skill-audit-loop.md § A2` (PRIMARY = Task panel; FALLBACK = `watch-panel`/`Monitor` reframed as a scrolling, not-pinned block; statusline still never the panel) and `skill-audit-loop/SKILL_AUDIT_LOOP.md` (§ "Surface ordering", § "In-session Agent-tool dispatch" reframed as the execution layer underneath the Task panel, § "Canonical way to run the PANEL loop VISIBLY"). The Agent-tool per-model dispatch (`propose-one.js`/`cross-review-one.js`/`revise-one.js`/`curate-one.js`) is preserved as the execution layer, not the panel surface. SYSTEM-only; no skill content touched. Companion: root-repo CHANGELOG + shims (`.claude/commands/skill-audit-loop.md`, `.opencode/commands/skill-audit-loop.md`) and `/boardmeeting` (Phase 0c `TaskCreate` = the panel; Phase 2b fallback demotion).
- **Panel-visibility surface fixed to the MAIN conversation area, never the statusline (2026-06-07T15:30+02:00).** The `/skill-audit-loop` command had regressed to documenting the per-skill **statusline bridge** as the default/primary way to surface the multi-agent panel — exactly the surface the user has repeatedly rejected. Rewrote `commands/audit/skill-audit-loop.md § A2` to an explicit three-tier priority: **PRIMARY** = the native Claude Code subagent panel via Agent-tool dispatch (the `↑/↓ · Enter to view` list, like `/manage`) using the per-model primitives `propose-one.js`/`cross-review-one.js`/`revise-one.js`/`curate-one.js` (orchestrator reads each `result.json`); **FALLBACK** = the `Monitor` + `scripts/watch-panel.js <heartbeat>` collected block (the same viewer `/boardmeeting` uses); **SECONDARY** = the statusline bridge as a cheap complement ONLY, with an explicit "DO NOT surface the panel via the statusline; the panel belongs in the main conversation area" note. Mirrored the same priority ordering + a one-line "operator MUST surface in the main area" rule into `skill-audit-loop/SKILL_AUDIT_LOOP.md` (§ Visibility + § Canonical way to run the PANEL loop VISIBLY). Demoted the statusline framing in `scripts/run-panel-loop.sh` (header + inline comments + the module-table row in SKILL_AUDIT_LOOP.md): the canonical viewer for the unattended drain's heartbeat is `watch-panel.js`; the statusline bridge stays only as an optional complement for that headless terminal run (NOT removed). No code logic changed; `test-panel-progress.js` (12) green. SYSTEM-only; no skill content touched. Companion: root-repo shims `.claude/commands/skill-audit-loop.md` (+ visibility note) and `.opencode/commands/skill-audit-loop.md` (batch loop, + visibility note).
- **Renamed the panel runner to the Skill Audit Loop + purged "enrich" naming cruft (2026-06-07T14:47+02:00).** "Enrich"/"Panel Enrich"/"THE OFFICIAL LOOP" were agent-legacy labels the user never authored; the thing is the **Skill Audit Loop** (the panel is its multi-model implementation). Renamed 10 files (run-panel-enrich.js -> run-skill-audit-loop.js, run-bidirectional-enrich.js -> run-skill-audit-loop-lite.js, the two *-live-deps, 4 tests, the improve-pass prompt, audit-loop-enrich-philosophy.md -> skill-audit-loop-philosophy.md) + all path/symbol refs (runPanelEnrich -> runSkillAuditLoop, createPanelEnrichDeps -> createSkillAuditLoopDeps, curator AGENT_ID enrich-curator -> skill-audit-loop-curator); purged "enrich" prose from SKILL_AUDIT_LOOP.md, AGENTS.md, the philosophy doc, the command + shims, and the lib/audit code comments (objective value -> skill-audit-loop; verb -> "curate"). Recover prior names via git history. **Follow-up (not done):** internal helper symbols still carry "Enrich" (buildEnrichPrompt, build{Claude,Codex,Gemini,Opencode}EnrichArgs, loadEnrichPromptTemplate, prepareEnrichedEval, extractEnrichedDoc, the SKILL_ENRICH_OS_FENCE / SKILL_ENRICH_CLI_TIMEOUT_MS env vars, the skill-panel-enrich loop id) — a deeper code/registration refactor, filed as a SYSTEM follow-up. test:unit green throughout.

### Added

- **ADR-0021 — Width Before Verdict (2026-06-07T19:25Z).** `docs/adr/0021-width-before-verdict-advisory-cross-review-and-mandatory-verification.md` records the decision + rationale behind the contractual advisory cross-review + Phase 3.1 verification gate (implemented in `003dbe9`, policy in workspace `a8c02539e`): advisory does independent research + cross-evaluates everyone (width); frontier verifies every relied-on claim bidirectionally before it is load-bearing and casts the dual deciding verdict (quality); width and quality are sequential gates, never in tension. Documents what was already built (Phases 1/2/4 + anti-loss) vs the three gaps closed.
- **`/skill-audit-loop` now runs + records the FULL per-skill loop, not just the improve step (2026-06-07T14:47+02:00).** recordFullLoop() in run-skill-audit-loop.js main() (CLI path; runSkillAuditLoop stays pure/DI) runs the **Integrity Gate** (skill-audit.js -> structural/truth/lint/last_audited, always) and, on keep, records the **Behavior Gate**: it reuses the bidirectional guardrail receipt (which IS runBidirectionalEval, the canonical grader) for the eval-mode dimension (eval_last_run.bidirectional + verdict, honestly capped to PROVISIONAL unless certifying-clean) and runs canonical evaluate for the other dimension when its artifact exists; a missing eval artifact records UNVERIFIED + an explicit finding (never a silent skip — Completion Rule). Recording is OBSERVATIONAL (runs after keep-or-revert, never feeds it — the philosophy guardrail is unchanged). Closes the defect where a skill run through the panel still showed all-UNVERIFIED verdicts.

### Deprecated

- **`/evolve` is deprecated in favor of `/skill-audit-loop` + `run-panel-loop.sh --worklist` (2026-06-07T14:47+02:00).** The legacy corpus walker (and its run-skill-improvement-loop.js engine) is superseded: the single-skill /skill-audit-loop now runs the full audit -> improve -> evaluate loop and records the four verdicts; corpus-scale draining is scripts/run-panel-loop.sh --worklist. commands/audit/evolve.md + the .claude shim are marked status: deprecated, superseded_by: skill-audit-loop. Kept for back-compat (the engine is still referenced by skill-evolution-loop.js); removing the redundant engine to git history is a SYSTEM follow-up once no consumer remains.

### Removed

- **Completed/superseded plans now leave the live tree instead of moving into `docs/_archived/` (2026-06-07T13:16+02:00).** Removed the remaining tracked completed-plan files from `docs/_archived/` and the stale completed handoff/plan files from `docs/plans/` with `git rm`; git history is the recovery path. Updated the Skill Graph document routing table, docs front door, plans index, draft index, recommended-skills note, generated marketplace queue source, package file list, audit-loop removal wording, and doc-check scripts so the current rule is explicit: active plans live in `docs/plans/`; completed or superseded plans are deleted from the live tree and recovered with git history, not preserved in an archive directory. Removed `_archived` leniency from `check-markdown-links.js`, `check-doc-drift.js`, and `check-inline-skill-counts.js`, so old archive references no longer get special treatment in active checks. Historical changelog entries that mention `docs/_archived/` remain as past-state records, not current routing guidance. SYSTEM-only; no skill content touched.
- **Completed the `337fe22` migration-codemod clean-cut - deleted 2 orphan tests + 4 stale references (2026-06-06T).** `337fe22` (per [ADR-0014](docs/adr/0014-canonical-only-schema-files.md)) retired the v2→v7 migration codemods (`scripts/migrate-skill-vN-to-vM.js`) to git history, but left their tests and several live references behind - a half-finished clean-cut. Removed: (1) `scripts/__tests__/migrate-skill-v2-to-v3.test.js` + `migrate-skill-v3-to-v4.test.js` - orphan tests whose target codemods are gone; they had self-skipped forever ("legacy migration CLI not present") and were wired only into `package.json`'s `test:migrate`. (2) The now-empty `test:migrate` npm script + its reference in `test` (now just `npm run test:unit`; `verify` already called `test:unit` directly, unaffected). (3) `SKILL_GRAPH.md` - the Tier-3 script-table row for the deleted `scripts/migrate-skill-v2-to-v3.js` and the stale Tier-1 rule #3 ("an author running migrate-skill-v2-to-v3.js gets the same result"; "Three rules"→"Two rules"). (4) `scripts/generate-manifest.js` - two deprecated-scalar WARN messages told users to "run scripts/migrate-skill-v2-to-v3.js" (a deleted file); the deprecation warnings stay (defensive, behavior unchanged), the broken pointer is dropped. Recover any codemod or test via `git show 337fe22^:<path>`. Frozen records (ADR-0014, research followups) correctly retain the historical mentions - they describe past state and are the recovery pointer. A read-only dead-code detective sweep of all 89 in-scope `lib/`+`scripts/`+`bin/`+`prompts/` files found ZERO other dead code (consistent with the 2026-05-28 cleanup; no new accumulation). Verified: `docs:drift` OK (78 docs), `node --check` on generate-manifest, no test asserts the changed strings, `test:migrate` correctly gone. SYSTEM-only; no skill content touched.

### Added

- **Codex GPT-5.5 evaluator profile for Claude-unavailable audit gates (2026-06-07T09:45+02:00).** Added canonical wrapper `lib/audit/evaluate-skill-codex-gpt-5.5.js` plus public CLI entries `skill-graph evaluate:gpt-5.5` and `skill-graph evaluate:codex-gpt-5.5`. The wrapper delegates to the existing `evaluate-skill.js` implementation while injecting `--grader codex`, `--generator codex`, `--tools-on`, `--single-model`, and the four comprehension/application model env vars pinned to `gpt-5.5`. This gives operators a version-controlled Codex GPT-5.5 path when the default evaluator would route through Claude, without duplicating grader logic or pretending same-family evidence can certify `PASS` / `APPLICABLE`. Added `npm run evaluate:gpt-5.5`, command docs, and a unit test for the injected args/env/fail-closed behavior. SYSTEM-only; no skill content touched.
- **Audit doctrine: "Breadth is not a finding" (2026-06-06T19:00Z).** Added a normative clause to the Audit Doctrine codifying that a deliberately broad, cross-cutting skill is not defective for being broad: declared breadth (a wide `scope`/`description`, or many `relations.boundary` edges) is **intent fidelity, not drift**, and topic overlap between related skills is **recall, not a defect** to drive to zero — the same principle already stated for shared keywords in `docs/quality-doctrine.md § "Reading skill-overlap.js output"`, now extended to scope + boundary edges. Auditors must judge each *addition* on correctness/placement/non-contradiction/organization, never on scope-narrowness; "make this skill narrower" is a user scope-decision, not a finding. A breadth-related finding is legitimate only when the breadth yields a *concrete* defect (a wrong claim, a misplaced section, a real routing ambiguity with no disambiguating edge, or self-contradiction). Canonical statement in the workspace pipeline doc `docs/reference/skill-audit-pipeline.md § "Breadth is not a finding"` (under "What the audit is NOT"); mirrored here as a one-paragraph clause after the "not a lint-test factory" statement in `SKILL_AUDIT_LOOP.md`. Doc-only; no schema/script/skill touched. Prompted by a 2026-06-06 session agreement that breadth was being mistaken for an audit defect.
- **`run-panel-loop.sh --worklist` + `scripts/start-panel-drain.sh` — unattended corpus-wide panel-enrich DRAIN (2026-06-06T16:40Z).** Hardened the batch driver into a supervised loop that "looks through every skill" automatically, and added a visible-session launcher. `run-panel-loop.sh --worklist` drains EVERY eligible skill via the shared claim/ledger (`scripts/skill/skill-audit-claim.js` `next`→`claim`→`release`): ranked + public-safe (reuses the claim system's private-skill filter), atomically claimed via `fs.linkSync` so it **cannot double-process** with the OpenCode `skill-audit` loop, ledger-completed skills auto-skipped, resumable corpus-wide (a session-stable `AGENT_ID` pairs claim+release). New resilience: a per-skill **watchdog** (`--timeout`, default 5400s; ports `grind-loop.sh:1030-1067` `start_watchdog` + a `pgrep -P` `kill_tree` that reaps the whole enrich process tree, mapping the 143/137 exit to a `timeout` ledger entry and continuing), an exhausted-lock **budget gate** for the mandatory frontier, best-effort `loop-checkpoint` writes (loop id `skill-panel-enrich`, registered in `loops.manifest.json` with `phase_thresholds.processing=100` so `loop-supervisor.js` can monitor it) and `loop-steering.json` (`pause_after_current`) honoring. `--skills-file` (curated sets) + `--dry-run` (finite offline single-skill preview, no claim/release/commit) retained. `start-panel-drain.sh` is the chosen vehicle: refreshes the worklist (`build-skill-list.js --write`), runs an auth PREFLIGHT (hard-requires claude+codex; prints the one-time `gemini /auth` / `opencode auth login` commands for the advisory tier — full-panel only holds when they're logged in, `GEMINI_API_KEY` unset per subscriptions-not-API), then spawns the drain in a visible Ghostty tab (`--here` for the current terminal). SYSTEM infra (canonical-location); CONTENT enrich commits stay separate under `AUDIT_LOOP=1`. Verified: `bash -n` both; watchdog kills a fake-hung child (exit 143) on schedule; `next --json` parse → top worklist skill; worklist `--dry-run` preview resolves the dir + runs the dry enrich with NO canonical claim/release/commit; launcher preflight passes claude+codex and hands off to the dry preview. Headless-auth reality (verified): Opus+GPT run detached; Gemini+OpenCode advisory need one-time interactive OAuth and otherwise skip non-blocking. Docs: `SKILL_AUDIT_LOOP.md` module table (+2 rows). Companion to the original driver entry below.
- **`scripts/run-panel-loop.sh` — resumable BATCH driver for the multi-agent panel enrich loop (2026-06-06T16:05Z).** The single-skill `/skill-audit-loop` command and `lib/audit/run-panel-enrich.js` cover one skill; there was no committed way to run the panel loop over a *list* of skills overnight. This bridge-wired bash driver walks a `--skills-file` of slugs and, per skill: resolves the canonical dir under `~/Development/skills/skills`, runs `run-panel-enrich.js` (full advisory panel by default; `--no-advisory` for the certifying floor only; `--max-rounds N`), then on the runner's exit-code contract — `0` KEPT → commits `SKILL.md` (+`audit-state.json`) path-limited in `~/Development/skills` under `AUDIT_LOOP=1` (CONTENT); `2` REVERTED → nothing committed; `1` crash → logged, batch continues. A per-skill statusline bridge (`scripts/agent/panel-heartbeat-to-agent-state.js`) is launched against each skill's own heartbeat so the multi-agent panel paints ABOVE the session statusline and the batch is never a blind background run — relaunched per skill (each bridge self-exits on its skill's `complete:true`, so a shared file would kill it after skill 1). Resumable via an append-only `batch-ledger.jsonl` (`applied|reverted|failed|missing|noop` per slug; re-runs skip terminal-success slugs, retry failures). `--dry-run` passes through to the runner AND skips the CONTENT commit for offline plumbing tests. Liveness is owned-PID `kill -0` only (never `ps`/`pgrep`, per `no-ps-for-liveness`); commits are `git commit --only` path-limited (per `multi-session-commits`). SYSTEM infra (canonical-location: Skill-Graph code lives in `skill-graph/`); it never mixes its own SYSTEM commit with the CONTENT commits it makes. Verified: `bash -n` clean; offline `--dry-run` smoke test on `methodical` resolved the dir, parsed the result JSON (`applied`/`keep`), correctly logged `noop` (dry apply=false) with no commit, and wrote an ISO-timestamped ledger line.
- **`schemas/panel-heartbeat.schema.json` — shared collected-TUI heartbeat contract; `watch-panel.js` now reused by `/boardmeeting` (2026-06-06T10:35Z).** Added a JSON Schema (draft 2020-12) for the panel heartbeat `status.json` shape (`{ ts, pid, skill, phase, elapsed_s, total, done, failed, running[], complete, agents:[{model, tier, phase, state, elapsed_s}] }`; `required`: `ts, skill, phase, total, done, failed, complete, agents`; tier enum `mandatory|quality|advisory`; state enum `queued|proposing|proposed|reviewing|revising|running|done|failed|skipped|timeout|complete`). This is now the single shared contract both heartbeat producers conform to: `lib/audit/panel-progress.js` (the Skill Audit Loop panel-enrich runner, whose header comment now points at the schema) AND the workspace `/boardmeeting` writer `scripts/agent/boardmeeting-heartbeat.js`. The collected-TUI viewer `scripts/watch-panel.js` (and its `renderCollected` renderer in `panel-progress.js`) stay single-canonical here; the workspace `/boardmeeting` Phase 2 now CONSUMES `watch-panel.js` read-only (via the `Monitor` tool) so its multi-agent panel renders byte-identically to `/skill-audit-loop`. Registered as the cross-repo `panel-heartbeat-viewer` reuse in the workspace `docs/reference/implementation-ownership.md` (read-only consumption, not a shim — `watch-panel.js` is invoked, never copied). `panel-progress.js` logic is unchanged (header note only); no schema-version bump (additive new schema file). SYSTEM-only; no skill content touched. Verified: a fake `/boardmeeting` heartbeat (3 agents, quality+advisory, 1 done/1 failed) rendered correctly by `watch-panel.js --once`; both new JS files pass `node --check`; the schema is valid JSON.
- **`/skill-audit-loop <skill> <project-or-repo>` — interactive single-skill enrich-then-apply command (2026-06-06T13:10Z).** New canonical command body `commands/audit/skill-audit-loop.md` (thin Claude resolver at `~/Development/.claude/commands/skill-audit-loop.md`). Two phases: **Phase A (CONTENT)** runs ONE skill through the official panel enrich loop (`lib/audit/run-panel-enrich.js` — Opus 4.8 + GPT-5.5 mandatory + advisory, cross-review to convergence, frontier-curated anti-loss synthesis, bidirectional eval guardrail + keep/revert), then commits the enriched `SKILL.md` (+`audit-state.json`) path-limited in `~/Development/skills` under `AUDIT_LOOP=1`; **Phase B** loads the now-enriched skill as a lens and USES it on the named project/repo (arg 2), producing a complete severity-tagged findings report (report-only by default; `--act` to fix + commit in the target repo). Flags: `--no-advisory`, `--max-rounds N`, `--enrich-only`, `--apply-only`, `--act`. The live runner is handed to the user via `!` (foreground, uncapped) per the foreground-loop rule; the `watch-panel.js` collected TUI streams in-session. Distinct from — and does NOT touch — the autonomous batch queue-worker `.opencode/commands/skill-audit-loop.md` (drains the whole worklist, no skill arg; verbatim `"prompt"`-loop registered in `scripts/loop/run-loop-sweep.sh`): same name, different runtime, different job. Honors the private-content boundary (Phase A research is public-only; Phase B findings never flow back into the skill). SYSTEM-only (building the command); its outputs are CONTENT (Phase A) / target-repo (Phase B `--act`).
- **Panel-enrich COLLECTED multi-agent TUI as canonical skill-graph code + viewer (2026-06-05T14:40Z).** Extends the visibility layer with `renderCollected(status)` in `lib/audit/panel-progress.js` — THE single, pure (no I/O, no ANSI) renderer of the collected view (summary header + one tree row per agent: Opus/GPT MANDATORY + free advisory, with phase/state/elapsed), reused by the live TTY header AND a new viewer. New canonical viewer `scripts/watch-panel.js` (observer only) reads the heartbeat `status.json` and paints `renderCollected()`: full-screen live refresh on a TTY, a collected block per change when piped — so the collected multi-agent view is watchable **in-session** (background task / Monitor), not only in a separate terminal. The heartbeat gained a top-level `elapsed_s`. This is the deliberate, user-confirmed shape: the collected TUI is canonical, version-controlled code in `skill-graph/` — NOT the harness Agent-tool tree (which can't be committed/reproduced) and NOT per-run session improvisation. `test-panel-progress.js` gains 3 `renderCollected` cases (10 total); `test:unit` exits 0. Verified: a dry-run heartbeat renders the full 9-agent collected view → `COMPLETE 9/9`. Docs: `SKILL_AUDIT_LOOP.md` module table (panel-progress `renderCollected` + watch-panel rows) + the "Canonical way to run the PANEL loop VISIBLY" section rewritten around `watch-panel.js`. SYSTEM-only; no skill content touched.
- **Panel-enrich heartbeat `status.json` + pinned-header TUI — the loop now runs VISIBLY and can never go stale unnoticed (2026-06-05T13:20Z).** New module `lib/audit/panel-progress.js` is the visibility layer: an injected `onProgress(event)` sink threaded through `runPanelEnrich` + `runConvergence` (fired at every agent start/finish and phase transition; default no-op so the orchestrator stays pure and the `assert.throws` unit contract stays synchronous). It (a) writes the documented heartbeat `status.json` — the `scripts/watch-audit-batch.sh` contract `{ts,pid,skill,phase,total,done,failed,running[],complete,agents[]}`, a superset of `dist/ab/comprehension-ab-driver.js writeStatus()`, where `agents[]` adds per-model tier/state — so a hang/crash surfaces to the Monitor as `HANG`/`STALE`, never silent staleness; and (b) on a real TTY pins a header with one row per panel agent (Opus/GPT **MANDATORY**, free models **advisory**) showing live phase/state via the `scripts/loop/manage-cycle.sh` scroll-region pattern, the `[+Ns]` log scrolling below. New `run-panel-enrich.js` flags: `--status-file <path>` (default under the per-skill run-dir tree), `--no-tui`, `--quiet`. Closes the documented gap in `SKILL_AUDIT_LOOP.md § "Running long batches… heartbeat + active Monitor"` ("canonical runners do not yet emit the heartbeat") for the panel runner. Verified: dry-run E2E writes a valid heartbeat (`total:9`, `complete:true`) that `watch-audit-batch.sh` consumes → `PROGRESS 9/9` → `COMPLETE`; new unit suite `scripts/__tests__/test-panel-progress.js` (7 cases — heartbeat shape, progress transitions, fail/skip counting, TTY gating, teardown) wired into `test:unit`; full `test:unit` exits 0. Docs: `SKILL_AUDIT_LOOP.md` gains a per-step-semantics table (what each phase means / who acts / expected outcome / abort rule), a "Visibility" subsection, the "Canonical way to run the PANEL loop VISIBLY" (spawn-in-Ghostty + Monitor) invocation, and the `panel-progress.js` module row. **Deferred (tracked follow-up):** true in-process *parallel* dispatch — the orchestrator stays synchronous (agents run one-at-a-time; heartbeat ticks at phase boundaries, `--proc` liveness covers mid-dispatch); async would break the `assert.throws` contract + touch the shared `enrich-live-deps.js` path, so it lands as its own test-migrated change. SYSTEM-only; no skill content touched.
- **Panel-enrich now streams phase + per-agent progress live so it runs VISIBLY in the foreground (2026-06-05T14:05Z).** `run-panel-enrich.js` was silent until its final JSON — which forced operators to run it as a polled black-box background process (the exact anti-pattern this work is removing; see `~/Development/.claude/rules/no-ps-for-liveness.md`). Added an injected `log` callback threaded through `runPanelEnrich` + `runConvergence` (default no-op; pure-orchestration contract preserved — the orchestrator still never touches fs). `main()` wires it to **stderr** with `[+Ns]` elapsed stamps, streaming each boundary live: research brief → phase 1a mandatory propose (per model, with the "multi-minute buffered call" note so a quiet terminal isn't misread as a hang) → phase 1b advisory (per model, ✓/✗) → phase 2 cross-review/revise per round (per reviewer + reviser + stability) → phase 3 curate → phase 4 eval guardrail (run/skipped) → phase 4b keep/revert decision → phase 5 apply. Final result JSON stays on **stdout** so batch drivers still parse it; `--quiet` silences the stream. 17/17 panel tests unaffected (default no-op log). SYSTEM-only.

### Fixed

- **System verification green after marketplace/status regeneration and stale publish guidance cleanup (2026-06-07T13:36+02:00).** Regenerated the public marketplace mirror with `scripts/export-marketplace-skills.js`, bringing the generated surface to 166 exported public skills and adding the missing generated exports for `bcg-matrix`, `value-chain-analysis`, and `vrio`; then regenerated `docs/status.generated.md` with `scripts/build-status-doc.js`. Updated current publish docs and marketplace release prompt prose so they no longer claim exported skills emit `skill_graph_protocol: Skill Metadata Protocol v7`; the live contract is factual provenance (`skill_graph_source_repo`, `skill_graph_project`, `skill_graph_canonical_skill`) plus stringified protocol metadata when present. Fixed the active improve-loop prompt to point at `skill-metadata-protocol/SKILL_METADATA_PROTOCOL.md` + `examples/skill-metadata-template.md`, use the 12-subject enum, describe `schema_version`/audit verdicts as sidecar-owned, and stop teaching retired capability/workflow/router/overlay body shapes. SYSTEM-only; generated marketplace outputs were regenerated, not hand-edited.
- **`run-panel-loop.sh --worklist` now ADVANCES instead of re-enriching the top skill forever (2026-06-06T17:55Z).** The drain looped on `skill-audit-claim.js next`, but `next` filters on the worklist's STATIC `status` field (`SKILL_LIST.json`, built once) — which a ledger completion does NOT refresh. So after `api-design` was enriched + committed (`982212c`) and released `completed`, `next` returned `api-design` again (worklist still said `pending`), and the drain re-claimed + re-enriched it (`[drain #2] api-design`) — it would have re-done skill #1 forever and never reached skill #2. Verified root cause: `build-skill-list.js --write` flips api-design to `status: completed` and only THEN does `next` return the next skill (`code-review`). Fix: `drain_worklist` now snapshots the ranked, public-safe, not-yet-completed slugs from `SKILL_LIST.json` ONCE and iterates the ARRAY (each slug exactly once, so a revert/fail moves on instead of looping), skips any slug already carrying a `content(<slug>): panel-enrich` commit (resume-safe), claims for cross-process dedup, and runs `build-skill-list.js --write` after each skill so a restart's snapshot excludes what just completed. Verified: `--worklist --dry-run` reports 99 eligible, skips the completed api-design, and previews `code-review`. (The api-design `982212c` enrichment itself was correct + kept — only advancement was broken.) Companion: the same session added `skill-panel-enrich` to `loop-checkpoint.js` VALID_LOOPS (Development `36736b4e8`) so checkpoints persist for supervisor monitoring. **Also fixed (same session): orphaned-slot poisoning.** The panel claims fixed per-model agent ids (`enrich-opus`, …) under a "one skill per agent" guard; a KILLED run leaves a per-model slot held, which then refuses every subsequent skill's phase-1a claim (`CLAIM REFUSED — enrich-opus still holds api-design`) → all 99 skills failed exit 1. `reap` did not clear it (malformed/`undefined`-holder payload). Fix: `drain_worklist` now clears panel per-model slot locks (`.claude/agent-memory/skill-audit-*--<model>`) at startup — the drain is the sole panel runner, so no slot is legitimately held at startup; a killed run self-heals on restart.
- **`renderCollected` is now tier-aware so the shared collected TUI serves BOTH panels (2026-06-06T12:50+02:00).** The renderer (`lib/audit/panel-progress.js`, single-canonical, reused by `/boardmeeting` via the unify-TUI change) only treated `tier === 'mandatory'` as the grade-carrying core, so `/boardmeeting`'s `quality`-tier co-reviewer (GPT) rendered mislabeled as `[advisory]` and was undercounted — and quality-vs-advisory is load-bearing in boardmeeting synthesis. Fixed: the top-tier count now includes `mandatory` OR `quality`; the per-row label shows `MANDATORY` / `QUALITY` / `advisory` by actual tier; the summary line reads `N MANDATORY (Opus + GPT)` for the skill-audit-loop panel (byte-unchanged) and `N QUALITY` for the boardmeeting panel. Verified: boardmeeting case → `2 QUALITY · 1 advisory` with `[QUALITY]` rows; skill-audit-loop case identical to before. `test-panel-progress.js` +2 cases (10→12, both green). SYSTEM-only. (Closes the deviation the unify-TUI subagent flagged in `a747ece`.)
- **Panel-enrich advisory delivery pivoted to TEXT-CAPTURE + the source-read-only fence series — first full live panel run landed (2026-06-06T).** Four SYSTEM fixes, each its own path-limited commit, hardened the in-session Agent-tool panel loop and were proven by the first end-to-end live run (`eval-driven-development`, 3 rounds × 8 models, Opus-curated, applied + committed to `jacob-balslev/skills` as `87f4a7f` — 248→528 lines; anti-loss + mandatory-coverage clean; lint clean; `subject: ai-engineering` kept over the GPT-5.5/Gemini-Flash subject misreads). (1) **`b680f11`** — `isolated-checkout.js::buildSeatbeltProfile` gained an optional `readOnlyRoots` set (emitted `(allow file-read* …)` before the read-write roots so a nested run-dir RW root wins by SBPL last-match; absent ⇒ byte-identical to the prior all-RW profile). The advisory fence now makes the skill SOURCE (skill-graph repo + skills library) **read-only** — a write EPERMs, so an advisory model physically cannot mutate a canonical `SKILL.md`; only the audit run-dir trees are writable. (2) **`ecd7df6`** — advisory `reviseProposal` delivery converted from the relative-file-write (the entry below) to **stdout TEXT-CAPTURE**, mirroring the propose path: sandboxed advisory models research under the read-only fence and EMIT the revised `SKILL.md` as reply text, captured via `extractEnrichedDoc`/`looksLikeSkillDoc` and written by us; frontier claude/codex keep native write-mode. **This supersedes the relative-write delivery recorded immediately below.** Convergence stays hash-authoritative; a text-capture reviser re-emits non-byte-identical text each round, so the `maxRounds` budget is the designed terminator. (3) **`431a34f`** — the Seatbelt deny anchor also EPERMed an lstat of the workspace-root NODE, which the **Gemini CLI** does at startup (`isWorkspaceHomeDir`), aborting it before any work; the fence now allows `file-read-metadata` on the workspace-root *literal* (readdir + private read/write stay denied — no private sibling leaks). (4) **`7c6becf`** — the panel hardcoded `osFenceActive:false` for codex, so under the outer Seatbelt codex started its OWN nested `-s workspace-write` sandbox; macOS denies nesting `sandbox_apply`, so codex (GPT-5.5, a MANDATORY frontier) silently wrote nothing and lost its revisions. The panel now passes `advisoryOsFence.active` → codex drops its inner sandbox (`--dangerously-bypass-approvals-and-sandbox`, SKI-169 Fix B). Tests: `test-isolated-checkout.js` 11→16 (+5, incl. live asserts: source read-OK / source write-DENIED / nested run-dir write-ALLOWED / workspace-root lstat ALLOWED / readdir DENIED); `test-panel-enrich-live-deps.js` 7→9 (+2 text-capture revise). Live-verified each fix (DeepSeek propose, Gemini probe, codex write probe, GPT-5.5 re-revise). Docs reconciled: `SKILL_AUDIT_LOOP.md` + `AGENTS.md` delivery contract (two tiers) + § "Kernel-fence interactions". SYSTEM-only; the CONTENT skill change is in the `skills` repo, not here.
- **Panel-enrich `reviseProposal` now uses the sandbox-correct relative-write delivery for advisory models (2026-06-05T13:30Z).** _(Superseded 2026-06-06 by stdout text-capture in `ecd7df6` — see the entry above. Retained as the dated timeline of the unreleased window.)_ In the convergence loop, `reviseProposal` (`lib/audit/panel-enrich-live-deps.js`) is dispatched for EVERY alive proposal — frontier (claude/codex) AND advisory (gemini/opencode). It previously always instructed an **absolute** overwrite (`OVERWRITE the proposal file at <abs path>`) and dispatched with `cwd: skillGraphRoot`, which silently misdelivered for gemini (which writes relative to cwd → wrote into the skill-graph root, not the run dir) and risked opencode's `external_directory` rejection. It now mirrors `dispatchWriteProposal` (the propose path): sandboxed advisory CLIs get a RELATIVE-filename overwrite (`basename(ownProposalPath)`) into the dir holding their own proposal, with `cwd = gemini ? writeDir : skillGraphRoot` (opencode write-scope via `--dir`), and the current proposal + canonical body inlined so no external read is attempted; frontier claude/codex keep the absolute-path overwrite they handle natively (codex via `writableRoots`). Verified: 24/24 panel tests pass (`test-panel-enrich.js` 17 + `test-panel-enrich-live-deps.js` 7). Live note: gemini advisory delivery is code-correct but currently unauthenticated for non-interactive use (`~/.gemini/settings.json selectedType: gemini-api-key` with no key; AI Studio subscription needs an interactive `gemini /auth` OAuth login — `GEMINI_API_KEY` is deliberately NOT set, per the subscriptions-not-API rule). Advisory failures are best-effort and never block the loop; the certifying floor is the mandatory frontier pair (Opus + GPT-via-codex), both verified present. SYSTEM-only.

### Changed

- **`/skill-audit-loop` A2/A3 corrected: the command RUNS the loop + attaches the `Monitor` viewer; it never hands the user a `!` paste (2026-06-06T12:40Z).** The first cut of `commands/audit/skill-audit-loop.md` told the agent to hand the user a `! node run-panel-enrich.js …` command — bad UX (the user invoked the slash command; pasting a runner after that defeats the point) and it produced repeated blind-background-run failures. Rewrote A2/A3 to the **validated** mechanism, proven live on the first `methodical` panel run: (1) launch `run-panel-enrich.js` via the Bash tool (the harness runs the ~15-25 min command as a managed task, notifies on exit); (2) **immediately attach `watch-panel.js` via the `Monitor` tool** (persistent) so the collected multi-agent view streams in-session. The documented requirement (`SKILL_AUDIT_LOOP.md` § "Canonical way to run the PANEL loop VISIBLY") is *"do NOT launch the runner as a blind background task **with no viewer attached**"* — the failure mode is a *blind* run, not the managed task. A3 now says: wait for the completion notification (authoritative done-signal), never `ps`/poll/tail; a STALE event during slow advisory dispatch is "quiet ≠ dead." SYSTEM-only.
- **Audit slash-command bodies migrated into `skill-graph/commands/audit/` (canonical-location migration Stage 1a, 2026-06-05T11:30Z).** Per the canonical-location directive (`AGENTS.md § Canonical Location`), the six audit commands (`audit`, `improve`, `evaluate`, `evolve`, `discover`, `merge`) now have their canonical definitions in `skill-graph/commands/audit/<name>.md`. The workspace `.claude/commands/audit/<name>.md` files are reduced to **thin resolver shims** (frontmatter for slash resolution + a one-line pointer, no logic) — they exist only because Claude Code resolves user-typed `/commands` solely from `.claude/commands/`. Recover prior inline bodies via `git show <pre-2026-06-05>:.claude/commands/audit/<name>.md`. Stage 1b (`.opencode/commands/*` skill commands) + Stage 2 (the 43 `scripts/skill/*` scripts, with shims for workspace consumers) follow; ADR-0015/0016 amendment pending. SYSTEM-only; no skill content touched.
- **Canonical-location migration Stage 1b reviewed — no OpenCode command body needed migrating; corrects the resume-plan's "move all 7" over-scope (2026-06-05T13:05Z).** All seven `.opencode/commands/*` skill/audit commands were verified against `AGENTS.md § Canonical Location` and live wiring. Outcome by file: (1) `audit-skill.md` and `skill-audit-merge-v1.md` are **already** thin pointers into skill-graph (`SKILL_AUDIT_LOOP.md § Part 3` / `audits/merge-protocol.md`) — compliant, unchanged. (2) `skill-audit-loop.md`, `skill-upgrade-loop.md`, `skill-compound-loop.md` are workspace **loop-orchestration prompts consumed verbatim** by `scripts/loop/run-loop-sweep.sh register_loop … "prompt"`; their bodies drive `.opencode/progress/` queue data (ADR-0016 workspace-coordinated data) and `skill-compound-loop` is Sales-Hub-grounded (forbidden in skill-graph per the private-content boundary). Shimming them would gut the dispatcher prompt and break the live loop system — they stay (ADR-0015: runtime resolvers/UX are workspace-owned; the audit *method* already defers to skill-graph). (3) `skill-search.md` wraps the skill-injector feeders (`skill-router`/`skill-search-tool`) — workspace consumer, stays (Stage 2 territory). (4) `deep-repo-audit.md` is a PM/repo-health audit (runs `scripts/model/pm-audit-opencode.js`), **not** Skill Graph code — it was misclassified in the resume plan; stays in the workspace. Net: the OpenCode skill-audit surface was already canonical-location-compliant (contracts in skill-graph, resolvers in `.opencode/commands/` per ADR-0015); the only edit was a doc-bug fix in `AGENTS.md § Canonical Location` — "OpenCode only from `.opencode/command/`" (singular) → `.opencode/commands/` (plural), verified against opencode.ai/docs + the live dir (OpenCode 1.15.10) — plus a new "verbatim-consumed loop prompts" exception clause so the three loop files are documented as legitimate residents, not migration debt. No deletions; no skill content touched. SYSTEM-only.
- **Audit-loop runner prompts relocated to the project-root `prompts/` dir (2026-06-01).** The four runner prompts (`skill-audit-loop-{single-model,batch-worker-v4,codex-autonomous-v5,minimal-iteration}.md`) moved from `skill-audit-loop/prompts/` to `prompts/`, giving the audit loop a single project-level prompts home — a runtime-neutral canonical source the Claude (`.claude/commands/audit/*`), Codex (app Automations + CLI), and OpenCode (`.opencode/commands/*`) adapters all point at. Moves used `git mv` (history preserved). Updated in the same change: `audits/manifest.json` runner + alias paths, the `audits-manifest.schema.json` `^skill-graph/prompts/` pattern, `skill-audit-loop/README.md` (relative links → `../prompts/`), the root `README.md` repo map (new `prompts/` row), `package.json` `files` (publishes `prompts/`), `AGENTS.md` (SYSTEM allowlist + Doc Ownership Map), `docs/skill-audit-loop-executable-map.md`, `SKILL_AUDIT_LOOP.md`, the ADR-0015 amendment, and every workspace reference (`prompts/audits/` stubs, `.opencode/commands/audit-skill.md`, `docs/reference/skill-audit-pipeline.md`, workspace `AGENTS.md` + `CONTEXT.md` + `.claude/rules/prompt-shape-structured-plus-novelty.md`). SYSTEM-only; no skill content touched.

### Added

- **Official multi-agent PANEL ENRICH loop (2026-06-05).** New orchestrator `lib/audit/run-panel-enrich.js` generalizes the 2-frontier enrich into the user-specified official Skill Audit Loop: Phase 1 — Opus 4.8 + GPT-5.5 (MANDATORY) AND every free agent (ADVISORY) each does its OWN research + proposal; Phase 2 — all agents cross-review each other (keep/wrong/missing) and revise, ITERATING TO CONVERGENCE (hash-authoritative stability, `maxRounds` budget); Phase 3 — a frontier curator union-merges the 2 mandatory under STRICT anti-loss + new `validateMandatoryCoverage`, MAY fold advisory in where valuable (frontier decides; `no-lesser-models-for-quality`); Phase 4 — bidirectional eval guardrail + keep-or-revert (advisory NEVER decides); Phase 5 — apply-on-keep. MANDATORY frontier failure ABORTS; ADVISORY failure recorded + skipped (quorum guard). **Robust delivery:** every tier WRITES its proposal to a verified path (no stdout-extraction of skill content; cross-review feedback is ephemeral stdout-JSON). **Self-contained in skill-graph** (per the 2026-06-05 canonical-location directive, `AGENTS.md § Canonical Location`): advisory via the models' OWN CLIs (`gemini --yolo`; `opencode run` spawnSync stdin-ignored + empty `--dir`) — NO workspace `dispatch-solver` dependency. New: `panel-enrich-live-deps.js`, `prompts/skill-audit-loop-{cross-review,revise}-pass.md`, `buildGeminiEnrichArgs`/`buildOpencodeEnrichArgs`, tests `test-panel-enrich.js` (17) + `test-panel-enrich-live-deps.js` (7). Contract: `skill-audit-loop/SKILL_AUDIT_LOOP.md § Multi-agent PANEL ENRICH — THE OFFICIAL LOOP`. Status: built + unit-tested + dry-run E2E green; live advisory write-to-path verification in progress (the first live opencode probe correctly surfaced an external-directory sandbox rejection — the verified-write contract recorded the failure, not a silent empty skill). SYSTEM-only.
- **Enrich eval guardrail skips (→ keep) on a missing eval artifact instead of crashing (2026-06-05).** `run-bidirectional-enrich.js` Phase-4 did an unconditional `readFileSync(evals/<mode>.json)` and threw ENOENT when absent — killing the run and discarding a successful enrichment (31/33 meta-bundle skills lack `application.json`; pilot best-practice). Now gated on an injected `evalArtifactExists` predicate (default true → DI tests unaffected; live deps supply the `fs` check): missing artifact → skip guardrail → keep + apply, eval=null. +2 tests. SYSTEM-only.
- **FULL OS fence for enrich/eval CLI dispatch — the SH-6681 remainder landed (2026-06-03).** The in-process `public-content-fence.js` (the SH-6681 down payment) refuses any path the *orchestrator* resolves outside the public scope, but could not stop the spawned model PROCESS from reading a private workspace tree by absolute path: `claude --permission-mode bypassPermissions` runs with no sandbox, and `codex -s workspace-write` restricts only writes. New `lib/audit/isolated-checkout.js` closes that gap with a kernel-enforced fence: on macOS it generates a Seatbelt (`sandbox-exec`) profile that is the policy-equivalent of an isolated checkout — `(allow default)` so the CLI runs normally, then `(deny file-read* file-write* (subpath <workspaceRoot>))` with each public root (`skill-graph` repo + `skills` tree + audit-artifacts run dir) ALLOWED back (last-match-wins). From the spawned process's view the workspace contains ONLY the public roots; every other workspace path — `sales-hub`, `printify`, `shopify`, any private sibling, `<ws>/.claude` — is EPERM for read AND write, even by absolute path, while public cross-tree refs (`skill-graph/audits/merge-protocol.md`) stay readable. Stronger than a physical checkout: no stale corpus copy, no disk cost, no broken relative refs. Wired into `enrich-live-deps.js` to wrap ONLY the model CLI dispatch (claude/codex) — the node claim calls stay unfenced (they legitimately write the workspace claim ledger). Default ON when supported (`SKILL_ENRICH_OS_FENCE=0` opts out), engages only for the real default dispatch (a dry-run / injected-stub dispatch leaves it inert), and degrades to the in-process guard on non-macOS with a logged notice. A container (Docker) fence for Linux/CI is the only piece of SH-6681 still open. New `scripts/__tests__/test-isolated-checkout.js` (11 cases, incl. a live `sandbox-exec` assertion that a node child under the generated profile reads a public file and is kernel-denied a private one) wired into `test:unit`. Doc: `docs/audit-loop-enrich-philosophy.md` point 3 rewritten (the fence is now four layers, OS-hard). SYSTEM-only; no skill content touched. Verified (exit 0): `test:unit` (incl. the live Seatbelt test + enrich/fence regressions), `node --check` on both changed JS, `models:check`. (Pre-existing unrelated reds — SH-6672 marketplace export / SH-6673 docs:drift — untouched.)
- **Application-eval runner: N-trial loop + in-code certification-tier gate (2026-05-30, SH-6624).** `lib/audit/application-eval.js` (`runApplicationEval`) now repeats each case over **N trials** (`--trials`, default 3, recommended 3–5) instead of a single baseline+with_skill pair, and takes the **MODE** of the per-trial verdicts as the authoritative per-case verdict — reporting `verdict_consistency` (modal-agreement fraction) and flagging `verdict_stable: false` when the mode holds across <60% of trials, so an unstable verdict is surfaced rather than silently averaged. Repetition smooths same-judge single-draw noise (IRT judge-reliability framework; CARE confounder-aware aggregation; verified 2026-05-30). Separately, the runner now stamps a `certification_tier` and `stampApplicationVerdict` enforces "never UNVERIFIED→APPLICABLE without evidence" **in code**: `APPLICABLE` survives only from a `certifying` run — reached ONLY when the operator passes `--certifying` AND declares two *different* vendor families via `--generator-family <F>` / `--grader-family <G>` (a declaration of provenance, NOT a model selector — the runner never selects a model). Every other shape (default, undeclared, or same-family) caps `APPLICABLE → PROVISIONAL`; `--single-model` is an additional stronger override. Cross-family is required because a same-family judge inflates its own family's outputs +10–25pp (self-preference bias). History records gain `trial_index` / `trials_total` / `trial_verdict` / `certification_tier` / declared families. New unit suite `scripts/__tests__/test-application-trials-and-certification.js` (59 assertions, fully mocked — no model calls) covers `modelFamily`, `resolveCertificationTier`, trial aggregation, the end-to-end trials loop, and the PROVISIONAL cap; added to `test:unit`. Docs: `docs/application-eval-spec.md` § How it is graded + § Verdict tiering. Coupled docs/schema (`schemas/application.schema.json`) authored earlier this session (23264d9). SYSTEM-only; no skill content touched. Verified (exit 0): both application unit suites, `lint:template`, `overlap`, `protocol:check`, `check-schema-constants`, `test:unit`. (Pre-existing unrelated reds — `doc:drift` on gitignored `dist/ab`, `marketplace:verify`/`status:check` stale-by-design per the 2026-05-29 render entry, `audit-manifest:check` SH-6548 CONTENT debt — are not introduced or affected by this change.)
- **`skill-audit-preflight.js` — readiness gate before operating on a skill (2026-05-29).** The recurring failure: an agent (or the audit loop) starts an operation on a skill without first checking the skill HAS what that operation needs — the right schema version, the required metadata fields, the Understanding fields, a valid `comprehension.json`, an `application.json` — and discovers the gap halfway in. `scripts/skill-audit-preflight.js` is the PRE-RUN companion to `scripts/watch-audit-batch.sh` (the during-run Monitor wrapper): it reads the LIVE schema (`schemas/SKILL_METADATA_PROTOCOL_schema.json`) as the contract — not a hardcoded field list — parses frontmatter via the shared `parse-frontmatter` (handles both flat and nested encodings), and reports a per-operation readiness matrix (`v8` / `comprehension` / `application` / `pairwise`) with exit codes (0 ready / 2 gaps / 3 error). It emits an ordered remediation plan distinguishing DETERMINISTIC fixes (run with `--ensure`: creates `evals/` + scaffolds schema-valid `comprehension.json`/`application.json` skeletons with clearly-marked TODO cases) from AUTHORING gaps that need an agent via `/audit:*` (Understanding fields, real eval cases). Usage: `node scripts/skill-audit-preflight.js <skill> [--for v8|comprehension|application|pairwise|all] [--json] [--ensure]`. First run already caught that both `methodical` and `journey-mapping` are missing the required `scope` field (neither is valid v8) and that `journey-mapping` has no comprehension content at all.
- **Heartbeat + active-Monitor wrapper for long audit-loop batches (2026-05-29).** Long graded runs (`evaluate`, `audit --graded`, `evolve --top N`) were being run as blind background tasks, which are silent through a crash or hang — indistinguishable from "still running," the recurring operator failure mode. Added `scripts/watch-audit-batch.sh`: a heartbeat-driven active listener that emits one event per `PROGRESS` / `HANG` (a unit past `--cell-stall`) / `STALE` (heartbeat stopped ticking) / `COMPLETE` (terminal), covering every terminal state so silence never reads as success. Pairs with a `status.json` heartbeat contract (`{ts,pid,total,done,failed,running:[{cell,elapsed_s}],complete}`) the batch runner writes on every unit start/finish + a ≤15s tick; operators stream it via the Claude Code `Monitor` tool. Documented in `SKILL_AUDIT_LOOP.md` § "Running long batches in the background." Reference producer of the heartbeat is the A/B experiment driver (`dist/ab/comprehension-ab-driver.js::writeStatus()`); wiring the heartbeat into the canonical `evaluate`/`evolve`/`batch-eval` runners is a tracked follow-up (until then, `--proc <pattern>` gives liveness-based death/stall detection).
- **`skill-graph render` — first-class local compile of the library into consumable skills (2026-05-29).** The marketplace export is only the public-publish profile; `render` is the primary deployment path for ACTUAL USE — yours locally or any cloner's. `scripts/render-skills.js` (CLI: `skill-graph render`, npm: `render` / `render:check`) compiles EVERY canonical skill — including `deployment_target: project` skills the marketplace publication gate excludes — into plain Agent-Skills `<name>/SKILL.md` files at `--out` (default `dist/skills`, git-ignored), each carrying clean frontmatter + the authored body + the projected `## Skill Graph context` section. No publication gate, no privacy scrub, no skills.sh URL rewriting (it is a local artifact, not a publish). `--check` for CI staleness. (Update 2026-05-29: the `--profile full/runtime` split was removed — there is one render shape; see the § Changed entry below.) Point `--out` at a runtime's skills dir (e.g. `~/.claude/skills`) to consume directly. The body-projection renderer was extracted to the shared `scripts/lib/render-skill-context.js` (with stable `<!-- skill-graph-context:start/end -->` fence markers) so `render` and `export` share one compile core; `export-marketplace-skills.js` now imports it. Coverage in `scripts/__tests__/test-render-skills.js`. Docs: README § Consuming the best-quality skills locally. Verified (all exit 0): render test, marketplace-export test, export-parser-drift, `npm run verify:system`.
- **Export-time body projection — readable `## Skill Graph context` section (2026-05-29).** Vendor auto-loaders (Claude/Codex/OpenCode/Gemini) read `name` + `description` at startup and the SKILL.md body on activation, but never the exported `metadata:` map — so the graph's classification, relations, Understanding fields, grounding, lifecycle, and audit status were invisible to consuming agents even though present in the file. `scripts/export-marketplace-skills.js::renderSkillGraphContext()` now projects every meaningful Skill Metadata Protocol field into a generated, deterministic `## Skill Graph context` section appended to each exported skill body (via a new `bodySuffix` hook on `scripts/export-skill.js::buildExportedSkill()`, which leaves generic single-skill exports byte-identical when unused). The `metadata:` map is preserved unchanged for lossless round-trip; the description boundary projection (2026-05-26) is untouched; pure machine-state fields (`urn`, drift hashes, `eval_failed_ids`, telemetry) stay in the map only. Slugs render as `code` spans (no Markdown links → no dangling-link findings), output is deterministic so `--check` stays stable, and the rendered strings already passed the privacy gate via the `metadata:` map so the change is privacy-neutral. All 154 marketplace exports regenerated. Coverage in `scripts/__tests__/test-marketplace-export.js`. Docs: `docs/marketplace-syndication.md` § Export-Time Body Projection. Verified (all exit 0): marketplace-export test, export-parser-drift, `export --check`, `marketplace:verify` (154/154), `verify-skill-md-export` (156/156), `test:unit`.

### Changed

- **ADR-0019 LANDED: audit/eval/provenance state split into a per-skill `audit-state.json` sidecar — the clean cut (2026-06-01).** The schema split scoped by the entry below now shipped, as a clean major-version-shaped cut (no dual-shape window). **Phase 1** (`16038f5`): new `schemas/skill-audit-state.schema.json` — 28 fields lift-and-shifted with identical enums/patterns, `required[7]` (`schema_version`, `owner`, `freshness`, `drift_check`, `eval_artifacts`, `eval_state`, `routing_eval`; `version` optional), plus the two intra-sidecar `eval_state ⇒ eval_artifacts` gates. **Phases 2–5** (`18dbda5`): `schemas/SKILL_METADATA_PROTOCOL_schema.json` stripped to the 25 agent-facing fields + `required` of the 5 core (`name`, `description`, `subject`, `deployment_target`, `scope`); `concept` (→ flat Understanding fields) and snake `allowed_tools` (→ kebab `allowed-tools`) retired; the `comprehension_state ⇒ Understanding` gate moved to a cross-file `skill-lint.js` check, both `eval_state ⇒ eval_artifacts` gates moved to the sidecar. `generate-manifest.js` merges the sidecar UNDER the frontmatter before `buildSkillEntry`, so the manifest `health`/`eval`/`lifecycle`/`concept` projections are **byte-identical** (proven by a pilot byte-diff on `database-migration`, 16 fields moved, entry unchanged); `manifest.schema.json` drops `version`+`owner` from the skill-entry `required`. Consumers made sidecar-aware: `check-schema-constants.js` (reads `schema_version` + the four verdicts from the sidecar; asserts the 5-field frontmatter `required` and the 7-field sidecar `required`), `check-protocol-consistency.js` C1 (unions both schemas), `build-field-reference.js` (covers both schemas), `schemas/skill.context.jsonld` (53 now-non-frontmatter keys pruned — the sidecar is never exported/RDF'd), `test-v8-schema-compat.js` (post-cut shape + a new assertion that a frontmatter audit field is rejected). The four `examples/fixture-skills/` + the authoring template split into `SKILL.md` + `audit-state.json` (+ `examples/skill-audit-state-template.md` companion preserving the relocated teaching prose). Prior single-file contract retrievable via `git tag schema-v8`. Verified (exit 0): `check-schema-constants` 19/19, `protocol:check` 7/7, `lint:template` + fixture lint 0 errors, `docs:links`, `test:unit`, pilot byte-identical. Phase 6 (corpus migration of the unmigrated `~/Development/skills/` library) is CONTENT-mode work the audit loop drains per-skill — until then the corpus lint (`npm run lint`, part of full `verify`, NOT `verify:system`) stays red by design. Pre-existing unrelated reds unaffected: `docs:drift` (SH-6655, operational `schema_version:7` strings in the v7→v8 migration plan doc), and `marketplace:verify`/`status:check` (mirror stale-by-design per the 2026-05-29 render entry).
- **ADR-0019 accepted: audit/eval/provenance state moves to a per-skill `audit-state.json` sidecar — DECISION + SCOPE only, schema unchanged (2026-06-01).** The field-relevance benchmark (`benchmarks/field-relevance/FIELD-PLACEMENT-MODEL.md`) classified all 56 metadata fields by *which consumer serves which moment*: 25 are agent-facing (the everyday agent reads them to find/understand/execute the skill → `SKILL.md` frontmatter); 28 are audit/eval/provenance (only the Skill Audit Loop reads them → a sidecar JSON in the skill folder). This split *is* the SYSTEM/CONTENT (skill-system-work vs individual-skill-work) boundary made physical, and structurally completes the 2026-05-29 "render carries guidance only, never maintenance state" change (which stopped projecting those fields into the agent-facing body; this removes them from the frontmatter source). [ADR-0019](docs/adr/0019-audit-state-sidecar-separation.md) is **Accepted**; the 6 open questions are resolved (sidecar = `audit-state.json` at skill-folder root; `grounding` stays frontmatter while `drift_check` moves; `lifecycle`→sidecar with `stale_after_days` carried into the manifest-join; `stability`/`superseded_by` stay frontmatter; `schema_version`→sidecar only; `routing_bundles` out of scope). Implementation is **scoped but NOT started** (`docs/plans/audit-state-sidecar-implementation.md`, 6 phases, clean major-version-shaped cut): the schema, consumers, and skill content are **unchanged** by this entry. The cut will be recorded past-tense here when it lands. Methodology note: the placement is conceptual (consumer × moment), not corpus-derived — the field-relevance benchmark retired routing-ablation (Machine R) and behavior-eval (Machine B) over the legacy corpus as relevance-verdict sources because the system postdates the skills.

- **Documentation front doors: two new `skill-metadata-protocol/` and `skill-audit-loop/` folders (2026-05-31).** The two big specs that sat at repo root and their companions scattered under `docs/` were collected into purpose-named folders, each with a hand-authored `README.md` front door. `SKILL_METADATA_PROTOCOL.md` + the four `SKILL_METADATA_PROTOCOL_*` companions + `docs/skill-metadata-protocol.md` (renamed `design-rationale.md`) moved into `skill-metadata-protocol/`; `SKILL_AUDIT_LOOP.md` + the four runner prompts (`audits/prompts/` → `skill-audit-loop/prompts/`) moved into `skill-audit-loop/`. Schemas (`schemas/`, per ADR-0014), audit code (`lib/audit/`), and per-skill audit artifacts (`audits/<skill>/`) stayed put and are linked. The root `README.md` gained a **The Three Layers** front-door section linking the two folders; `package.json` `files` now publishes the two folders (so the front-door READMEs + companions ship); `build-field-reference.js` writes the generated reference to the new path and emits sibling links; `check-protocol-consistency.js` C1/C7 read the new paths; `audits/manifest.json` + `audits-manifest.schema.json` point at `skill-audit-loop/prompts/`; the work-mode classifier (`check-work-mode-separation.js`) classifies both new folders as SYSTEM. All moved-file internal links, every workspace + skill-graph reference, and the ADR-0015 amendment were updated in the same change. Moves used `git mv` (history preserved). SYSTEM-only; no skill content touched. Verified (exit 0): `protocol:check` (C1+C7 green), `docs:links` (1397 files), `audit-manifest:check`, `mirror:freeze`, the work-mode unit test, and `bin/skill-graph.js --help`. (Pre-existing unrelated red: `docs:drift` on a `schema_version: 7` string in a 2026-05-30 plan doc — not touched by this change.)

- **Agent-facing render carries guidance only, never maintenance state (2026-05-29).** `scripts/lib/render-skill-context.js` no longer projects audit verdicts, eval status, lifecycle, or provenance (version/schema_version/owner) into the `## Skill Graph context` body section. The same-day body-projection (above, § Added) defaulted to a `full` profile that emitted "Lifecycle & audit status" + "Provenance" blocks, and `scripts/export-marketplace-skills.js` (the public skills.sh export) called the renderer with no profile — so every published skill leaked its `structural`/`truth`/`comprehension`/`application` verdicts and `eval_state` into the body a vendor loader feeds straight to the model. Audit/eval state is bookkeeping for the audit loop, not guidance for an agent USING the skill. The `full`/`runtime` profile split is removed entirely (one render shape; no footgun); `render-skills.js --profile` and `skill-graph render --profile` are gone. Keywords (an activation signal) are retained under their own heading. The two unit tests were inverted to assert the body carries no maintenance state. The marketplace staging surface was deliberately NOT regenerated (no publish this session); it stays on the prior output and will be regenerated only when a publish is next requested — so `export --check` will report the marketplace as stale against the new renderer until then, by design. SYSTEM-only; no schema or skill-content changes. Verified: `scripts/__tests__/test-render-skills.js` + `test-marketplace-export.js` pass; the renderer emits no audit/eval/provenance block.

- **Skill-audit fork cluster collapsed to single-canonical `lib/audit/` + workspace shims (SH-6603, 2026-05-28).** The three divergent skill-audit runners — `evaluate-skill.js`, `run-skill-improvement-loop.js`, `skill-improvement-helpers.js` — existed as forks in both this package and the Development workspace (`scripts/skill/`). Resolved per ADR-0016 (operational-data-ownership) + the eval-staleness-checker precedent via a **bidirectional** merge into the `lib/audit/` canonical (the divergence ran both ways; a one-directional port would have dropped behavior from one side): the package `evaluate-skill.js` absorbed the workspace's v6 Health Block writer (`writeHealthFieldsFromEvalResult` + the `--write-verdict` gate + the 4 understanding-field helpers), the `--case-id` / `--application <skill-dir>` CLI surfaces, and the grader-math discrepancy observability (`logGraderDiscrepancy` + the now-bundled `grader-output-validator.js`), while keeping its own SH-6302 four-verdict write-back (`stampApplicationVerdict` / `stampComprehensionVerdict`). The package `run-skill-improvement-loop.js` adopted the workspace's feature-superset content (the `--field` single-axis hard scope, `--commit` fast-forward merge, `diffFrontmatterKeys`, `UNDERSTANDING_FIELDS`, and the v8-correct `renderPrompt`), fixing a stale "schema v7" string, with portable `../audit-shared/` requires. The package `skill-improvement-helpers.js` was already the portability-improved superset (no change needed). The coupled grader prompts were unified into `lib/audit/graders/` — the package concept grader absorbed the workspace's "Anti-Compression Mandate" + "Calibration Reference Set" block — and `grader-output-validator.js` moved into `lib/audit/`. The Development workspace copies became thin `require()` shims; the workspace `scripts/skill/graders/` directory and `scripts/skill/grader-output-validator.js` were removed. `verify:system`, both repos' test suites (modulo pre-existing corpus-drift CONTENT failures), and `check-implementation-ownership.js` all pass.

### Fixed

- **`improve` was non-functional on the real skill library — three stacked bugs the 2026-05-28 fix missed (2026-05-30, SH-6640).** Found by running the audit loop end-to-end on a real skill (`okrs`), not by reading code. The 2026-05-28 entry below fixed this bug CLASS in `batch-eval.js` + `skill-improvement-helpers.js::findSkillDirByName`, but `run-skill-improvement-loop.js` (the `improve` entry point itself) was missed, so `improve` still: (1) **crashed** `ENOENT scandir .claude/skills` — `skillsRoot` hardcoded `<cwd>/.claude/skills`, ignoring the canonical `.skill-graph/config.json` `skill_roots`; now defaults to `resolveSkillRoots()[0]` (explicit `--skills-root`/`--skills-dir` still win; `.claude/skills` only a bare-workspace fallback). (2) After the root fix, **processed ZERO skills** because `collectSkillDirs` did a one-level `readdir` (FLAT-root assumption) while the canonical library is NESTED by subject — now walks recursively via the canonical `walkSkillFiles`. (3) `improve --skill <name>` (the documented public flag) was **ignored** (only `--include` was read), silently processing the whole library — `--skill` now aliases the include filter. Verified by running: crash → `0 processed` → `1 processed` (okrs resolved, eval file found, status `analyzed`). **Still unproven, tracked under SH-6640:** the full apply-mode generate→evaluate→keep/revert cycle (dry-run short-circuits before the eval) and the apply-mode worktree relative-path math now that the resolved root is outside the workspace. Preflight passed in all three broken states — the discipline that caught each bug was checking the processed-count, not the exit code. See `docs/research/2026-05-30-skill-audit-loop-end-to-end-verification.md`.
- **ADR-0016 status labels reconciled to verified on-disk state (2026-05-28).** `AGENTS.md § Doc Ownership Map` and `SKILL_AUDIT_LOOP.md § Part 3` carried stale "ADR-0016 (Proposed)" / "sequencing deferred" labels even though the ADR is **Accepted 2026-05-27**. Audited all six ADR-0016 surfaces against disk: S1 lanes.json + S2 merge-protocol are MIGRATED (the `.opencode/skill-audit-lanes.json` is deleted; `.opencode/commands/skill-audit-merge-v1.md` is a 9-line MOVED stub); S4 routing-config schema is AUTHORED + instance-compatible but unwired (no-deps project); S3 run-dir layout (`run-layout.js` absent) and S5 finding-schema migration are genuinely PENDING (filed SH-6607, SH-6608). Also corrected a **false claim** in `SKILL_AUDIT_LOOP.md`: it stated `scripts/skill/evaluate-skill.js` "is a thin delegator (ADR 0009 closure)" — it is in fact a divergent fork (bidirectional divergence with the package copy), collapse tracked by SH-6603. Updated the Doc Ownership Map "Grader prompts" row: the `scripts/skill/graders/` copies are NOT "deprecated shims" but divergent forks (the workspace concept grader carries an Anti-Compression Mandate + Calibration Reference Set block the package copy lacks), coupled to the SH-6603 evaluate-skill cluster.
- **`lib/audit/` package-portability — removed monorepo path assumptions (2026-05-28).** `lib/audit/batch-eval.js` and `lib/audit/skill-improvement-helpers.js` computed a local `REPO_ROOT = path.resolve(__dirname, '../..')` and hardcoded workspace subpaths (`agent-orchestration/`, `.opencode/`, `sales-hub/`, `skills/sales-hub`), which assumed the parent Development monorepo. They now resolve the skill library via `lib/audit/roots.js` (`resolveSkillRoots` + nested-aware `walkSkillFiles`) and the eval/progress/ledger paths via `lib/audit/log-paths.js` (env-var → monorepo → standalone fallback). This also fixed two **latent functional bugs**: `batch-eval.js` discovered **zero** skills in the package context (`skill-graph/skills/` does not exist — the canonical library is at the configured `../skills/skills`), and `skill-improvement-helpers.js::findSkillDirByName` was a flat `skills/<name>` lookup that silently missed every nested `skills/<subject>/<name>` skill, so the improve-loop's methodology-skill and related-skill injection found nothing. Closes the `lib/` cross-repo path-escape failures in `scripts/__tests__/test-standalone-pipeline.js`.
- **`skill-lint.js` rendered warnings with an `[error]` header (2026-05-28).** The warning render path passed `severity: 'warning'` to `scripts/lint/format-code-frame.js`, whose contract token is `'warn'` — so every warning printed an `[error]`-tagged diagnostic header while the summary correctly counted it as a warning (`0 error(s), 1 warning(s)`). Any consumer parsing diagnostic-header severity (notably `skill-audit.js::parseLintOutput`, which feeds the dry-run exit code and `structural_verdict` derivation) mis-classified warnings as hard errors. Fixed the caller to emit `'warn'` and hardened `format-code-frame.js` to accept `'warning'` as a synonym so the mis-render cannot recur. This unblocked `skill-graph audit <skill> --dry-run`, which had exited 1 on a warning-only (lint-clean) skill.
- **`examples/fixture-skills/minimal-capability` updated to clean v8 (2026-05-28).** The standalone-pipeline fixture still carried the retired `operation` axis plus the removed v7 `type`/`category` fields and lacked the now-required `deployment_target`, so it failed lint (the fixture's own docstring promises it stays lint-clean). Removed the retired fields, added `deployment_target: portable`, and dropped the banned "compatibility-window" wording from its body.
- **`scripts/__tests__/test-v8-schema-compat.js` rewritten to the clean-cut contract (2026-05-28).** Its fixtures and assertions still encoded the retired v7→v8 "compatibility window" (v7 `type`/`category` treated as optional-but-valid; grounding keyed off `scope`; `domain_object` grounding label) and failed 9/12 against the current clean-cut schema. Rewritten to assert the live contract: `subject` + `deployment_target` + `scope` required, retired v7 fields rejected by `additionalProperties`, `scope` free-text, grounding required when `deployment_target: project`, and `grounding.subject_matter` (not the retired `domain_object`). Now 14/14. `scripts/__tests__/test-lib-audit-smoke.js` was adjusted to invoke the now-functional `batch-eval.js` with `--dry-run` so its unknown-flag smoke check no longer launches a real corpus eval.
- **`skill-graph init` scaffolded from a stale pre-v8 template (2026-05-28).** `bin/skill-graph.js::runInit` copied `examples/protocol/skill-metadata-template.md`, which referenced the non-existent `skill.v4.schema.json` and linted with **9 errors** — so the public `init` command produced a skill that failed the current schema. Repointed the scaffolder (and the `--include-template` help text, which already mis-named the path) to the canonical, lint-clean `examples/skill-metadata-template.md`, and deleted the orphaned `examples/protocol/` duplicates (a stale template + a stale `skills.manifest.sample.json` carrying `domain_object`/`workspace_tags`/`by_category`/`by_type`, referenced by no active tooling — the canonical sample is the C4-validated `examples/skills.manifest.sample.json`). Fresh scaffolds now carry the v8 schema reference. Removes a "keep copies in sync" duplication per [AGENTS.md § Coupled Changes].
- **Residual v7 current-guidance swept from docs + specimens + a generated comment (2026-05-28).** Follow-up to the v7→v8 clean cut catching surfaces that still taught removed vocabulary as the *current* contract (migration-history references were left intact): `SKILL_GRAPH.md` (Tier-1/Tier-2 table called the field-reference "v7" and the manifest schema's verdicts "under v7"); `docs/SKILL_METADATA_PROTOCOL_field-reference.md` and `docs/glossary.md` (used the removed `scope: codebase` value as live authoring guidance → `deployment_target: project`); `docs/field-state-matrix.md` (the "Classification (taxonomy)" subsection, the `extends` conditional-required row, and resolution-rule 3 still presented the removed `type`/`categories`/`primaryCategory`/`layerPrimary`/`routingRole` fields as current — replaced with the live `subjects[]` axis and an honest note that `extends` is declared but no longer schema-gated); `scripts/backfill-field-purpose-comments.js` (the generated `routing_bundles` comment referenced the removed `workspace_tags` → `project[]`). Fixed the four `examples/projects/saas-stripe-postgres` specimens carrying an invalid `portability.readiness: portable` (enum is `declared`/`scripted`/`verified` → `scripted`).
- **Archetype / `type` / `category` model + `extends` field fully retired from the live tree (2026-05-28).** The v8 clean cut removed `type` (the capability/workflow/router/overlay archetype axis) and `category` from the schema, but the explanation and onboarding docs still taught them as the *current* contract, and `extends` (the overlay-parent field) was still declared. Per [AGENTS.md § Major Version Is a Clean Cut], removed `extends` from `schemas/SKILL_METADATA_PROTOCOL_schema.json` + `manifest.schema.json` + `skill.context.jsonld` + `generate-manifest.js` projection + `export-skill.js` + `examples/skills.manifest.sample.json`, and regenerated `docs/SKILL_METADATA_PROTOCOL_field-reference.generated.md`. Deleted the archetype-as-current doctrine from `docs/skill-metadata-protocol.md` (the "Archetype Section Map" and "Why archetypes are rigid vs anti-rigid" sections → replaced by a single uniform "Body Structure" section; overlay composition-precedence subsection removed), `SKILL_METADATA_PROTOCOL_PRIMER.md` (collapsed the five-layer model to four — the "Inheritance" layer is gone; Layer 2 now teaches `subject`/`taxonomy_domain` not `category`), `concept-map.md` (Classification group + Inheritance group + the now-false "type binds H2 sections, enforced by skill-lint" claim), `glossary.md` (the `extends` term + whole Archetypes section), `SKILL_METADATA_PROTOCOL_field-reference.md`, `field-rationale.md`, `SKILL_METADATA_PROTOCOL_field-decision-guide.md`, `field-state-matrix.md`, `AUTHORING-QUICKSTART.md`, `manifest-field-mapping.md`, `positioning-vs-marketplaces.md`, the root `SKILL_METADATA_PROTOCOL.md` (field list + the stale legacy-scope-normalization table that described behaviour no live code performs), `SKILL_GRAPH.md`, `SKILL_AUDIT_LOOP.md`, and this repo's `AGENTS.md` (Doc Ownership Map archetype row, the archetype-keyed eval-coverage guidance, the Skill Authoring checklist). Deleted the orphaned `schemas/vocabulary/workspace_tags.json` (self-bannered historical; no live tooling loaded it). One corpus skill (`lint-overlay`) still authors `extends` and now fails lint — expected clean-cut CONTENT debt routed to the audit loop per the version-schema-contract companion rule (the SYSTEM commit ships independent of corpus migration). Migration-history references (CHANGELOG, ADRs, the explicitly-labelled historical archetype-coverage table in `SKILL_GRAPH.md`) were preserved.

### Changed

- **v7 → v8 clean cut (2026-05-27).** Per [AGENTS.md § Major Version Is a Clean Cut](AGENTS.md), the live tree describes v8 only. The cut consolidated the iterative v8 design — initial classification model 2026-05-26 (per [ADR-0017](docs/adr/0017-five-axis-classification-model.md), closing SH-6547); `operation` axis retirement 2026-05-27 commit `f88603d`; and entity-vocabulary disambiguation + `workspace_tags` removal 2026-05-27 — into the final v8 contract documented below.

  **Required classification axes:** `subject` (closed 9-enum browse shelf) + `deployment_target` (closed 2-enum `portable`/`project`; the `workspace` enum value removed).

  **Required free-text axis:** `scope` — PRD-style label, repurposed from the prior closed enum. Value phrasing standardized.

  **Optional structural axes:** `subjects[]` (polyhierarchy, max 2, primary first), `taxonomy_domain` (slash-delimited sub-path, renamed from `domain`), `project[]` (array of `{handle, role}` belonging-entity references), `repo[]` (array of `{handle, url}` references). The four structural axes `deployment_target` / `project[]` / `repo[]` / `grounding` are orthogonal — no field's value is derived from another.

  **Companion doctrinal renames (commit `f88603d`):** `description` is now an about-statement (not a routing contract); "Eval-health triple" renamed to "Evaluation Status"; "Health Block" renamed to "Audit Status"; "Domain affiliation" → "Projects"; `scope` value phrasing standardized. Audit Status preserves the four discrete verdicts (`structural_verdict` / `truth_verdict` / `comprehension_verdict` / `application_verdict`); `application_verdict` is the primary quality signal.

  **Grounding rename:** `grounding.domain_object` → `grounding.subject_matter`; the v3.1 `grounding.subject` alias retired (`scripts/lib/alias-contract.js` drops the alias pair).

  **Removed from schema:** `type`, `archetype`, `category`, `categories`, `secondary_categories`, `primaryCategory`, `layerPrimary`, `routingRole`, `family`, `layer`, `operation`, `eval_status`, `workspace_tags`. The 6 category-based `allOf` branches removed. The `type:overlay → extends` and `type ≠ overlay → no extends` `allOf` branches removed. The `codebase` and `reference` scope-enum values removed. Manifest aggregates `summary.by_type` / `summary.by_category` / `summary.by_operation` and the `skills[].operation` field removed. JSON-LD `@context` term mappings for `operation` / `type` / `category` / `categories` / `primaryCategory` / `layerPrimary` / `routingRole` / `secondary_categories` removed. `manifest.schema.json` top-level `workspace` block removed (closes the open removal flagged in [ADR-0017](docs/adr/0017-five-axis-classification-model.md) § Landing strategy).

  **Tooling sweep:** schema (`schemas/SKILL_METADATA_PROTOCOL_schema.json`), manifest schema (`schemas/manifest.schema.json`), JSON-LD context (`schemas/skill.context.jsonld`), normalizer (`scripts/lib/alias-contract.js`), router (`scripts/skill-graph-route.js` — `SCOPE_RANK` → `DEPLOYMENT_TARGET_RANK`; project-fit filter reads `project[]` directly; semantic-tag expansion removed), manifest gen (`scripts/generate-manifest.js` — `by_scope` → `by_deployment_target`; workspace block emission removed), config (`.skill-graph/config.json` flattened: `workspace.skill_roots` → top-level `skill_roots`), roots loader (`scripts/lib/roots.js` — `loadWorkspaceConfig` → `loadRootsConfig`), and protocol-consistency check C8 hardened to catch `@context` keys mapping nothing in the schema (the retired-field drift gate that closed the gap which let `operation` ship after retirement). Corpus cleanup `scripts/finish-v8-corpus.js` rewrites each SKILL.md mechanically (no inference): scope value → deployment_target value, domain key rename, grounding label rename, `workspace_tags` block deletion, residual field stripping. `scope: workspace` skills are flagged for author review — the codemod refuses to infer their target value.

  **Codemods retired:** `scripts/migrate-skill-v7-to-v8.js` deleted (its history is in git); `scripts/lint/check-category-enum.js` deleted; the `category:check` package-script + verify-chain step removed.

  **ADR-0017 amended** with the `operation`-axis retirement; **ADR-0018's "v7 sunset window" framing replaced** with a frame-correction block.

  **Normative docs swept:** `SKILL_METADATA_PROTOCOL.md`, `SKILL_AUDIT_LOOP.md`, `SKILL_GRAPH.md`, `docs/skill-metadata-protocol.md`, `AGENTS.md`. Field-reference + decision-guide + manifest-mapping + authoring entrypoints (template, `README.md`, `ADOPTION.md`, `QUICKSTART-30MIN.md`, `AUTHORING-QUICKSTART.md`, `SKILL_METADATA_PROTOCOL_PRIMER.md`, `CONTRIBUTING.md`) all swept to v8.

  **Git tag `schema-v7`** created at the pre-cut commit (`c26ee1e`) so the prior contract remains retrievable via `git show schema-v7:schemas/SKILL_METADATA_PROTOCOL_schema.json`. Skills still carrying retired fields or retired scope-enum values fail validation against the live schema; their migration is CONTENT-mode work via `/audit:*` per `AGENTS.md § Work Modes`.

- **Body section headings renamed (2026-05-26).** `## Concept Card` → `## Concept of the skill`. `## Philosophy` → `## Philosophy of the skill`. SYSTEM-only change: updated `SKILL_METADATA_PROTOCOL.md`, `SKILL_AUDIT_LOOP.md`, `docs/skill-metadata-protocol.md` archetype section map, `examples/skill-metadata-template.md`, `scripts/skill/skill-census.js` (heading-detection regex + comments), `scripts/skill/select-duplicate-canonicals.js`, `scripts/eval/compression-lane.js`. **No SKILL.md files touched** per work-mode separation — skills still carrying the legacy headings now fail the census heading-detection check and are CONTENT-mode migration work routed through the audit loop. Companion to the Philosophy semantic reframe earlier the same day (Philosophy is now defined as "the philosophy BEHIND the skill — methodological stance / principles / opinionated worldview" instead of the previously redundant "why this skill file exists in this repo" framing that overlapped with `## Concept of the skill`'s `**Why it exists:**` field).

- **Marketplace exporter exclusion switched to the `deployment_target: project` axis (2026-05-28).** `scripts/export-marketplace-skills.js` now excludes skills by the v8 `deployment_target: project` axis (with the legacy closed-scope values `codebase`/`operational`/`project` and `grounding_mode: repo_specific|repo_internal` retained as transitional guards), replacing the prior `scope`-based exclusion and its v7→v8 "compatibility window" comments. Net effect on the current corpus: two project-grounded, repo-specific skills (`agent-ops/skill-router`, `quality-assurance/graph-audit`) are excluded from the public surface. The live exported-skill count is the single source of truth in `SKILL_GRAPH.md § Current State`.

### Added

- **Skill Metadata Protocol v8 — classification model** ([ADR 0017](docs/adr/0017-five-axis-classification-model.md)). Replaces the v7 `category` / `categories` / `primaryCategory` / `layerPrimary` / `routingRole` tangle with `subject` (closed 9-enum), `deployment_target` (closed 2-enum), required free-text `scope`, optional `subjects[]` polyhierarchy (max 2), optional `taxonomy_domain` (slash-delimited sub-path), optional `project[]` / `repo[]` belonging-entity references, capped `keywords` (≤10), and typed `relations` (`related`/`boundary`/`verify_with`/`depends_on`/`broader`/`narrower`/`disjoint_with`). 147 SKILL.md files migrated to v8 in 9 per-subject batches via the (now-deleted) `scripts/migrate-skill-v7-to-v8.js` codemod + per-skill mapping artifact (`audits/migration-mapping-v7-to-v8.json`). Companion changes: router rank tables, normalizer, manifest projection, marketplace exporter exclusion gate, `docs/SKILL_METADATA_PROTOCOL_field-reference.md` per-field prose. The initial 2026-05-26 design carried an `operation` axis and a closed-enum `scope` — both reshaped by the 2026-05-27 amendment (operation retired commit `f88603d`; scope repurposed to required free text; `deployment_target` introduced as the closed-enum deployment axis); see the consolidated **v7 → v8 clean cut** entry above for the final contract.
- **`skill-graph add <slug>` — marketplace install command (2026-05-28).** New `scripts/marketplace-install.js`, wired as the `add` subcommand in `bin/skill-graph.js`. Copies a skill from the `marketplace/skills/<slug>/` staging surface into a consumer project (`add <slug>`, `add --list`, `add --force`, `add --help`). Completes the public CLI install path — `init` scaffolds a blank skill, `add` pulls an existing marketplace skill. The script self-handles `--help`/`--list`, so the dispatcher delegates rather than carrying a duplicate static help string.
- **`verify:system` package script + CI gate (2026-05-28).** A SYSTEM-only verification gate (`schema-constants`, `protocol:check`, `docs:links`, `docs:drift`, `mirror:freeze`, `charter:parity`, `stability:check`, `lint:template`, fixture lint, `marketplace:verify`, `status:check`, `overlap`, `test:unit`) that excludes the corpus-dependent gates (`lint`, `manifest:validate`, `routing-eval`, `export:verify-skill-md`). Lets SYSTEM work verify green while the canonical skill corpus migrates to v8 through the audit loop; full `npm run verify` stays red until that migration completes. Wired into `.github/workflows/skill-graph-lint.yml` as the blocking CI gate, with full `verify` retained as an informational (`continue-on-error`) job so corpus state stays visible without blocking SYSTEM PRs.

### Changed

- **Audit-doc consolidation** — `SKILL_AUDIT_CHECKLIST.md` (214 lines) and `audits/per-skill-contract.md` (274 lines) merged into `SKILL_AUDIT_LOOP.md` as **Part 2** (checklist) and **Part 3** (runbook). The two source files were deleted; existing cross-references (146 across docs, ADRs, runner prompts, manifest, scripts, examples) now point at the merged Loop's Part anchors. Eliminates the "is it the Checklist or the Worklist or the Contract?" confusion documented during the 2026-05-25 multi-model restructure review.

### Migration notes

- Skills still carrying v7 fields are migrated one at a time through `/audit:*` per `skill-graph/AGENTS.md § Work Modes`.
- Skill-graph repo touched 9 files for tooling (schema, manifest schema, JSON-LD context, normalizer, router, manifest gen, exporter, codemod, lint check); the canonical library at `~/Development/skills/` was migrated in 9 per-subject batches (one git commit per subject).
- `audits/per-skill-contract.md` references in tooling (audit-loop runner prompts, manifest.json, scripts) now read `SKILL_AUDIT_LOOP.md § Part 3 — Per-Skill Audit Runbook`. Markdown anchor: `#part-3--per-skill-audit-runbook`.

## [0.5.10] — 2026-05-25

## [0.5.10] — 2026-05-25

The "canonical-shape sweep" release. Closes the 2026-05-25 multi-model restructure review backlog (51 commits, 22 distinct entries) with: ADR-0014 / ADR-0015 / ADR-0016 fully recorded; canonical homes for verdict semantics, comprehension eval shape, audits-manifest shape, and skill-routing-config shape; lane config + merge protocol relocated under `skill-graph/audits/`; the false-canonicality verifier hardened (enum split + resolution-via-downgrade); workspace zombies archived (`.claude/commands/audit-loop.md`, `.opencode/commands/skill-audit-opencode.md`); the 3 May 22-25 incident skills (`backend`, `mcp-builder`, `token-cost-estimation`) resolved via honest SKILL.md downgrade to `comprehension_verdict: UNVERIFIED`; and the upstream-currency third-axis question recorded as "stays as finding category, no fifth verdict slot."

### Added

- **ADR 0016 — Operational data ownership.** Proposes the trinary classification for operational data (pure project content / pure workspace orchestration / project-protocol scripts that operate over workspace-coordinated data) that resolves the post-ADR-0015 ambiguity about audit-runs, lane config, routing config, and comprehension logs. Sequences 5 per-surface migrations lowest-risk-first. Records Opus's "trinary not binary" dissent as the binding model so future ownership questions are answered by category, not by ad-hoc judgment. See [`docs/adr/0016-operational-data-ownership.md`](docs/adr/0016-operational-data-ownership.md). Synthesized from the 2026-05-25 multi-model restructure review at `.roundtable/skill-graph-restructure-review-2026-05-25/`.

- **ADR 0015 — Project-owned operational prompts.** Moves Skill Audit Loop runner prompts, the per-skill audit contract, the machine-readable protocol index, and the false-canonicality verifier into `skill-graph/audits/` and `skill-graph/scripts/`. Closes the May 22-25 incident root cause (`backend` / `mcp-builder` / `token-cost-estimation` shipped PROVISIONAL stamps against fragmented multi-file authority). Workspace `.opencode/commands/skill-audit-prompt-v2.{1,2}.md` deleted; `.claude/commands/audit/*` and `.opencode/commands/audit-skill.md` converted to thin slash-command resolvers. See [`docs/adr/0015-project-owned-operational-prompts.md`](docs/adr/0015-project-owned-operational-prompts.md).

- **ADR 0014 — Canonical-only schema files.** Adopts canonical-only schemas on disk (`schemas/SKILL_METADATA_PROTOCOL_schema.json` and `schemas/manifest.schema.json` only); prior contract versions live in git history; per-version codemods and the C6 "Versioned schema parity" protocol-consistency check are retired. The current contract version is declared inside every SKILL.md via `schema_version: N` and enforced by the canonical schema's `oneOf`. See [`docs/adr/0014-canonical-only-schema-files.md`](docs/adr/0014-canonical-only-schema-files.md). (Decided 2026-05-23; this entry retroactively records the governance-visible decision per the 2026-05-25 changelog-completeness sweep.)

- **ADR 0012 — Internal skill library separation: defense-in-depth gate vs separate repo.** Records the decision to enforce the public/private skill boundary via layered gates (export scope exclusion, pre-push hook, CI scan) rather than a hard two-repo split. Chooses Option B (defense-in-depth) over Option A (separate private repo) because Option B covers all three attack surfaces (export path, direct git path, eval pipeline path) while Option A covers only one. Documents the Hard Rule (no Sales Hub / customer / PII data in the public skills repo or eval pipeline), the prior SH-6286 leak incident, and the PRIVACY_PATTERNS-based gate architecture that unblocks SH-6323 (pre-push hook), SH-6324 (CONTRIBUTING.md docs), and SH-6325 (CI enforcement). See [`docs/adr/0012-internal-skill-library-separation.md`](docs/adr/0012-internal-skill-library-separation.md).

- **`schema_version: 7` — four-verdict Audit Status.** Splits the single v6 `audit_verdict` field into four discrete verdicts (`structural_verdict`, `truth_verdict`, `comprehension_verdict`, `application_verdict`) so the Audit Status carries independent verdicts for each layer of the skill-audit loop instead of compressing form, truth, comprehension, and behavior into one PASS/FAIL signal. `application_verdict` is the new primary quality signal — a skill is only behaviorally certified when this verdict is `APPLICABLE`. The other three are necessary infrastructure but not sufficient. Default for the v6→v7 corpus migration is `application_verdict: UNVERIFIED` on every skill — the honest state, since gate 9 (the application grader) has never run on a live skill. Added `schemas/SKILL_METADATA_PROTOCOL_schema.json` + the v6→v7 migration codemod at `scripts/migrate-skill-v6-to-v7.js`. Coupled doctrine change: form-gate authority narrows to external-constraint compliance only (Anthropic Agent Skills 1024-char description limit, required-fields shape, valid YAML). Internal style preferences no longer set `structural_verdict: FAIL`. Lint demotion is a follow-up commit. Full author-facing procedure was at `docs/migrations/v6-to-v7.md` (retired by [ADR 0014](docs/adr/0014-canonical-only-schema-files.md); narrative now in git history + ADR 0011). Decision rationale: [`docs/adr/0011-split-audit-verdict-into-four-verdicts.md`](docs/adr/0011-split-audit-verdict-into-four-verdicts.md). External corroboration: SkillsBench (arXiv 2602.12670) found 19% of evaluated agent skills produce negative deltas — the v6 pipeline could not detect any of those.

### Changed

- **Operational scripts updated for v7.** `scripts/skill/skill-evolution-loop.js` aggregates `lint_verdict` → `structural_verdict`, `drift_status` → `truth_verdict`, and writes all four verdicts on every audit run (`comprehension_verdict: UNVERIFIED` and `application_verdict: UNVERIFIED` by default until their respective graders run). `scripts/skill/skill-census.js` replaces `VALID_AUDIT_VERDICTS` with per-field enum validators. `scripts/skill/backfill-audit-state.js` derives `structural_verdict` from grade letter instead of `audit_verdict`. `skill-graph/lib/audit/skill-status.js` displays the four new verdicts in canonical Audit Status order. Coupled doc updates land in the same commit per `AGENTS.md § Coupled Changes`.
- **`scripts/check-doc-drift.js` catches stale-version *prose*.** Added a `stale-version-phrase` pattern (warn-class) that flags non-current version tokens qualifying a contract noun — `v6 frontmatter`, `v5 protocol`, `v4 top-level fields` — the exact drift class the prior `schema_version: N` sentinel missed. Warn-class, not error, because describing an old version as legacy/back-compat is correct, not drift; a new `LEGACY_CONTEXT_RE` suppresses lines carrying legacy/deprecated/back-compat/historical/`allOf`/`anyOf` context. Surfaced via `--include-warn` (advisory, exit 0); the default error-class run is unchanged.

### Fixed

- **`check-audit-manifest.js` enum leak — split comprehension and application verdict sets (`scripts/check-audit-manifest.js`, `audits/manifest.json`).** The verifier shipped on 2026-05-25 in commit `5b21f26` defined `GRADED_COMPREHENSION_VERDICTS` as `{PROVISIONAL, APPLICABLE, PASS, SHALLOW, REDUNDANT, MIXED, HARMFUL}` — a union that mixed application-layer enums (`APPLICABLE` / `MIXED` / `HARMFUL`) into the comprehension check. Per `schemas/SKILL_METADATA_PROTOCOL_schema.json:261-285` and ADR 0011, the two verdict enums are disjoint: comprehension graded set is `{PROVISIONAL, PASS, SHALLOW, REDUNDANT}` only; `{APPLICABLE, MIXED, HARMFUL}` is the application graded set. Same fix applied to the `when` clause for the `comprehension.json` artifact in `audits/manifest.json:47`. Added a category-error guard that flags verdicts which stamp application-layer enums into the `comprehension_verdict` slot (e.g., `| comprehension | APPLICABLE |` becomes a distinct failure rather than silently mis-classifying). Caught by GPT-5.5 in `.roundtable/skill-graph-restructure-review-2026-05-25/gpt55-response.md:132-133`.
- **`check-audit-manifest.js` respects honest SKILL.md downgrade as resolution.** When a per-run `verdict.md` claims graded comprehension without the artifact BUT the current SKILL.md Audit Status has been honestly downgraded (e.g., `comprehension_verdict: UNVERIFIED`), the verifier treats the historical drift as resolved-via-downgrade rather than a live failure. This closes the loop on the May 22-25 incident: the 3 historical runs (`backend`, `mcp-builder`, `token-cost-estimation`) had their SKILL.md verdicts downgraded to UNVERIFIED in this commit, and the verifier now passes cleanly. Historical `verdict.md` artifacts are NOT rewritten (per the read-only audit-run contract in `.opencode/progress/skill-audits/_orphaned/README.md`); the SKILL.md Audit Status is the authoritative current state.
- **Completed the ADR 0014 stale-reference sweep.** `337fe22`'s commit message claimed it renamed `skill.v7.schema.json` → `SKILL_METADATA_PROTOCOL_schema.json` "across all docs and ADRs," but it only fixed the schema *filenames* — it left dangling links to the deleted `docs/migrations/` directory behind. Cleaned up the residue: the `schema_version` field description in `schemas/SKILL_METADATA_PROTOCOL_schema.json` (which propagated into the generated `docs/SKILL_METADATA_PROTOCOL_field-reference.md`), two relative `migrations/v6-to-v7.md` links in canonical docs (`docs/skill-metadata-protocol.md`, `docs/manifest-field-mapping.md`) that were failing `npm run docs:links`, a broken local link in this CHANGELOG, the `evaluate-skill.js` header comment, and the ADR 0011 body (fixed the `337fe22` sed corruption that duplicated `SKILL_METADATA_PROTOCOL_schema.json` across two bullets, and annotated the pinned-schema / codemod / migration-doc references it lists as retired by ADR 0014). Canonical skill-scaffold grounding (sibling `jacob-balslev/skills` repo) also dropped its dead `skill.v7.schema.json` + `migrations/v6-to-v7.md` truth-source links. Generated artifacts (`marketplace/skills/`, `examples/skills.manifest.sample.json`) still carry the link until the next full export/manifest resync, which is independently owed (categorized-tree path reorg + a pre-existing `/sales-hub/` privacy FAIL in `best-practice`).
- **Verifier trust-surface cleanup.** `npm run lint:template` no longer passes the template both as an explicit file and through `--include-template`, so the linter reports the real file count. `scripts/build-status-doc.js --check` no longer writes `docs/status.generated.md` or claims it wrote the file; check mode now runs the status checks and reports `checked`.
- **Agent-Skills-compatible `compatibility` strings no longer warn during manifest validation.** `generate-manifest.js` now distinguishes protocol-native scalar `compatibility` from the valid Agent-Skills-compatible physical encoding documented in `SKILL_METADATA_PROTOCOL.md`, removing false deprecation noise for the sibling canonical library.

- **Generated field reference parity is deterministic (`scripts/build-field-reference.js`, `docs/SKILL_METADATA_PROTOCOL_field-reference.generated.md`).** Removed the wall-clock date from the generated header so `npm run protocol:check` C7 compares schema-derived content instead of failing once per day on a date-only diff. Regenerated the generated field reference with the deterministic header.
- **JSON-LD `@context` v7 correctness pass (`schemas/skill.context.jsonld`).** Surfaced by the skill-library-as-lens design review (`docs/plans/skill-library-as-lens-review-2026-05-21.md`). (1) **`boundary` term collision resolved.** The token `boundary` had two distinct senses — the v6+ Understanding field (a string) and `relations.boundary` (the routing predicate, an array) — both resolving to one global term (`sg:disjointOwnership`, `@set`), so the Understanding-field string mis-projected to RDF as a routing-disjointness set. Fixed with a JSON-LD 1.1 **property-scoped `@context`** on `relations` (`relations.boundary` → `sg:disjointOwnership`) while top-level `boundary` now maps to a new `sg:conceptBoundary`. Honors the file's stated promise of RDF/OWL/SKOS projection "without changing the authoring surface." (2) Stale metadata corrected: `_comment` v4 → v7, `_version` 3.0.0 → 3.1.0, `_schema_version_target` 6 → 7. (3) Stale `_note` enums corrected: `eval_score` 0.0–1.0 → 0.0–5.0; `drift_status` `clean|drifted|unknown` → the real seven-value sentinel enum; `lint_verdict` `pass|fail|warn` → `PASS|FAIL|UNKNOWN`. (4) `secondary_categories` and `marketplace_tier` annotated as non-v7 back-read-only terms. C8 (JSON-LD context coverage) still passes. Note: C8 validates coverage and IRI prefixes, not `_note` accuracy — these were stale because nothing enforced them.
- **Canonical skill-count drift corrected to 143 + made generated-mirror-backed (`SKILL_GRAPH.md`, `AGENTS.md`, `README.md`, `skills.manifest.json`, `docs/status.generated.md`).** The library grew to 143 but three prose docs still said 142, the committed root `skills.manifest.json` was stale at 142 (the sample manifest and live generation were already 143), and `docs/status.generated.md` read that stale manifest (and still showed "schema version 6"). Regenerated the root manifest and the status doc via their owning scripts (`generate-manifest.js`, `build-status-doc.js`) — both now report 143 / schema v7 — and updated the prose counts to 143 with a pointer to the generated status doc as the live check. (The `marketplace/skills/` export surface still shows 142 and is one skill behind the library; regenerating it is a deliberate release-sync action, tracked for the next sync.)
- **Protocol version-string drift (`SKILL_METADATA_PROTOCOL.md`).** Header advertised "Skill Graph 0.6.0"; `package.json`, `AGENTS.md`, and `README.md` are 0.5.8. Corrected to 0.5.8.

- **Documented the dual physical encoding of the frontmatter contract (`SKILL_METADATA_PROTOCOL.md` § Overview).** The normative field tables illustrate the protocol-native shape (top-level YAML), but the entire canonical library is authored in the Agent-Skills-compatible shape (fields nested under `metadata:`, structured values JSON-string-encoded) because that repo is also the public release repo. Added a "Two physical encodings, one logical contract" subsection documenting both shapes, the `normalizeFrontmatter()` reconciliation, the top-level-wins precedence rule, and the consequence that the stripped `skill_graph_protocol` content-label is invisible to deterministic tooling (human-governed only). An author following the old spec literally would have produced a non-corpus-shaped file.
- **Documentation v7 currency pass (`AGENTS.md`, `SKILL_GRAPH.md`, `README.md`).** Removed stale v5/v6-era language that contradicted the v7 specs. `AGENTS.md`: the `category` enum is now stated as the closed 6-value set (was "open / being closed in v5"); the concept-shape contract is replaced by the five flat Understanding fields (`mental_model`/`purpose`/`boundary`/`analogy`/`misconception`), with the 7-field nested `concept` block noted as deprecated v5 back-compat; removed the non-existent `grounding.external_sources ≥2` requirement; corrected the `eval_state` enum to `unverified`/`passing`/`monitored` (was `verified`/`unverified`); fixed the coupled-change manifest pin to `manifest.schema.json` (was v6); corrected "v6 frontmatter" / "v5 protocol" → v7; updated the canonical skill count 145 → 142 (verified via `find`). `SKILL_GRAPH.md`: protocol and audit-loop links now point in-repo (`SKILL_METADATA_PROTOCOL.md` / `SKILL_AUDIT_LOOP.md`) instead of the archived sibling mirrors; the governance row describes the audit loop as four operations (`audit`/`improve`/`evaluate`/`evolve`), with the five-phase flow noted as the inner pipeline of `audit`; the "v4 top-level fields" label corrected to v7; Tier 5 carries a location note that the `skills/` starter tree was consolidated to the sibling library per [ADR 0009](docs/adr/0009-sibling-repo-deprecation.md), the in-repo specimens are `examples/fixture-skills/`, and three of the original eight starters (`documentation`, `skill-router`, `graph-audit`) are no longer in the library. The stale eight-node "starter graph" diagram (which wired three now-deleted skills and referenced a no-longer-generated PNG) was **reauthored** as "The fixture graph" against the four in-repo `examples/fixture-skills/`, which form a closed reference set exercising all four `relations.*` edge types. Clarified the source↔release topology: `~/Development/skills/` is the local clone of `jacob-balslev/skills` and is **both** the canonical authoring source and the public release repo (not two distinct skill repos); the two-step sync moves content from this tooling repo's `marketplace/skills/` staging surface into that one library repo. `README.md`: the "Current Top-Level Categories" list corrected from the pre-v5 open enum to the closed 6-value enum; domain/group tables reattributed to the sibling library; skill count 145 → 142.

### Added

- **Doctrine: "Version labels are earned, not bumped" (`AGENTS.md`, `docs/quality-doctrine.md`).** Codifies that `schema_version` / `skill_graph_protocol` / any `vN` label asserts substantive content conformance — advancing it without performing the migration the version represents (v6: five flat Understanding fields; v7: four-verdict Audit Status) is fake-conformance, the same defect class as `eval_state: passing` without evidence. A `schema_version: 7` + `skill_graph_protocol: v5` mismatch is honest record-keeping, not drift; bulk `sed` of version labels is prohibited. Flags the conformance caveat that `export-marketplace-skills.js` hardcodes `skill_graph_protocol: v7` on every export regardless of source content. Prompted by a 2026-05-20 near-miss where a 140-file label restamp was proposed and correctly rejected.
- **`SKILL_GRAPH.md` § Current State + § Source vs Marketplace.** A single canonical "Current State" block (schema v7, manifest v7, 142 skills, library location, repo role) that other docs should link to instead of restating — closing the duplicate-truth gap that produced the version/count drift. A "Source vs Marketplace" section explains why the canonical library and the in-repo `marketplace/skills/` export are not duplicates (publication gate, frontmatter normalization, directory flattening). Verified the export is self-consistent (`--check` clean, write run is a no-op): the flat `marketplace/skills/` layout is the current publish shape, not drift. Documented the conformance caveat that the exporter hardcodes `skill_graph_protocol: v7` on every skill regardless of source content level.

### Security

- **Marketplace export publication gate.** `scripts/export-marketplace-skills.js` now refuses to export any skill grounded in a private/internal codebase: it excludes `scope: codebase|operational` and `grounding_mode: repo_specific|repo_internal` skills (logged as `EXCLUDED from marketplace export`), and adds internal-path and internal-DB-surface patterns to `PRIVACY_PATTERNS` so a mis-scoped "portable" skill still fails `--check`. This is the frontmatter-driven counterpart to the existing secret/path scanner — it cannot be defeated by a body that happens to avoid the regexes. See `AGENTS.md § Pre-release verification` and `§ When skills.sh is wrong about us`.

## [0.5.9] — 2026-05-23

### Added

- **`lib/` published in package `files`.** Added `lib/` to the `files` array in `package.json` so the audit library (`lib/audit/application-eval.js`, `lib/audit/skill-improvement-helpers.js`, etc.) is available to consumers via `require('@skill-graph/cli/lib/audit/<module>')`. Previously only `bin/`, `docs/`, `examples/`, `schemas/`, `scripts/`, and `marketplace/` were included. This enables `scripts/skill/evaluate-skill.js` in the parent Development repo to delegate its APPLICATION eval layer to `@skill-graph/cli/lib/audit/application-eval` (SH-6198).

### Changed

- **`scripts/skill/evaluate-skill.js` delegates APPLICATION eval to `@skill-graph/cli`.** Replaced the ~650-line inline APPLICATION block (lines 1205–1782) with a `require` delegation to `@skill-graph/cli/lib/audit/application-eval`. All exported symbol names are preserved so callers and tests are unaffected (SH-6198).

## [0.5.8] — 2026-05-19

The "Karpathy-loop Phase 2" release. Ships the Phase 2 deterministic-drift sentinels, mirror-freeze linter, generated status doc, `doctor` subcommand, first hermetic test fixtures, and positioning + onboarding rework. Also absorbs the accumulated `[Unreleased]` backlog from 0.5.0 → 0.5.7 (publication-classification ledger, npm publish pipeline, stability-promotion lint check, schema_version 4 → 5 → 6 migrations, cross-repo version reconciliation, and the skill audit loop re-grounding reframe). The 33 Phase 1 truth-repair commits landed under 0.5.7 and are not re-listed here.

### Added

- **`skill-graph eval-staleness`** — exposes the consolidated eval staleness checker as a first-class CLI diagnostic. The checker now scans `examples/evals/*.json` and resolves `truth_sources` through `.skill-graph/config.json` into the sibling canonical skills library, instead of using the removed pre-consolidation `skills/**/evals/evals.json` layout. Added `scripts/__tests__/test-eval-staleness-checker.js` to prevent the old "No eval files found" regression.
- **`scripts/check-doc-drift.js`** — schema_version drift sentinel that scans active docs for stale `schema_version: N` references and refuses to teach an old contract as current. Reads the active version from `schemas/SKILL_METADATA_PROTOCOL_schema.json`; allowlists `_archived/`, `docs/migrations/`, `examples/`, `CHANGELOG.md`, and `vN → vM` migration sections. Flags: `--json`, `--quiet`, `--include-warn`.
- **`scripts/check-mirror-freeze.js`** — fails on active-source / active-package claims in the two docs-only deprecation mirrors (`skill-metadata-protocol`, `skill-audit-loop`). Detects `@skill-graph/protocol`, `@skill-graph/audit`, `src/`, `lib/schemas/`, `schemas/skill.v*.schema.json`, and `evals/` references that contradict the post-ADR-0009 docs-only status. File-level banner detection skips files explicitly stamped as historical / frozen / deprecation-mirror snapshots.
- **`scripts/build-status-doc.js` + `docs/status.generated.md`** — single-source-of-truth status snapshot that pulls live values from `package.json`, the schema, the manifest, and the four deterministic check scripts. Replaces hand-maintained "Latest release" lines and ad-hoc skill-count claims. Flags: `--check`, `--stdout`, `--no-checks`.
- **`skill-graph doctor`** — new CLI subcommand grouping all six deterministic checks (links, protocol, drift, mirror-freeze, lint, manifest) into one pass with a single summary table. Recommended first command for bug reports. Flags: `--json`, `--bail`, `--skip <name>`.
- **`examples/fixture-skills/`** — hermetic test fixtures for `@skill-graph/cli` package tests. Ships `minimal-capability` (bare-minimum v6 frontmatter), `with-grounding` (4-sub-field `grounding` object for `scope: codebase`), `with-relations` (all four `relations.*` edges), and `comprehension-full` (flat Understanding fields + Audit Status). Each demonstrates a different fixture archetype; `minimal-capability`, `with-grounding`, and `comprehension-full` lint clean against the active schema.
- **`docs/positioning.md`** — explicit positioning of Skill Graph in the agent-skills ecosystem. Names what Skill Graph is **not** (a runtime, a marketplace, a tool platform) before naming what it **is** (an authoring + audit-time contract with a Karpathy keep-or-revert loop). Compares against Anthropic Skills, the Agent Skills spec, MCP, A2A, Smithery, Composio, and AGENTS.md. Includes the "60-second pitch", the "what this is not" matrix, the "when to reach for Skill Graph" gate, and the layered ecosystem map.
- **README hero rework** — three new sections above the ecosystem diagram: "Is this for me?" (yes-if / no-if qualifying signals for a 30-second visitor), "What makes this different" (Karpathy keep-or-revert loop applied to skill libraries, with explicit citation), and a surfacing of `docs/quality-doctrine.md` as the codified quality bar.
- **README two-onboarding-paths framing** — explicit routing of authors to `docs/QUICKSTART-30MIN.md` for hands-on first-skill authoring and to `docs/SKILL_METADATA_PROTOCOL_PRIMER.md` for the conceptual model first. Synthesis Item 48 resolved as "link, do not merge" because they serve different reader genres.
- **`data/publication-classification.json`** — hand-edited ledger of per-skill marketplace publication classification (`publishable` / `sales-hub-bound` / `personal-infra`) plus market-state attributes (`tier`, `pop_score`, `source`, `needs_sanitization`, `demand_signal`, `rename_to`, `notes`). Seeds the `publicationQueue` in the unified `.opencode/progress/skill-audit-worklist.json` (consumed by `skill-audit-loop@0.3.0`). 218 skills classified at seed (136 publishable, 55 sales-hub-bound, 27 personal-infra). Seeded by `scripts/seed-publication-classification.js` from the now-archived 2026-05-18 priority doc; hand-edited going forward.
- **`scripts/seed-publication-classification.js`** — one-off seeder script that parses the priority doc tables into the ledger. Re-runnable; preserves first-classification-wins semantics.
- **`docs/marketplace-publication-queue.generated.md`** — auto-generated by `skill-audit-loop@0.3.0` from the ledger. Tier-grouped publication queue with conflict and orphan tracking. Replaces the hand-curated priority doc as the living source of marketplace publication priority.
- **npm publish pipeline (SH-6111).** Added `.github/workflows/publish.yml` — CI publishes `@skill-graph/cli` to npm on `v*.*.*` tag push after `npm test` passes. Uses `NPM_TOKEN` secret and npm provenance attestation (`--provenance`). Manual prereqs before first publish: (1) create the `@skill-graph` npm org at https://www.npmjs.com/org/create (the npm CLI does NOT support org creation; the org must be created via the website), (2) add `NPM_TOKEN` as a GitHub repo secret. First publish is pending Jacob's `npm version patch && git push --tags`. See [README.md § Releasing](README.md#releasing-maintainers).
- **Lint check 14 — stability promotion gate (SH-6109).** `scripts/skill-lint.js` now includes a WARN-level check that fires when a skill declares `stability: stable` without meeting five promotion criteria: (1) `eval_state` is `passing` or `monitored`, (2) `eval_score` ≥ 4.0 on the 0.0–5.0 scale, (3) `routing_eval: present`, (4) `drift_check.last_verified` within the last 90 days, (5) `grounding.truth_sources` is populated for `scope: codebase` and `scope: reference` skills (`scope: portable` is exempt). All five criteria are evaluated independently — all failures are reported, never short-circuited. WARN level only (never ERROR), so the 140 currently-experimental skills are unaffected. Implemented in `scripts/lint/check-stability-promotion.js`; 8 tests in `scripts/__tests__/test-stability-promotion.js` (6 required cases + 2 edge cases). Promotion criteria documented in `docs/skill-metadata-protocol.md § Stability Promotion Criteria`. Status: 0/140 library skills currently declare `stability: stable`.

### Changed

- **`scripts/check-markdown-links.js`** — broken links in `_archived/` paths now emit warnings instead of failing the build. Active docs remain strict. New `--strict-archived` flag elevates archived warnings to errors (useful when migrating an archive batch). Synthesis Item 49.
- **README "External Context"** — replaced 5-bullet list of tangential references with a "Where Skill Graph fits" section that links directly to `docs/positioning.md` and surfaces the unique angle in one line.
- **`bin/skill-graph.js`** — `evolve` subcommand labeled `[PREVIEW · monorepo-only]` in both the README quickstart and the CLI `--help`. Aligns the discoverability surface with the dispatcher's existing not-standalone-compatible error message.
- **Cross-repo version reconciliation (SH-6124).** README and CHANGELOG references to `schema_version: 4` updated to `schema_version: 6` (the actual current contract owned by `skill-metadata-protocol`). Repository Map updated to mention v6 in `schemas/`. The local `schemas/` directory still pins v4 and v5 alongside v6 for tooling that consumes the schemas directly; the unversioned mirror tracks v6.
- **schema_version 6 — flat fields + Audit Status.** Promotes the seven nested `concept.*` sub-fields to flat top-level fields (`mental_model`, `purpose`, `boundary`, `analogy`, `misconception`; `definition` collapses into `description`; `taxonomy` collapses into `category` + `relations.broader`) and introduces the seven-field flat Audit Status (`last_audited`, `last_changed`, `audit_verdict`, `eval_score`, `eval_failed_ids`, `lint_verdict`, `drift_status`). Added `schemas/skill.v6.schema.json` + the v5→v6 migration codemod at `scripts/migrate-skill-v5-to-v6.js`. Full author-facing procedure lived in `skill-metadata-protocol/docs/migrations/v5-to-v6.md` (since deleted in the audit-doc consolidation; recoverable from git history).
- **schema_version 5 — closed `category` enum.** Closed the `category` field to a 6-value enum (`foundations | engineering | design | quality | agent | product`) and removed 7 deprecated values. Promoted `quality` from facet to 6th category. Added `schemas/skill.v5.schema.json`. The v4→v5 content migration was performed by `scripts/migrate-category-to-enum.js` (per-skill explicit mapping to the canonical 6 values); a dedicated `migrate-skill-v4-to-v5.js` codemod was never authored because the enum closure was a content/data migration rather than a structural-shape migration. Earlier wording in this entry naming `scripts/migrate-skill-v4-to-v5.js` was a documentation error — that script never existed.
- **schema_version 4 naming cleanup.** Renamed `browse_category` to `category`, hierarchical `category` / `category_path` to `domain`, `project_tags` to `workspace_tags`, and `routing_groups` to `routing_bundles`. Added active and pinned v4 skill/manifest schemas, migrated shipped skills, regenerated manifests/exports/marketplace artifacts, and added `scripts/migrate-skill-v3-to-v4.js`.
- **Skill Audit Loop reframed as a re-grounding mechanism (docs only, no `schema_version` impact).** The loop was documented mechanics-first — its cadence and outputs without its purpose. `README.md` and `SKILL_AUDIT_LOOP.md` now open with the conceptual frame: a skill is a contract about a subject, and the loop is what re-grounds it against its two moving targets — the codebase it is grounded in and the subject it describes. `README.md` closing line changed "maintenance loop" to "re-grounding loop"; `SKILL_GRAPH.md` gains a three-layer relationship paragraph (protocol declares grounding, graph operates across the library, loop re-grounds each skill on a cadence). Rationale recorded in new `docs/proposals/skill-audit-loop-positioning.md`. Stated entirely in existing protocol vocabulary — no new fields, scripts, or CLI verbs.

### Removed

- **`docs/marketplace-publication-priority-2026-05-18.md`** — archived to `docs/_archived/marketplace-publication-priority-2026-05-18.md` and superseded by `docs/marketplace-publication-queue.generated.md`. The hand-curated priority doc lived for one day before being merged into the auto-generated queue (via `skill-graph/data/publication-classification.json` + `skill-audit-loop@0.3.0`). Reasoning preserved in the archived copy for audit-trail grep.

### Documented

- **`docs/_archived/marketplace-publication-priority-2026-05-18.md`** — the archived plan's broken `./adr/...` link was fixed to `../adr/...` (Phase 1 Item 1).
- **Mirror governance** — five mirror docs banner-stamped as frozen deprecation snapshots: `skill-metadata-protocol/docs/{concept-map, field-reference, field-reference.generated, skill-metadata-protocol}.md` and `skill-metadata-protocol/CONTRIBUTING.md` + `skill-audit-loop/{CONTRIBUTING, SKILL_AUDIT_LOOP, SKILL_AUDIT_CHECKLIST}.md` (Phase 1 Items 30, 25 + Phase 2 Item 36 follow-ups).
- **`docs/plans/skill-graph-oss-docs-refresh.md`** (lives in the Development workspace, not this repo) — banner-stamped as superseded by ADR 0009 (Phase 2 Item 41).

## [0.5.0] — 2026-05-13

The "package-ready CLI + contract honesty" release. It keeps the existing zero-dependency scripts as the implementation surface, exposes them through one npm-ready `skill-graph` binary, closes the v3.1 alias pass-through gap, and tightens router/path behavior that made the contract look stronger than the implementation.

### Added
- **Conformance and compatibility docs.** Added `docs/CONFORMANCE.md` for L0-L4 adoption levels, `docs/SKILL-MD-FORMAT-COMPATIBILITY.md` for the base SKILL.md export contract, and `docs/ROUTING-METRICS.md` for routing metrics, confusion-matrix interpretation, and scale limits.
- **SKILL.md export verifier.** Added `scripts/verify-skill-md-export.js`, `npm run export:verify-skill-md`, and `skill-graph export-verify` to prove exported skills keep only base SKILL.md top-level fields and string-only metadata values.
- **Routing eval confusion matrix.** `scripts/skill-graph-routing-eval.js --confusion-matrix` now reports expected skill vs actual top-1 winner for positive examples plus negative-case summaries.
- **Markdown link verification.** Added `scripts/check-markdown-links.js` and `npm run docs:links`; `npm run verify` now fails when repo-local Markdown links or anchors point at missing files/sections.
- **Export/parser/drift regression coverage.** Added `scripts/__tests__/test-export-parser-drift.js` for CRLF export preservation, documented inline YAML parsing, URL truth-source drift status, and lint enforcement of schema conditionals.
- **Installable CLI package shape.** `package.json` now publishes as `@skill-graph/cli`, exposes the `skill-graph` binary, declares public scoped-package publishing metadata, and ships the scripts, schemas, examples, docs, starter skills, and license needed by downstream users. `bin/skill-graph.js` dispatches to the existing implementation scripts (`lint`, `manifest`, `route`, `drift`, `overlap`, `routing-eval`, `export`, `audit`, `protocol-check`) instead of introducing a second command framework.
- **Anchored grounding truth sources + eval-run receipts.** `grounding.truth_sources` now accepts structured entries with `path`, optional `line_range`, optional `anchor`, and optional `note` while preserving legacy string entries. Drift hashing now normalizes CRLF and can hash only the cited line slice. New optional `eval_last_run` records `{ at, status, runner?, model?, receipt?, receipt_hash? }` so `eval_state: passing` / `monitored` can point at evidence.
- **Top-level `concept` block + `comprehension_state` field (additive, no `schema_version` bump).** The 7-field universal-subject teaching block — previously authored as a `## Concept Card` body section in adopter SKILL.md files with bold-label parsing — is now a structured frontmatter object. Schema additions (all in `schemas/skill.v3.schema.json`): (1) new optional top-level `comprehension_state` enum `[absent, present]`; (2) mirrored nested `eval.comprehension_state` (v3.1 alias pattern); (3) new optional top-level `concept` object with seven required string sub-fields (`definition`, `mental_model`, `purpose`, `boundary`, `taxonomy`, `analogy`, `misconception`) and per-field `minLength` floors (10 or 20 chars) that prevent empty strings without capping maximum length; (4) one new `allOf` rule that requires the `concept` block when `comprehension_state: present`. **No total word ceiling, no token budget.** Author each field as deeply as the concept requires — skill content is sized by what the subject demands, not by a length cap. Drove the migration in adopter repos away from body-section grafts and back into schema-validated metadata — closes the "Concept Card is a 6th layer outside the contract" finding from the 2026-05-12 review. Distinct from `## Philosophy` (about *why this skill file exists in this repo*): the `concept` block is about *what the subject is, universally*. Adopters with legacy body sections migrate via a per-adopter codemod that lifts each `**<Label>:**` value into the corresponding frontmatter field; the seven labels map deterministically to the seven keys (`What it is` → `definition`, `Mental model` → `mental_model`, `Why it exists` → `purpose`, `What it is NOT` → `boundary`, `Adjacent concepts` → `taxonomy`, `One-line analogy` → `analogy`, `Common misconception` → `misconception`). The codemod preserves all prose verbatim; it does not summarise, shorten, or rewrite content.
- **v3.1 preferred field-name aliases (additive, non-breaking).** A roundtable critique of the v3 contract identified sign-drift, polysemy, and casing-inconsistency issues in 10 field names. v3.1 adds the renamed forms as optional aliases that validate alongside the legacy names. v4 will make them canonical and remove the legacy names. The added aliases: `archetype` (for `type`), `category_path` (for `category`), `reviewed_at` (for `freshness`), `allowed_tools` (for `allowed-tools`), `eval: { artifacts, content_state, routing_coverage }` (for the top-level `eval_artifacts` + `eval_state` + `routing_eval` triple), `grounding.subject` (for `grounding.domain_object`), `grounding.claim_scope` (for `grounding.grounding_mode`), `compatibility.agent_runtimes` (for `compatibility.runtimes`), `compatibility.node_version` (for `compatibility.node`), `portability.export_targets` (for `portability.targets`), `drift_check.verified_at` (for `drift_check.last_verified`). All 80 shipped skills continue to validate unchanged. `SKILL_METADATA_PROTOCOL.md` § "v3.1 — preferred field-name aliases" documents the migration; `docs/SKILL_METADATA_PROTOCOL_field-reference.md` carries the per-alias narrative.

### Fixed
- **Export body loss on CRLF files.** `scripts/export-skill.js` now preserves the body when the source `SKILL.md` uses Windows line endings, and the export verifier exercises the same full export path instead of only rebuilding frontmatter.
- **Exported skill-name safety.** Export normalizes `/` and `:` in names for plain SKILL.md compatibility, and the verifier rejects exported names that still contain those path/YAML-hostile characters.
- **Schema conditional lint parity.** `scripts/skill-lint.js` now enforces `stability: deprecated -> superseded_by` and `comprehension_state: present -> concept`, matching the JSON Schema `allOf` contract.
- **Scoped package spelling.** Package docs and metadata now consistently use `@skill-graph/cli`, matching the project name and exposed `skill-graph` binary.
- **Router `activation.paths` semantics.** `scripts/skill-graph-route.js` now applies gitignore-style negation lists correctly: positive globs include, later `!` globs subtract, and negation-only lists match nothing. The matcher also supports brace expansion (`*.{js,ts}`) and exports `matchPathList()` for unit coverage. This closes the gap where a negated path could accidentally behave like a positive match.
- **v3.1 alias contract enforcement.** `scripts/lib/alias-contract.js` centralizes preferred/legacy alias parity checks. Lint now errors when both forms are present with divergent values, and the manifest generator refuses to compile divergent aliases while passing valid alias fields through into the manifest projection.
- **Installed CLI root resolution.** `scripts/lib/roots.js` separates the package root from the caller workspace. Installed commands resolve schemas and packaged fixtures from `@skill-graph/cli`, but read and write skills, manifests, drift baselines, and workspace config relative to the consumer repository. Lint and overlap now honor `.skill-graph/config.json` skill roots and explicit non-default root directories.
- **ADR 0006 normative bug — `owl:disjointWith` is irreflexive, not reflexive.** Line 32 originally claimed `owl:disjointWith` is "symmetric, reflexive". OWL class-disjointness is irreflexive (a class is never disjoint with itself; every class is identical to itself). Fixed in `docs/adr/0006-revise-predicate-rename.md`.
- **Schema description hygiene.** Stripped ADR back-references (`Per ADR 0003`, `See ADR 0006`, etc.) and version-history phrasing (`v3.0 name`, `v3.1 preferred name`, `v3.1 addition`, `v2-compatible`, `v0.5.0 loosened`) from inline `description:` fields in `schemas/skill.v3.schema.json` and the unversioned mirror. Migration history belongs in `SKILL_METADATA_PROTOCOL.md` and this `CHANGELOG.md`, not in JSON-Schema descriptions that surface in IDE hovers and validation errors.
- **`allowed-tools` separator inconsistency.** Schema description said "comma-separated whitelist"; `SKILL_METADATA_PROTOCOL.md` and every example use space-separated. Schema description corrected to "space-separated".
- **`SKILL_METADATA_PROTOCOL.md` § Conditionally required.** The `keywords` row claimed `allOf` enforcement; the schema has no such `allOf`. The table now distinguishes schema-`allOf` rules (`extends`, `grounding`, `superseded_by`) from lint-enforced rules (`keywords`).
- **`docs/skill-metadata-protocol.md` § Anatomy Mermaid diagram.** Field count corrected from 32 to 33.
- **`SKILL_METADATA_PROTOCOL.md` v2→v3 migration table.** The relation-predicate-names row implied `boundary` was deprecated alongside `adjacent`. Per ADR 0006, `boundary` stays canonical; only `adjacent` is deprecated. Row rewritten.
- **Windows-safe verification and script metadata.** `package.json` now describes the repo as a private CommonJS script project instead of advertising missing build outputs, and the verification scripts run under Node's CommonJS loader. Frontmatter parsing, archetype-section extraction, contract heading extraction, manifest path normalization, and drift-baseline recording now handle CRLF paths/files correctly on Windows.
- **Current-library documentation drift.** README, architecture, primer, manifest-contract, concept-map, quickstart, and recommendation docs now reflect the current 36-field canonical schema, 40 shipped skills plus template sample manifest, 7-check contract gate, shipped overlap checker, and the role of `skillsmp.com` / `skills.sh` as public skill libraries used before Skill Graph relevance metadata.
- **Positioning angle corrected.** README, primer, and marketplace positioning now frame Skill Graph as a project-relevance metadata protocol for AI SKILL.md: area, angle, taxonomy, semantic cluster, project fit, methodology, framework, grounding, and verification loop first; routing, trust, clustering, drift checks, and eval loops are downstream effects.
- **Skill metadata hygiene.** Long routing descriptions, duplicate trigger/keyword surfaces, and stale drift baselines were cleaned up; `docs/SKILL_METADATA_PROTOCOL_field-reference.generated.md` and `examples/skills.manifest.sample.json` were regenerated from source tooling.

### Changed
- **SKILL.md export strictness.** `scripts/export-skill.js` now JSON-encodes structured Skill Graph extension metadata as strings under SKILL.md `metadata`, matching the base spec's string-to-string metadata contract.
- **URL truth-source drift semantics.** `scripts/skill-graph-drift.js` now reports URL grounding references as `EXTERNAL_UNHASHED` instead of treating them as broken local files; manifest drift detection skips URL hashes unless a separate fetch-and-hash workflow records evidence.
- **Documented inline YAML forms now parse.** The frontmatter parser now accepts inline arrays/maps and inline object sequence items used by docs and examples, such as `keywords: [a, b]` and `- { skill: x, reason: y }`.
- **R1 — v3.1 SKOS predicate runtime migration closed (Critical).** The 4-reviewer board meeting (2026-05-04 multi-model synthesis, kept private) flagged a silent data-loss bug: ADR 0001 added `related`, `broader`, `narrower` as preferred v3.1 predicates and the schema accepted them, but the runtime hardcoded the iteration list at `['adjacent', 'boundary', 'verify_with', 'depends_on']` in `scripts/generate-manifest.js`, `scripts/skill-lint.js`, and the alias-target check in `scripts/skill-graph-route.js`. Skills following the ADR with the new SKOS names had their relations silently dropped from the compiled manifest. The fix extends the iteration list across all four runtime files to include `related`, `broader`, `narrower`, plus `disjoint_with` (now a separate orthogonal relation per ADR 0006). Back-compat for `adjacent` is preserved — both names continue to round-trip. Verified by new test `scripts/__tests__/test-v3-1-skos-runtime.js`, which round-trips the full predicate set through `buildSkillEntry()` against the new fixture at `examples/tests/v3-1-skos-fixture/SKILL.md`.
- **R2 — ADR 0001 § Decision #2 revised by ADR 0006 — `boundary` is not `owl:disjointWith`.** The original ADR framed `boundary` as a deprecated alias for `disjoint_with` mapping to OWL `owl:disjointWith`. The board meeting flagged this as a category error: `boundary` is a routing-layer asymmetric handoff (the router uses it to redirect wrong-skill queries) and `owl:disjointWith` is a formal class-theoretic claim — they operate at different semantic layers. GPT-5.5's empirical audit confirmed the in-tree JSON-LD context already mapped `boundary` to `sg:disjointOwnership` (a custom Skill-Graph predicate), not to `owl:disjointWith` — so the implementation was correct; only the ADR text was wrong. New ADR `docs/adr/0006-revise-predicate-rename.md` documents what the implementation already does: `boundary` stays canonical for routing-layer use, `disjoint_with` becomes a separate orthogonal relation with explicit OWL class-disjointness semantics (Option B from synthesis IP-R2 § 4 — preserves capability per the project's quality doctrine). Schema descriptions, JSON-LD `@context`, lint deprecation warnings, ADR 0001 status banner, and `docs/SKILL_METADATA_PROTOCOL_field-reference.md § relations` all updated to match. The deprecation warning on `relations.boundary` is removed; the warning on `relations.adjacent → related` (Decision #1, sound) stays.
- **`scripts/skill-graph-route.js` Stage 4b — `broader` parent recall boost.** Per ADR 0001 Decision #3, `relations.broader` declares cross-skill generalisation. The router now treats this as a co-load signal: when a top-match declares `broader: [parent]`, the parent is added to the selection as a generalisation companion. One hop only (no transitive ancestor walk), to mirror Stage 4's verify_with shape. The inverse `narrower` is NOT used to drive co-load — a parent match should not pull in arbitrary children.
- **`schemas/skill.context.jsonld` v1.1.0** — `boundary` keeps its existing `sg:disjointOwnership` mapping (correct per ADR 0006); `disjoint_with` switches from alias-of-`boundary` to its own `owl:disjointWith` mapping. New `owl` namespace declaration (`http://www.w3.org/2002/07/owl#`) added. New `_adr_anchors` block points back to ADR 0001, ADR 0002, and ADR 0006 so future maintainers see the reasoning for the split mapping.
- **`docs/SKILL_METADATA_PROTOCOL_field-reference.md § relations` enriched with the ADR 0006 split.** The relations key reference now describes the seven valid keys (was four), the `boundary` vs `disjoint_with` semantic distinction, and a worked example for the rare formal-OWL-class-disjointness case. The "deprecated alias" framing on `boundary` is removed. Added a new "When NOT to use" warning: do not use `disjoint_with` as an emphatic `boundary` — the OWL semantics are real and reaching for them changes how RDF consumers reason about the graph. Also fixes F22: the `routing_eval` § block now correctly states all 8 starters declare `present` and pass the harness 8-of-8.
- **`scripts/generate-manifest.js`** — when imported as a module (rather than run via `node generate-manifest.js`), the file now exports `buildSkillEntry`, `sortKeys`, `validate`, `detectDrift`, `normalizeTruthSource`, `sha256TruthSource`, `computeSummary` for unit-test consumers. CLI behaviour is unchanged when invoked directly. Enables the new R1 round-trip test without ceremony.
- **`scripts/skill-lint.js`** — relation-target validation now iterates the full v3.1 predicate set (`related`, `broader`, `narrower`, `adjacent`, `boundary`, `verify_with`, `depends_on`, `disjoint_with`) instead of the hardcoded v3.0 four. Dangling targets in any of the new predicates are now caught.
- **`schemas/manifest.schema.json` + `manifest.v3.schema.json`** — extended the manifest's `relations` block to accept `related`, `broader`, `narrower`, and `disjoint_with` alongside the v3.0 four. Without this update the runtime fix in `generate-manifest.js` would emit manifests that fail their own validation (the gap was found during Path 1 lint when new skills used `related`). Both schema files updated to keep C6 parity.
- **R8 — field-reference docs split + F22 schema description drift fixed.** The 1000-line `docs/SKILL_METADATA_PROTOCOL_field-reference.md` is now the apex of a three-doc structure: (1) `SKILL_METADATA_PROTOCOL_field-reference.md` — hand-curated prose with examples and lint notes (canonical for authoring); (2) `SKILL_METADATA_PROTOCOL_field-reference.generated.md` — auto-generated index from `schemas/skill.v3.schema.json` description strings (drift-free against the schema); (3) `field-rationale.md` — hand-authored "why this field exists" rationale for the ten non-obvious fields (`scope`, `eval_artifacts`, `eval_state`, `routing_eval`, `relations.depends_on`, `relations.verify_with`, `relations.broader`, `grounding.evidence_priority`, `lifecycle.review_cadence`, `portability.readiness`). The existing prose was preserved entirely (per `quality-doctrine` § "improve = enrich, never simplify"); a new "Three docs, three genres" preamble at the top of `SKILL_METADATA_PROTOCOL_field-reference.md` navigates between them. F22 root cause closed: 20 top-level schema fields that lacked `description` strings (which made the auto-generated index emit `"_No description in schema._"` placeholders) now have canonical descriptions in both `schemas/skill.v3.schema.json` and `schemas/SKILL_METADATA_PROTOCOL_schema.json`, sourced from the hand-curated prose.
- **`scripts/gen-field-reference.js` → `scripts/build-field-reference.js`** — renamed to match the canonical "build-" verb convention (synthesis IP-R8). Header banner in the generated file now points at the new name. No callers existed outside the script itself; no CI breakage.
- **R13 — SKILL_METADATA_PROTOCOL.md and skill-metadata-protocol.md enriched by genre.** The two docs are now coordinated by distinct genres: SKILL_METADATA_PROTOCOL.md is the **normative spec** (terse, boundary-aware, no rationale — the doc a consumer prints out and tapes to the wall); skill-metadata-protocol.md is the **design rationale** (pedagogical, philosophical, ADR-referencing). Three new sections in skill-metadata-protocol.md sharpen the rationale genre: (1) "Why archetypes are rigid vs anti-rigid" — full OntoClean treatment of `capability` / `workflow` / `router` / `overlay` per ADR 0003, including the `extends` vs `relations.broader` test; (2) "Why the Evaluation Status is orthogonal" — the three-axis decomposition of `eval_artifacts` / `eval_state` / `routing_eval` and why conflating them would force consumers to guess; (3) "How JSON-LD context maps fields to W3C terms" — full mapping table including the ADR 0006 split between `boundary` (sg:disjointOwnership) and `disjoint_with` (owl:disjointWith). SKILL_METADATA_PROTOCOL.md preamble now points at skill-metadata-protocol.md for the *why* and explicitly names itself as the *what*. README.md § Tier 2 entry updated to reflect the genre split. Both files grew (the normative spec 392→394, skill-metadata-protocol.md 325→405). Two of four reviewers had recommended retiring the separate normative spec; v4 explicitly rejected this under quality-doctrine § "improve = enrich, never simplify".

### Added
- **`docs/field-rationale.md`** — new hand-authored rationale doc for 10 non-obvious fields. Each section follows the same three-part structure: "Why this field exists", "Common confusion", "Worked example". Linked from `SKILL_METADATA_PROTOCOL_field-reference.md` and `SKILL_METADATA_PROTOCOL_field-reference.generated.md`.
- **`scripts/check-contract-consistency.js` C7 — Generated field-reference parity.** New check spawns `node scripts/build-field-reference.js --check` and surfaces non-zero exit as a structured contract error. Closes the loop where a schema description edit could land without a corresponding regeneration of `docs/SKILL_METADATA_PROTOCOL_field-reference.generated.md`. Sits alongside C1-C6 in the per-PR consistency gate.
- **Routing harness follow-up sprint — 9/9 starters PASS.** The library-wide harness run now returns `9 skill(s): 9 PASS, 0 FAIL, 0 SKIP`, up from `0 PASS, 9 FAIL` at the end of the harness sprint. The work split into two halves, both executed together per `docs/plans/routing-harness-followup.md`:
  - **L4 router scoring (`scripts/skill-graph-route.js`)** — four surgical changes closing 28 of 32 original failures. (1) `tokenize()` now drops a 46-entry English stopword set from both query and keyword sides; the keyword side additionally enforces `length >= 3` to keep single-char particles like "it" out of scoring. (2) `scoreSkill()` now runs keywords in TWO dedup passes — exact first, then substring — with separate per-token sets so a substring credit on "clean" from the phrase "cleanup" no longer blocks a later exact match on "clean" from the phrase "clean this up" (the bug that broke a11y and refactor hard-negatives in the first implementation pass). (3) Stage-5 boundary exclusion is now score-aware AND iterates only over `topMatches`, never `coLoaded` — a co-loaded `verify_with` partner can no longer boundary-exclude the very skill that brought it into the selection, which was the regression source that dropped debugging / refactor / testing-strategy to 1/7 on the first authoring pass. (4) `scored.sort()` comparator extended with the documented doctrine: scope (codebase > reference > portable) then type (workflow > capability > router > overlay) before alphabetical.
  - **L5 authoring sweep (8 SKILL.md files + `examples/skill-metadata-template.md`)** — targeted keyword enrichment, boundary extension, anti_example rewriting, and three adjacent-relation removals. Keywords: a11y gained 7 form/assistive-tech terms; debugging gained 16 failure-idiom terms including "used to work", "agent stuck", "broke the build"; documentation gained 12 prose-audience terms including "progressive disclosure" and "reading level"; lint-overlay gained 13 migration/gate terms; refactor gained 13 decomposition/rename terms; skill-router gained 15 meta-routing terms including "activate skill", "dispatch request", "routing decision"; skill-metadata-template gained 11 authoring terms; testing-strategy gained 7 coverage-planning terms. Boundaries: debugging↔testing-strategy↔refactor triangle hardened into mutual boundary (was adjacent, which the router was conflating); a11y += documentation; lint-overlay += testing-strategy; refactor += debugging; skill-router += debugging; skill-metadata-template += skill-router + graph-audit. Anti_examples: a11y dropped the visual-design coverage-gap; graph-audit rewrote two antis to use debugging and documentation vocabulary so the router absorbs them correctly; refactor rewrote the retry-logic anti to route to debugging; skill-router replaced the falsified "router activated the wrong skill" anti with "reproduce this routing mis-dispatch from production logs" (which correctly routes to debugging per the plan's P2.2 recommendation). Example rewrite: skill-router's first positive example changed from `"which skill should activate for 'my tests are failing in CI'?"` to `"activate the right skill for this agent request: 'my tests are failing in CI'"` — same meta-routing intent, but "request" adds a third skill-router-exclusive token that outscores debugging's two `tests`+`failing` tokens.
- **`routing_eval` flipped `absent` → `present` for 7 starter SKILL.md files** (a11y, debugging, documentation, graph-audit, lint-overlay, refactor, skill-router, testing-strategy; `examples/skill-metadata-template.md` stays `absent` because it teaches the authoring-time default). Restored under the same "honesty over green checkmarks" rule that drove the original downgrade — the harness now agrees, so the assertion is earned. Lint check 12 gates future regressions on every changed SKILL.md.
- **`skills/skill-router/SKILL.md`** — the `routing_eval: absent` downgrade block from the harness sprint is replaced with a `present`-restoration block explaining which of the four L4 fixes + L5 rewrites resolved the hard-negative.
- **`examples/skills.manifest.sample.json`** — regenerated via `node scripts/generate-manifest.js --include-template --timestamp 1970-01-01T00:00:00Z --output examples/skills.manifest.sample.json` to reflect all SKILL.md edits and the `routing_eval` flips.

### Added
- **5 new Tier A starter skills (Path 1 / IP-R9 Phase 1).** The OSS library grows from 8 starters to 13. Each new skill follows the v3.1 contract end-to-end (≥8 examples, ≥5 anti_examples, `relations.boundary` with `{skill, reason}` entries, grounding declared where applicable, `routing_eval: absent` until the harness verifies). Per the curated picks documented in [`docs/recommended-skills.md`](docs/recommended-skills.md):
  - **`skills/skill-scaffold/SKILL.md`** — closes the empty `scope: reference` gap. Authoring contract for new skills: copy → rename → adapt → strip teaching annotations → verify → commit. Covers archetype selection, semantic-layer discipline (`description` vs `## Coverage`), the lint-first authoring gate, and the routing-eval honesty rule.
  - **`skills/naming-conventions/SKILL.md`** — universal portable skill. Identifier morphology (verb-noun choice, plural/singular, prefix/suffix), casing per artifact kind, the verb contract (get vs fetch vs load vs compute vs validate vs assert), names that lie detection, rename coordination workflow.
  - **`skills/code-review/SKILL.md`** — universal workflow for reviewing PRs / diffs / proposed changes. Six-phase workflow (pre-review fact-gathering → tests-first reading → implementation reading → call-site reading → comment authoring → merge decision). Severity-grading rubric across nine review concerns. Explicit no-rubber-stamp rule for AI-generated diffs (with the 1.7-2.74× security-issue premium evidence).
  - **`skills/prompt-craft/SKILL.md`** — universal AI-engineering skill for writing, structuring, and improving LLM prompts. Seven-layer prompt anatomy. Output-format discipline (schema + example + output cue triple-reinforcement). The X-mention reinforcement effect on negative instructions. Defence against adversarial input. Iterative-improvement loop with mandatory eval-set measurement.
  - **`skills/owasp-security/SKILL.md`** — security baseline. Full OWASP Top 10 (2021) coverage with detection patterns and mitigation patterns per category. Threat-modelling four-question STRIDE-lite. AI-generated code premium (1.7-2.74× security-issue rate, top five CWEs). Defence-in-depth posture.
- **`docs/adr/0006-revise-predicate-rename.md`** — new ADR. Status: Accepted. Supersedes ADR 0001 § Decision #2 only (Decisions #1, #3, #4 stand). Documents the routing-layer-vs-ontology-layer distinction with W3C OWL 2 spec citation and the empirical evidence that the JSON-LD context was already correctly mapped. Cross-references ADR 0001 with the explicit "Supersedes Decision #2 only" note.
- **`scripts/skill-lint.js` — relation double-declaration check.** New warning when a skill declares the same target under both `adjacent` (deprecated) and `related` (preferred) — drops the deprecated entry to avoid double-counting in the compiled manifest. The check is intentionally narrow: `boundary` and `disjoint_with` are NOT treated as alias pairs (per ADR 0006 they have distinct semantics) so declaring the same target under both is legitimate and is not flagged.
- **`scripts/__tests__/test-v3-1-skos-runtime.js`** — new test. Builds a manifest entry from the v3.1 fixture and asserts every declared predicate round-trips: `related`, `broader`, `narrower`, `boundary`, `disjoint_with`, `verify_with`, `depends_on`. Synthesises a parallel in-memory fixture to verify `adjacent` back-compat continues to work. Exits 0 on PASS, 1 on FAIL. Self-contained (only Node built-ins, only the existing parse-frontmatter and generate-manifest helpers).
- **`examples/tests/v3-1-skos-fixture/SKILL.md`** — new fixture skill exercising the full v3.1 predicate set. Used by the test runner above; not a production skill (declares `eval_artifacts: none` and lives outside the default `skills/` walk).
- **`scripts/skill-graph-routing-eval.js`** — new Tier 4 consumer script that makes `routing_eval: present` honest. For every skill in the manifest it runs each `activation.examples[]` entry through `scripts/skill-graph-route.js` and asserts the top-1 winner equals the skill itself (positive-class); runs each `activation.anti_examples[]` entry and asserts the winner is NOT the skill and (when non-null) is named in `relations.boundary[]` (negative-class). Emits per-skill verdicts (`PASS` / `FAIL` / `NO_CASES`) plus per-case evidence. Verdicts: `PASS` when every case resolves correctly; `FAIL` on any positive miss or any hard-negative regression; `COVERAGE_GAP` (informational) when an anti-example correctly avoids the skill but no other skill absorbs it — the anti-example did its job; the gap is a future-sprint signal. CLI: `--manifest PATH`, `--skill NAME`, `--only-asserted`, `--json`, `--quiet`. Exits 1 on any `FAIL`. Self-contained — only Node built-ins, `require()`s `scripts/skill-graph-route.js` directly so the scoring/boundary pipeline is reused verbatim.
- **`scripts/lint/check-routing-eval.js`** + **`scripts/skill-lint.js` check 12** — new per-file lint check. Fires only when `routing_eval: present`; errors when (a) `examples` or `anti_examples` is empty (the harness has no prompts to evaluate), or (b) the harness reports any `FAIL` case for this skill. Each failing prompt surfaces as an individual lint error with a code-framed pointer at the `routing_eval:` line. Turns the `present` assertion into a verifiable claim — authors can no longer ship `present` without the harness agreeing. See `docs/SKILL_METADATA_PROTOCOL_field-reference.md § routing_eval` for the full enforcement contract.
- **`docs/ARCHITECTURE.md` § Tier 4 — Routing harness** — new Mermaid diagram + rendered PNG (`docs/images/routing-harness.png`) showing the per-skill decision path the harness follows. Caption asks one question ("How does `skill-graph-routing-eval.js` decide whether a skill's `routing_eval: present` claim holds?"). Twelve nodes, explicit legend (green = pass / endpoint, red = FAIL, pink = decision gate, blue = work, purple-dashed = `COVERAGE_GAP`). The asymmetry between the positive and negative gates is the whole point — positive cases must uniquely identify the skill; negative cases must only avoid it. Source in `docs/diagrams/routing-harness.mmd`. This is the fourth diagram in Tier 4's neighborhood (pipeline, drift-states, starter-graph, routing-harness) and is the only one that explains the harness's decision path, which no existing diagram covers.
- **`.github/workflows/skill-graph-lint.yml`** — new `routing-eval` job running after `lint`. Regenerates the sample manifest (so the harness reads the current authored state) then runs `node scripts/skill-graph-routing-eval.js --only-asserted` — exits non-zero if any skill with `routing_eval: present` fails the harness library-wide, complementing the per-skill lint check 12 for the same-PR enforcement case.

### Changed
- **`skills/skill-router/SKILL.md`** — `routing_eval: present` → `routing_eval: absent`. The harness revealed one hard-negative regression: the anti-example `"the router activated the wrong skill once — debug it"` routes back to `skill-router` instead of to `debugging`. Per the "honesty over green checkmarks" rule now documented in `docs/SKILL_METADATA_PROTOCOL_field-reference.md § routing_eval`, a falsified assertion is downgraded rather than defended. Re-flip back to `present` once the router's scoring or `skill-router`'s description boundary clause tightens enough that the harness agrees.
- **`docs/SKILL_METADATA_PROTOCOL_field-reference.md § routing_eval`** — the "Current status (forward-looking)" block is replaced with an "Enforcement" block documenting the harness + lint check 12 and naming the current starter-library state (0/9 pass today; the harness reveals the work). The forward-looking pointers to planned `scripts/skill-router.js` and `scripts/build-coverage.js` are deleted — the harness that absorbed both responsibilities now ships.
- **`examples/skill-metadata-template.md`** — the `routing_eval` TEMPLATE NOTE is rewritten to teach the new lint enforcement: `present` now requires populated `examples` + `anti_examples` AND a passing harness run (`node scripts/skill-graph-routing-eval.js --skill <name>`). Default remains `absent` at authoring time; adopters flip only after the harness agrees.
- **`scripts/lib/parse-frontmatter.js`** — scalar-value parsing now strips trailing YAML inline comments (` # ...` preceded by whitespace, outside of quoted strings). Previously the parser kept the entire raw line including the comment as the string value, which polluted `examples` / `anti_examples` array entries in the generated manifest with `# author-note` tails (authors write `- "prompt" # why this routes elsewhere`). The fix brings the parser in line with standard YAML semantics and cleans the manifest; all other consumers benefit transparently.
- **`examples/skills.manifest.sample.json`** — regenerated via `node scripts/generate-manifest.js --include-template --timestamp 1970-01-01T00:00:00Z --output examples/skills.manifest.sample.json` to reflect the cleaned parser (inline-comment tails dropped from `activation.examples` and `activation.anti_examples`) and the skill-router `routing_eval` downgrade.

### Added
- **`scripts/skill-lint.js`** — `truth_sources` entries in eval artifacts may now use `path#anchor` format (e.g. `skills/documentation/SKILL.md#doc-type-selection`) alongside the existing `path` and `path:start-end` forms. The linter extracts all markdown headings from the target file, slugifies them using the common GitHub/pandoc convention (lowercase, non-alphanumerics → hyphens, collapsed), and reports an error with the known-anchor list when an entry cites a slug that no heading produces. Anchors are drift-resistant: renaming a section forces an anchor update the linter catches, whereas editing section content around fixed line numbers silently drifts. Authors are encouraged to cite both a line range and a heading anchor for defense in depth.
- **`examples/evals/comprehension.json`** — documentation eval artifact expanded from 10 to 12 cases and migrated to dual-citation (line range + heading anchor). Eval 6 now also cites the frontmatter `relations.boundary` block so boundary grounding spans both the Do-NOT-Use table and the declared boundary reasons. Evals 11–12 add a new `rule_conflict` dimension — adversarial cases where two skill rules collide (same-commit doc updates vs. security hotfix time pressure; structural checklist passing vs. the "faster mental model" philosophical test) with explicit `expected_reasoning`.
- **`skills/documentation/SKILL.md` § Relations Rationale** — explicit justification for an empty `verify_with` surface: doc-drift and link-checking are owned by CI tools (`lychee`, `markdownlint`) that live outside the skill graph rather than by sibling skills. Points at where a future `doc-freshness` or `link-check` skill would live.
- **`docs/ARCHITECTURE.md` § System Model — how the pieces fit together** — top-level orientation diagram (5 entities: `SKILL.md`, `skill-lint.js`, `skills.manifest.json`, `skill-audit.js`, `audit artifacts`). Caption asks the single question the diagram answers ("What are the moving parts of Skill Graph, and who talks to whom?"). First thing readers see before the five authority tiers. Legend named inline. Rendered companion PNG committed at `docs/images/system-model.png` and embedded with an `<img>` tag so any viewer (not only Mermaid-aware ones) sees the diagram.
- **`docs/skill-metadata-protocol.md` § Anatomy** — rewritten to follow progressive disclosure. Replaces the previous 9-subgraph convoluted flowchart with a 4-node composition diagram (`SKILL.md` = Frontmatter + Body + optional Teaching Layer) and a navigable Markdown table listing all 32 authored fields grouped by 8 semantic purposes (Identity, Classification, Health & Drift, Evaluation Status, Activation & Routing, Relations, Grounding, Portability & Standards). Each field name links to its canonical definition in `docs/SKILL_METADATA_PROTOCOL_field-reference.md` — the diagram conveys shape, the table conveys enumeration, neither duplicates the schema. Rendered companion PNG at `docs/images/skill-anatomy.png`.
- **`docs/library-audit-workflow.md` § Loop at a Glance** — rewritten as two focused diagrams plus a bullet list. Diagram 1: the 5-phase horizontal flow captioned "What are the phases of an audit?" showing Select → Deterministic → Graded (optional) → Aggregate → Fix → Re-verify → Next with the PASS-skip shortcut. Diagram 2: a zoomed view of Phase 2 captioned "What happens inside graded mode?" showing the 7-dimension fan-out through the grader CLI and the parse step. Artifact traceability (which phase writes to `findings.md` / `verdict.md` / `scorecard.md`) moved from tangled dotted arrows into a clean bullet list. Rendered companion PNGs at `docs/images/audit-phases.png` and `docs/images/graded-mode.png`.
- **`docs/images/`** — new directory shipping rendered PNG copies of every Mermaid diagram in the docs. Each PNG is regenerated from the Mermaid source via `npx @mermaid-js/mermaid-cli -i <source>.mmd -o docs/images/<name>.png`. Source remains in the Markdown files so the diagrams stay diffable and editable; the PNGs exist purely so readers using viewers without Mermaid support still see the rendered image. Seven PNGs today: `system-model.png`, `skill-anatomy.png`, `audit-phases.png`, `graded-mode.png`, `manifest-pipeline.png`, `drift-states.png`, `starter-graph.png`.
- **`docs/diagrams/`** — new directory holding the standalone `.mmd` Mermaid sources for the three newest diagrams (`manifest-pipeline.mmd`, `drift-states.mmd`, `starter-graph.mmd`). Inline Mermaid in Markdown remains the editable canonical copy; the `.mmd` files exist so `npx @mermaid-js/mermaid-cli -i <file>.mmd -o <png>` renders without having to extract blocks from the Markdown first.
- **`docs/ARCHITECTURE.md` § Tier 3 — Pipeline** — new Mermaid diagram + rendered PNG (`docs/images/manifest-pipeline.png`) showing how `generate-manifest.js` projects authored frontmatter into `skills.manifest.json`. Caption question: "How does a SKILL.md become a manifest entry?" Seven nodes (authored SKILL.md set + optional workspace config → parse-frontmatter → rename/group → SHA-256 on truth sources → ajv validation → compiled manifest, with an exit-1 branch on validation failure). Ends the old gap where the compiler was prose-only. Proves Tier 3's topology earns its complexity.
- **`docs/ARCHITECTURE.md` § Tier 4 — Drift sentinel state machine** — new Mermaid `stateDiagram-v2` + rendered PNG (`docs/images/drift-states.png`) showing the five states a grounded skill can sit in (`NO_BASELINE`, `OK`, `STALE`, `DRIFT`, `BROKEN`) and every transition between them. Drives the argument for the v3 `drift_check.truth_source_hashes` + `lifecycle.stale_after_days` fields — without them the sentinel has nothing to compare against and nothing to time-box. Caption question: "What state can `skill-graph-drift.js` put a skill in, and what transitions it?"
- **`docs/ARCHITECTURE.md` § The starter graph** — new Mermaid diagram + rendered PNG (`docs/images/starter-graph.png`) showing the relations graph across all eight starter skills plus the template. First visual aid for Layer 7 (the Graph layer in the 7-layer authority model) — the layer whose silent drift produces confidently-wrong router decisions. Edge styles encode relation type (thick solid = `depends_on` / `extends`, dashed = `verify_with`, thin undirected = `adjacent`, red = `boundary`). Node style encodes archetype + scope. Caption question: "Who verifies whom, depends on whom, and boundaries whom out across the eight starters?"
- **`examples/skill-metadata-template.md` — new TEMPLATE NOTE for `category`** — explains that `category` is OPTIONAL, complements (never replaces) `category`, and should be removed for small libraries per `docs/SKILL_METADATA_PROTOCOL_field-reference.md § category` ("fewer than 20 skills rarely benefits"). Closes the authoring gap where adopters copy-pasted the template's hierarchical path without thinking about whether a tree structure earned its line count.
- **`examples/skill-metadata-template.md` — new TEMPLATE NOTE for `stability` + `superseded_by`** — explains the schema's `allOf` rule that requires `superseded_by` whenever `stability: deprecated`, with a commented-out worked example showing the two-field deprecation shape. Prior readers had to reach `schemas/skill.v3.schema.json` directly to discover the rule; the template now teaches it in-place.

### Changed
- **`docs/skill-metadata-protocol.md` § Authored vs Generated Fields** — corrected field count from 29 to 32 (doc drift fix). The previous list was missing `superseded_by` (required when `stability: deprecated`), `examples` (positive activation prompts — v0.5.0+), and `anti_examples` (negative activation prompts — v0.5.0+). All three fields exist in `schemas/skill.v3.schema.json` and have their own sections in `docs/SKILL_METADATA_PROTOCOL_field-reference.md`; only the summary count in Skill Metadata Protocol was stale.
- **`docs/manifest-contract.md` § Top-level authored fields** — same drift fix propagated here. Corrected heading count from 29 to 32 and added three table rows with the correct manifest projections: `superseded_by` (top-level flow-through when present, required when `stability: deprecated`), `examples` (grouped under `activation.examples`), `anti_examples` (grouped under `activation.anti_examples`). Renumbered rows 16–32 to preserve schema order. Caught while loading the full doc set in prep for the layered consultant prompt; fix follows the same pattern already applied in Skill Metadata Protocol.
- **`README.md`** — same fix, matched the 29 → 32 count in the "Shipping today" bullet that points at Skill Metadata Protocol.
- **`docs/ARCHITECTURE.md` § Further reading** — now points at the new System Model / Anatomy / Loop at a Glance diagram sections so the tiered tour terminates at the visual overviews. Extended again to include the three 2026-04-19 diagrams (Pipeline, Drift state machine, Starter graph).
- **`docs/manifest-contract.md` § Related documents + § Verification** — residual drift cleanup on top of the earlier 29 → 32 fix: "six shipped skills" → "eight starter skills plus the template (nine entries total)" (§ Related documents) and "five starters plus the template" → "eight starters plus the template" (§ Verification). The body rename-map table was already repaired; these two captions lagged by the same count mismatch.
- **`docs/skill-metadata-protocol.md` § Schema Strictness** — dropped the dated "v1 OSS schemas are intentionally strict" framing to "The Skill Graph schemas are intentionally strict." Version-scoped framing dates itself; the strictness rule is not a v1 artefact.
- **`docs/skill-metadata-protocol.md` § Relationship to the SKILL.md Standard** — the "Skill Graph extension" row now lists `examples`, `anti_examples`, and `superseded_by` alongside the other extension fields. The three fields exist in the v3 schema and have their own reference sections but were absent from this relationship table, creating an inconsistency inside the same document.
- **`docs/single-skill-audit-checklist.md` § 1 Frontmatter validity** — corrected the "(schema_version 3, orthogonal triple)" parenthetical on the Evaluation Status checkbox to "(orthogonal triple — shipped in schema_version 2 under SH-5784, retained in v3)". The triple was introduced in v2 and carried forward; the old wording misattributed its origin.
- **`docs/single-skill-audit-checklist.md` § 5 Content quality** — added a previously-missing checkbox for `## Verification` section presence, cross-referencing `docs/skill-metadata-protocol.md § Archetype Section Map` and `scripts/lint/check-archetype-sections.js`. Lint already enforced the requirement for `capability` and `workflow` archetypes; the human checklist silently skipped it.
- **`docs/SKILL_METADATA_PROTOCOL_field-decision-guide.md` § 1 Migration from v1** — collapsed the duplicated v1 → v2 scope rename table to a one-line pointer at `docs/SKILL_METADATA_PROTOCOL_field-reference.md § scope` plus a link to the `docs/manifest-contract.md § Migration Note — v2 → v3` codemod. Per the `documentation` skill's source-of-truth rule, canonical content lives in one place and is indexed elsewhere.
- **`skills/skill-router/SKILL.md` § description** — opener rewritten from "MUST be used when …" to "Use when …" for parity with the other seven starter descriptions. The description-opener convention documented in `docs/skill-metadata-protocol.md § Description Opener Convention` prefers "Use when"; the voice drift made the router skill stylistically louder than its siblings without adding routing precision.
- **`skills/testing-strategy/SKILL.md` § description** — negative-boundary clause tightened to name replacement skills inline: `debugging` (for chasing a known failure), `documentation` (for pure doc writing), and "no dedicated skill yet" (for conceptual architecture discussion). Prior version named only one of the three, leaving two implicit.
- **`examples/skills.manifest.sample.json`** — regenerated via `node scripts/generate-manifest.js --include-template --output examples/skills.manifest.sample.json` to reflect the updated `skill-router` and `testing-strategy` descriptions. Verified clean by `node scripts/check-contract-consistency.js` (C1–C6, 0 warnings) and `node scripts/skill-lint.js --include-template` (9 files, 0 errors).

### Added
- **`docs/plans/consultant-showcase-prompt.md`** — reusable layered prompt designed to work unchanged across Opus (Claude Code), GPT-5.4 (Codex plugin), and Gemini Pro. Teaches the 7-layer model of Skill Graph (Contract · Explanation · Enforcement · Consumer · Specimens · Knowledge · Graph, with Evolution as an orthogonal cross-cut) and requires consulting models to produce a layer map as Artifact 1 so they prove multi-layer understanding before proposing any changes. Includes dispatch instructions for all three CLIs plus guidance on the `gemini-2.5-pro` fallback (the `gemini` CLI on this machine does not expose `gemini-3-pro` as a valid alias — verified 2026-04-19).

### Design rationale (recorded for future diagram work)
Diagrams in this repo follow progressive disclosure — each answers exactly one question and states that question in the caption. The 32-field list lives once, in the schema, and is indexed (not duplicated) by the Anatomy section table, satisfying the `documentation` skill's source-of-truth rule. Every diagram has a legend block naming what each shape/colour means, a ~12-node ceiling, and a rendered PNG companion so readers in any viewer see the image. These rules were informed by a three-way consultation (Opus, Codex / GPT-5.4 via the in-session plugin, Gemini 2.5 Pro) plus web research on progressive-disclosure limits (max 2 levels before readers get lost — the C4-model rationale).

## [0.4.0] — 2026-04-18

The "v3 contract + reference consumer + multi-project workspace" release. A breaking schema bump (v2 → v3) coordinated with new authoring fields, executable drift evidence, multi-root workspace support, and two new reference consumer tools that exercise the full graph contract end-to-end.

### Added
- **`schemas/skill.v3.schema.json` + `schemas/manifest.v3.schema.json`** — pinned v3 copies. The unversioned schemas now track v3; v2 pinned copies remain in the repo as frozen prior-version stable copies.
- **`scripts/migrate-skill-v2-to-v3.js`** — line-based codemod that preserves author YAML style (comments, quoting, indentation). Applies the four v2 → v3 transformations: `schema_version: 2 → 3`, `family → category`, scalar `drift_check` → object with `last_verified`, scalar `compatibility` → object with `notes`.
- **`scripts/skill-graph-route.js`** — the reference consumer. Graph-aware skill selector that uses `relations` (all four kinds), `grounding.truth_sources`, `eval_state`, `drift_check`, `lifecycle`, and `workspace_tags` to make routing decisions visible. Emits ranked lists with per-skill reasons explaining why each skill was selected, co-loaded via `verify_with` / `depends_on`, or excluded via `boundary`. Supports `--project`, `--max`, `--min-eval-state`, `--path`, `--manifest`, `--json`.
- **`scripts/skill-graph-drift.js`** — the drift sentinel. Hashes every `grounding.truth_sources` entry with SHA-256, compares against the stored `drift_check.truth_source_hashes` baseline, and reports DRIFT / BROKEN / STALE / NO_BASELINE states. `--record --apply` updates the SKILL.md frontmatter in place with current hashes and today's date.
- **`.skill-graph/config.json`** (optional workspace config) — declares `skill_roots` (multiple directories the generator unions into one manifest) and `projects` (literal handle → `semantic_tags` mapping). Fallback to single-root `skills/` when absent. Documented in `docs/plans/multi-root-workspace.md`.
- **Four new optional v3 fields** in the authored frontmatter contract:
  - `category` — hierarchical browse path (`ecommerce/integrations/shopify`), pattern-validated.
  - `workspace_tags` — array of literal project handles or semantic tags for multi-project filtering. Absent = ambient/cross-project.
  - `lifecycle` — object with `stale_after_days` and `review_cadence`. Drives the drift sentinel's staleness flag.
  - `runtime_telemetry` — object with `feedback_source` path, `last_updated`, and optional success-rate metrics. Enables closing the loop from self-reported `eval_state` to externally-observed success/failure.
- **Relation item object form** — `relations.boundary` items may now be `{skill, reason}` objects (back-compat with bare strings), and `relations.depends_on` items may be `{skill, min_version}` objects. Reasons make the anti-ownership field self-documenting.
- **Manifest-level generated fields**: `workspace` block echoing the config, `summary.by_project` rollup, `skills[].project` handle, `skills[].category`, `skills[].workspace_tags`, `health.lifecycle`, `health.runtime_telemetry`, `health.drift_detected` (computed by the generator when hashes are recorded).
- **Paths negation** — `paths` globs may now be prefixed with `!` for gitignore-style exclusion, matching how real file scanners work.
- **`docs/plans/multi-root-workspace.md`** — design reference for the hybrid layout, config format, scope placement rules, and migration path from single-root to multi-root.
- **`docs/SKILL_METADATA_PROTOCOL_field-decision-guide.md` § 4–5** — new decision tables for `workspace_tags` (literal vs semantic tags, when to tag vs leave ambient) and a cross-cutting table disambiguating `category` vs `category` vs `workspace_tags` vs `routing_bundles`.

### Changed (breaking)
- **`schema_version`: 2 → 3.** All v2 field names and scalar shapes that change are hard-rejected by the v3 schema's `additionalProperties: false` and type constraints. Lint emits friendlier WARN lines pointing at the rename for the first v3 minor release window.
- **`family` → `category`** (rename). Values unchanged. The v2 name invited misuse as a routing signal; the v3 name makes the browse-taxonomy intent explicit.
- **`drift_check` shape: date string → object.** v2: `drift_check: "2026-04-15"`. v3: `drift_check: { last_verified: "2026-04-15", truth_source_hashes?: { ... } }`. The new hash map is optional but strongly recommended — it turns drift detection from self-asserted into evidence-backed.
- **`compatibility` shape: free-text string → object.** v2: `compatibility: "Node.js 18+, Git"`. v3: `compatibility: { runtimes?: [...], node?: string, notes?: string }`. The codemod moves the old string verbatim into `compatibility.notes`; authors upgrade to structured fields manually.
- **`summary.by_family` → `summary.by_category`** in the manifest projection. Consumers reading the old key must update.
- **`scripts/skill-lint.js`** — `DEPRECATED_V1_FIELDS` becomes `DEPRECATED_V1_FIELDS` for v1 names plus new v2 → v3 warnings for `family`, scalar `drift_check`, scalar `compatibility`. `AUTHORED_FIELDS_MUST_FLOW` adds `category`, `workspace_tags`, `category`.
- **`scripts/check-contract-consistency.js` C6** — now version-aware: resolves the current schema version from the unversioned schema, checks parity against the matching pinned copy, treats all prior versions as frozen (must exist, not checked for parity).
- **`scripts/generate-manifest.js`** — reads `.skill-graph/config.json` when present and walks multiple skill roots; falls back to single-root `skills/` otherwise. Computes SHA-256 on truth sources and emits `health.drift_detected` when baselines exist. `schema_version` written as `3`.
- **`scripts/export-skill.js`** — flattens the v3 `compatibility` object to a single string for SKILL.md export, concatenating `runtimes`, `node`, and `notes`. New v3 fields flow through the `metadata:` envelope without special handling.
- **`scripts/lib/parse-frontmatter.js`** — extended to parse block sequences of map entries (needed for v3 `boundary: [{skill, reason}]` and `depends_on: [{skill, min_version}]` shapes).
- **`examples/skill-metadata-template.md`** — upgraded to v3, now exercises `category`, `category`, object `drift_check`, object `compatibility`, object `boundary` with reason, and `lifecycle`.
- **All 8 starter skills** migrated to v3 via the codemod.
- **All 5 SKILL.md exports** regenerated with the flattened v3 `compatibility`.
- **`examples/skills.manifest.sample.json`** regenerated with v3 shape (nine skill entries: 8 starters + template).

### Migration
- Run `node scripts/migrate-skill-v2-to-v3.js` against any v2 skill directory to apply all four transformations automatically.
- See `docs/manifest-contract.md § Migration Note — v2 → v3` for the full mapping table.
- See `docs/SKILL_METADATA_PROTOCOL_field-reference.md` for per-field rules, v3 examples, and v2 → v3 migration notes inline with each changed field.

### Verification
- `node scripts/skill-lint.js --include-template` → 0 errors across 8 starters + template.
- `node scripts/check-contract-consistency.js` → C1–C6 OK, 0 warnings. C6 confirms v3 tracks unversioned and v2 is correctly frozen.
- `node scripts/generate-manifest.js --include-template --validate-only` → manifest valid against `schemas/manifest.schema.json`.
- `node scripts/skill-graph-route.js "accessibility screen reader"` → produces expected selection + co-load + boundary-exclusion output with per-skill reasons.
- `node scripts/skill-graph-drift.js` → reports baseline status for every skill with truth sources.

## [0.3.0] — 2026-04-17

The "contract honesty + graded audits" release. Three priorities from the internal-only OSS roadmap (`docs/plans/skill-graph-oss-roadmap.md`): narrow the `portability.targets` enum to what actually has a working transform (P1), promote the audit runner from a lint-stub seeder to a real prompt-driven grader (P2), and close the versioning-policy-to-reality gap with pinned v2 schema files (P3). No GitHub remote, no npm publish — this release matures the contract for internal use.

### Added
- `scripts/lib/audit-prompt-builder.js` — 7-dimension registry, context collector (reads `SKILL.md` plus `grounding.truth_sources`), IDENTITY-STEPS-OUTPUT prompt composer, response parser (requires a `<verdict>…</verdict>` JSON block), and coarse verdict aggregator. Powers the new graded audit mode.
- `scripts/skill-audit.js --graded` mode — calls an explicit external grader CLI once per scorecard dimension and merges the responses into `findings.md` / `verdict.md` / `scorecard.md`, replacing TODO placeholders with evidence-backed PASS / PASS WITH FIXES / FAIL verdicts. Grader CLI is user-supplied via `--grader-cli`; no API key is embedded and no default provider is selected by the script.
- `scripts/lib/mock-grader.js` — deterministic stand-in grader that produces canned `<verdict>` blocks per dimension. Lets CI smoke-test the graded pipeline without an API key and powers the `examples/audits/documentation/` graded fixture.
- `schemas/skill.v2.schema.json` and `schemas/manifest.v2.schema.json` — pinned v2 copies of the unversioned schemas, content-identical modulo `$id` and `title`. Consumers that want stability across a future v3 bump pin to these; consumers that want to follow latest use the unversioned files.
- `check-contract-consistency.js` C6 — enforces parity between the versioned and unversioned schemas. Drift is an error; the v2 files must track the unversioned files exactly until v3 ships.
- `CHANGELOG.md` (this file) — Keep-a-Changelog-formatted history seeded with the current bundle. Future releases get entries under `## [Unreleased]` as they land.

### Changed
- `examples/audits/documentation/` regenerated via the mock grader to demonstrate what `--graded` output looks like: real scores, `N/A` for grounding on a portable skill, evidence-cited findings.
- `docs/integrations/github-actions.md` — installation guidance rewritten so the primary path for that release was clone-and-vendor. The then-future `npm install --save-dev skill-graph` / `npx skill-graph-lint` snippets were preserved as a secondary **WHEN PUBLISHED** section so no reader mistook them for current 0.3.0 guidance.
- `docs/skill-metadata-protocol.md § Schema Versioning Policy` — rewrites policy point 3 as a current-state description now that the versioned schema files exist.
- `README.md` Status section — audit runner moves from "stub generator" bullet to a two-mode description and out of the Planned list. Validation table expands to six checks (adds C6). Quick Tour lists unversioned and v2 schemas side by side.
- `docs/plans/scripts-roadmap.md § Audit runner` — marked SHIPPED with full two-mode description, grader-CLI discipline, and links to the new `scripts/lib/audit-prompt-builder.js` and `scripts/lib/mock-grader.js`.

### Removed (breaking)
- `portability.targets` enum values `cursor`, `windsurf`, `copilot`, and `agents-md`. Only `skill-md` has a working transform (`scripts/export-skill.js`); the other four were compatibility goals that violated the contract's `additionalProperties: false` strictness rule. The enum now accepts only `["skill-md"]`. Re-adding a runtime is gated on a new RFC and the same PR that ships its transform. Starter skills, the template, the sample manifest, the exports fixtures, and all docs that mentioned the removed values were updated to match.

### Verification
- `node scripts/skill-lint.js --strict` → 0 errors across 8 starters.
- `node scripts/check-contract-consistency.js` → C1–C6 OK, 0 warnings.
- `node scripts/generate-manifest.js --validate-only` → manifest valid.
- `node scripts/skill-audit.js documentation --graded --grader-cli "node scripts/lib/mock-grader.js" --force` → 7 dimensions processed (6 grader calls + 1 N/A skip), 3 graded findings, 0 grader errors, non-TODO verdicts in all 3 artifact files.

## [0.2.0] — 2026-04-17 (pre-changelog, reconstructed from git)

The "v2 contract + scripts toolchain" release. Before 0.3.0 the project did not maintain a running changelog; this section reconstructs the bundle from the git history (commits `34bf6c0` through `3b2cec0`). Use this as a guide for pinning older consumers, not as a complete line-by-line history.

### Added
- `schema_version: 2` contract — split `eval_status` into the `eval_artifacts` / `eval_state` / `routing_eval` triple, renamed `scope` enum values (`generic` → `portable`, `operational` → `codebase`), renamed `portability.level` → `readiness` and `portability.exports` → `targets`, renamed `route_bundles` → `routing_bundles` (`6727a71`, SH-5784).
- Eight starter skills across all four archetypes and all three scopes: `a11y`, `debugging`, `documentation`, `refactor`, `testing-strategy`, `skill-router` (`router`), `lint-overlay` (`overlay`), `graph-audit` (`codebase` with a full `grounding` block). (`17c6609`, `823f19f`, SH-5783.)
- Five shipping scripts, all self-contained (Node built-ins only): `scripts/skill-lint.js` (schema + structural validation with code-frame diagnostics and archetype-aware section validator), `scripts/generate-manifest.js` (authored → generated projection), `scripts/export-skill.js` (SKILL.md export transform — single target: `skill-md`), `scripts/check-contract-consistency.js` (C1–C5 cross-artifact checks), `scripts/skill-audit.js` (lint-seeded stub generator). (`747f446`, `5c2ae16`, `d98a6a7`, `b88c61b`, `aad2e87`.)
- GitHub Actions self-CI workflow and a copy-paste consumer integration doc (`7fa5af6`, SH-5786).
- Authored-to-generated bridge doc, Schema Versioning Policy, relation-semantics decision tables, and the authoring-via-self-referential-template pattern (`6c2f694`, `a9cdd42`, `760e083`, `4194d54`).

### Changed
- `docs/skill-metadata-protocol.md` split into three focused files: overview + archetype map, field reference, and manifest contract (`90f78e5`, SH-5782).
- Renamed frontmatter fields: `domain_frame` → `grounding`, `evaluation_mode` → `grounding_mode` (`5862283`, SH-5779).

### Fixed
- Restored four optional schema fields that were accidentally dropped in an earlier manifest refactor (`8791558`, SH-5776).
- Repaired six factual errors in the shipped worked examples (`873c463`, SH-5777).

[Unreleased]: https://github.com/jacob-balslev/skill-graph/compare/v0.5.0...HEAD
[0.5.0]: https://github.com/jacob-balslev/skill-graph/compare/v0.4.0...v0.5.0
[0.4.0]: https://github.com/jacob-balslev/skill-graph/compare/v0.3.0...v0.4.0
[0.3.0]: https://github.com/jacob-balslev/skill-graph/compare/v0.2.0...v0.3.0
[0.2.0]: https://github.com/jacob-balslev/skill-graph/releases/tag/v0.2.0
