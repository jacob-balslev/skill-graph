name: Skill Graph Lint

on:
  push:
    branches: [main]
    paths:
      - 'skills/**'
      - 'scripts/skill-lint.js'
      - 'scripts/lint/**'
      - 'scripts/lib/**'
      - 'schemas/**'
      - 'examples/**'
  pull_request:
    paths:
      - 'skills/**'
      - 'scripts/skill-lint.js'
      - 'scripts/lint/**'
      - 'scripts/lib/**'
      - 'schemas/**'
      - 'examples/**'

jobs:
  lint:
    name: Lint skills
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Run skill lint
        run: node scripts/skill-lint.js --include-template
