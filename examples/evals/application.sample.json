{
  "_doc": "Worked specimen of a gate-9 application eval consumed by evaluate-skill.js --mode application and graded by lib/audit/graders/application-comparative-grader-prompt.md. Demonstrates the executable cases[] shape for the canonical database-migration skill. This sample is illustrative; live per-skill application evals belong under <skill>/evals/application.json.",
  "skill_name": "database-migration",
  "subject": "safe production database migrations",
  "mode": "application",
  "schema_version": 1,
  "cases": [
    {
      "id": 1,
      "scenario_type": "live-schema-change",
      "criticality": "critical",
      "red_herring": false,
      "scenario": "Add a non-null status column with a default to the live, 80M-row orders table without taking write downtime.",
      "context": "Table: orders (~80M rows, high write throughput). Requirement: add status text not null default 'pending'. Postgres 15, behind PgBouncer transaction pooling.",
      "question": "Produce the migration SQL and deploy sequence.",
      "expected_flags": [
        "A single blocking ALTER with NOT NULL DEFAULT can rewrite or lock a large live table.",
        "DDL must run on a direct/unpooled connection, not the PgBouncer transaction-pooled port."
      ],
      "expected_fix_hints": [
        "Use an expand-contract sequence: add nullable/defaulted column, backfill in batches, then enforce NOT NULL after verification.",
        "Split the rollout across deploys and include rollback/contract-phase handling."
      ],
      "absent_signals": [
        "Do not claim ADD COLUMN ... NOT NULL DEFAULT is safe merely because there is a default.",
        "Do not ignore the pooled-connection constraint."
      ]
    },
    {
      "id": 2,
      "scenario_type": "foreign-key-validation",
      "criticality": "high",
      "red_herring": false,
      "scenario": "Add a foreign key from order_items.order_id to orders.id on a 10M-row live table without blocking writes.",
      "context": "Tables: order_items (~10M rows) and orders. Live traffic continues during the rollout.",
      "question": "What migration sequence should be used?",
      "expected_flags": [
        "Adding a fully validated foreign key inline on a large live table can take an unsafe lock and scan.",
        "Validation should be decoupled from constraint creation."
      ],
      "expected_fix_hints": [
        "Add the constraint as NOT VALID first.",
        "Run VALIDATE CONSTRAINT in a separate step after checking data quality and operational timing."
      ],
      "absent_signals": [
        "Do not recommend a plain ADD CONSTRAINT ... FOREIGN KEY validated inline on a 10M-row live table."
      ]
    },
    {
      "id": 3,
      "scenario_type": "frontend-performance-red-herring",
      "criticality": "normal",
      "red_herring": true,
      "scenario": "Refactor the React OrderTable component to use a virtualized list for performance.",
      "context": "components/OrderTable.tsx renders 5,000 rows and scrolling janks.",
      "question": "How should the implementation be improved?",
      "expected_flags": [
        "This is a front-end rendering performance task, not a database migration task."
      ],
      "expected_fix_hints": [
        "Route to frontend performance/rendering guidance rather than SQL/DDL migration guidance."
      ],
      "absent_signals": [
        "Must not inject migration or DDL advice into a front-end virtualization task."
      ]
    }
  ]
}
