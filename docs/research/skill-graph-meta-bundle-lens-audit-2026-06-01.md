# Skill Graph Meta Bundle Lens Audit — 2026-06-01

> Mode: SYSTEM documentation and SYSTEM fixes only. CONTENT audits for individual skills are committed separately through the Skill Audit Loop.
> Scope: apply each available `skill-graph-meta` bundle skill as a lens against the `/Users/jacobbalslev/Development/skill-graph` repo/project.

## Bundle Status

| Skill | Load status | CONTENT audit status | SYSTEM lens status |
|---|---:|---:|---:|
| agent-eval-design | loaded | complete | complete |
| architecture-decision-records | loaded | complete | complete |
| best-practice | loaded | complete | complete |
| cognitive-load-theory | loaded | complete | complete |
| epistemic-grounding | loaded | complete | complete |
| eval-driven-development | loaded | complete | complete |
| evaluation | loaded | complete | complete |
| graph-audit | loaded | complete | complete |
| information-architecture | loaded | complete | complete |
| keywords | loaded | complete | complete |
| semantics | loaded | complete | complete |
| semantic-relations | loaded | complete | complete |
| ontology-modeling | loaded | complete | complete |
| taxonomy-design | loaded | complete | complete |
| knowledge-modeling | loaded | complete | complete |
| context-graph | loaded | complete | complete |
| context-engineering | loaded | complete | complete |
| skill-infrastructure | loaded | complete | complete |
| skill-router | loaded | complete | complete |
| skill-scaffold | loaded | complete | complete |
| doc-updater | missing from local skill roots | blocked | blocked |
| no-cutting-corners | missing from local skill roots | blocked | blocked |

The remaining loaded bundle skills are pending in bundle order.

## Findings Table

| ID | Lens skill | Severity | Surface | Finding | Evidence | Action taken |
|---|---|---:|---|---|---|---|
| SEM-1 | semantics | HIGH | Active docs and CLI help | Several active surfaces used the old meaning that Audit Status lives in `SKILL.md` frontmatter or a “Health Block,” even though ADR-0019 moved audit/eval/provenance state into `audit-state.json`. This creates a false operator signal about where audit/eval commands write. | `AGENTS.md` command table said “Writes to SKILL.md frontmatter”; `bin/skill-graph.js` help said “stamp the SKILL.md Health Block”; `skill-audit-loop/SKILL_AUDIT_LOOP.md` said Audit Status carried verdicts on every `SKILL.md` frontmatter. | Updated `AGENTS.md`, `bin/skill-graph.js`, and `skill-audit-loop/SKILL_AUDIT_LOOP.md` to name `audit-state.json` as the write/read surface. |
| SEM-2 | semantics | HIGH | Authoring docs | Several active onboarding/spec docs still taught the retired single-file required-field set. New authors would put sidecar-owned fields such as `schema_version`, `owner`, `freshness`, `drift_check`, `eval_artifacts`, `eval_state`, and `routing_eval` into `SKILL.md`. | `docs/QUICKSTART-30MIN.md` and `docs/ADOPTION.md` listed the old required fields as SKILL.md/frontmatter fields; `README.md` example included sidecar fields in YAML frontmatter; `docs/concept-map.md` described the old field count. | Split the examples and required-field descriptions into `SKILL.md` frontmatter plus `audit-state.json` sidecar in the active docs. |
| SEM-3 | semantics | MEDIUM | Protocol prose | The normative protocol doc had a correct ADR-0019 top callout but lower sections still used single-frontmatter wording, making the document internally inconsistent. | `skill-metadata-protocol/SKILL_METADATA_PROTOCOL.md` still said every skill is a single `SKILL.md`, described required thirteen fields, and called Audit Status frontmatter state. | Reworded the overview, required fields, health/eval sections, Audit Status section, and authored-vs-loop-owned field list around the two-file contract. |
| REL-1 | semantic-relations | HIGH | Active docs | Relation summaries exposed a partial relation vocabulary even though the schema and protocol define seven current relation fields plus the deprecated `adjacent` alias. This can make authors miss `relations.disjoint_with` and mistake the fixture graph for the full relation contract. | `AGENTS.md` listed only `related`, `boundary`, `verify_with`, and `depends_on`; `README.md` omitted `relations.disjoint_with`; `SKILL_GRAPH.md` said the fixture skill exercises "all four `relations.*` edge types." | Expanded `AGENTS.md` and `README.md` to include `broader`, `narrower`, and `disjoint_with`; reworded the fixture section to say it exercises four representative relation edge types and points readers back to the full schema. |
| REL-2 | semantic-relations | MEDIUM | Schema descriptions and protocol prose | Some schema descriptions still called `relations.boundary` a routing-layer "handoff," which conflicts with the actual score-aware exclusion mechanic and with the protocol warning that the field does not defer to the target. | `schemas/SKILL_METADATA_PROTOCOL_schema.json` described top-level `boundary` as distinct from `relations.boundary` "routing-layer handoff" and described `relations.boundary` as an "asymmetric handoff"; the protocol's same-domain doctrine used "handoffs" language. | Reworded those descriptions to "routing-layer exclusion" / "asymmetric exclusion edge" and changed the protocol doctrine to describe routing exclusions and routing distinctions. |
| ONT-1 | ontology-modeling | HIGH | JSON-LD context | The formal JSON-LD projection still described `relations.boundary` as a routing-layer "handoff" inside `schemas/skill.context.jsonld`, even though the ontology-level distinction is that `boundary` is a custom routing-exclusion predicate and `disjoint_with` is the OWL class-disjointness predicate. | `_adr_anchors.0006` and the property-scoped `relations.boundary` note used "handoff" wording while mapping to `sg:disjointOwnership`. | Updated the JSON-LD comments to "routing-layer asymmetric exclusion edge" and linked the note to ADR 0006 plus ADR 0018. |
| ONT-2 | ontology-modeling | MEDIUM | Accepted ADRs | The original JSON-LD ADR predates ADR 0006, ADR 0018, and ADR 0019, so a reader could still infer that `boundary` and `disjoint_with` share one predicate or that audit/eval fields are exported through frontmatter JSON-LD. | `docs/adr/0002-json-ld-context.md` mapped "`boundary` / `disjoint_with`" together and listed audit/eval/provenance fields in the original verification section; `docs/adr/0006-revise-predicate-rename.md` still used pre-ADR-0018 "handoff" wording. | Added current-state update notes to ADR 0002 and ADR 0006, and updated ADR 0018's related-doc pointer to avoid repeating "handoff." |
| TAX-1 | taxonomy-design | HIGH | README taxonomy docs | README's "Current Domain Paths" section still presented a v7-style global `domain` root table, mixing old domain roots with current v8 `subject` shelves and `taxonomy_domain`. This violates the current classification shape and teaches authors to copy a retired global category tree. | `README.md` listed "Domain root" / "Current `domain` paths" with roots such as `ai-engineering`, `architecture`, `content`, `modeling`, and `skill-system`; the live schema requires `subject` and treats `taxonomy_domain` as subject-local. | Replaced the table with subject-local `taxonomy_domain` guidance, shallow-path rules, and current examples from `knowledge-organization` and `agent-ops`. |
| TAX-2 | taxonomy-design | MEDIUM | Schema/reference wording | The schema and authored field reference still told authors to subdivide overloaded subjects via "`domain` slash-path" after the field was renamed to `taxonomy_domain`. | `schemas/SKILL_METADATA_PROTOCOL_schema.json` and `skill-metadata-protocol/field-reference.md` used "`domain` slash-path" in the `subject` balance rule. | Reworded both to "`taxonomy_domain` slash-path" and regenerated `field-reference.generated.md`. |
| KNO-1 | knowledge-modeling | HIGH | System model diagram | The library-level system model still represented Audit Status as part of `SKILL.md`, instead of modeling `SKILL.md` and `audit-state.json` as distinct knowledge artifacts joined by tooling. This was the wrong representation paradigm after ADR-0019. | `SKILL_GRAPH.md` diagram node said "`SKILL.md` frontmatter + body + Audit Status"; the auditor arrow said it "stamps Audit Status" onto the skill and the legend said state-of-truth lives in the skill. | Reworked the diagram to add `audit-state.json` as loop-owned state, show manifest/drift/lint joins across both files, and remove the stale rendered-image copy whose alt text encoded the old single-file model. |
| KNO-2 | knowledge-modeling | MEDIUM | Concept map/adoption docs | The concept map still blended strict classification with routing grouping and used "handoff" language for `relations.boundary`; the adoption doc still mentioned `workspace_tags` and implied `disjoint_with` was part of routing-failure repair. | `docs/concept-map.md` titled the section "four orthogonal classification axes" but listed five rows including `routing_bundles`; it described `boundary` as a handoff. `docs/ADOPTION.md` referenced `workspace_tags` and paired `relations.disjoint_with` with routing failures. | Reworded the concept map around classification/belonging dimensions plus routing bundles, changed `boundary` to routing-layer exclusion, and updated adoption guidance to `project[]` / `repo[]` plus `examples` / `anti_examples` / `relations.boundary`. |
| CTX-1 | context-graph | HIGH | Active graph diagram | The starter graph diagram still modeled the retired v7 starter-skill topology using archetype labels (`capability`, `workflow`, `router`, `overlay`), legacy `scope: codebase`, and `adjacent`/`extends` edges. That makes the active context graph look current while carrying retired nodes and edge names. | `docs/diagrams/starter-graph.mmd` used `a11y`, `debugging`, `documentation`, `graph-audit`, `lint-overlay`, `skill-router`, and `testing-strategy` with v7 archetypes and `adjacent`. | Replaced the diagram with the current four-fixture graph: `minimal-capability`, `with-grounding`, `with-relations`, and `comprehension-full`, using `depends_on`, `verify_with`, `related`, and `boundary`. |
| CTX-2 | context-graph | HIGH | Manifest mapping doc | The manifest worked example still modeled a single-file source graph, putting sidecar-owned fields such as `schema_version`, `version`, `owner`, `freshness`, `drift_check`, `eval_artifacts`, `eval_state`, `routing_eval`, and `lifecycle` in `SKILL.md` frontmatter. | `docs/manifest-field-mapping.md` "Authored frontmatter" example listed sidecar fields in YAML; the "deliberately absent" section claimed per-skill `schema_version` was not emitted in `skills[]`. | Split the worked example into `SKILL.md` frontmatter plus `audit-state.json`, changed `adjacent` to `related`, and clarified the two schema-version surfaces: manifest root version and per-skill sidecar schema version. |
| CENG-1 | context-engineering | MEDIUM | Agent-facing context guidance | The repo told agents to "load" broad sets of skills and audit references before changing agent behavior, quality rules, marketplace positioning, or skill-library structure, but it did not say how to keep that context selective, source-owned, and fresh. That encourages the overwhelming-context failure mode in the exact docs that govern future agent work. | `AGENTS.md § Quality Doctrine` named several skills and audit references to load, including skills that may be absent locally, without a rule for starting from the owning surface, avoiding generated/historical context, or continuing from a compact evidence list after large searches. | Reworked the guidance into a smallest-sufficient-context checklist: start from the owning surface, read only task-relevant skills and audit references, report missing skills as limitations, avoid generated/historical context unless source-owned, and compact long sessions into evidence lists. |
| INF-1 | skill-infrastructure | HIGH | Verification-gate docs | Active docs disagreed with the deterministic health-tooling surface: `package.json` already includes `audit-manifest:check` in full `npm run verify`, and the command now passes, but `README.md` and `SKILL_GRAPH.md` still described the audit-manifest verifier as separate or failing. | `package.json` includes `audit-manifest:check` in `verify`; `npm run audit-manifest:check` on 2026-06-01 reported `OK — every graded verdict has its gradeable artifact on disk`; `README.md` said the verifier runs separately; `SKILL_GRAPH.md § Current State` said it was a separate red gate not in verify and currently failed. | Updated README's Integrity Gate summary and `SKILL_GRAPH.md § Current State` to reflect the current wiring and green audit-ledger result, while keeping `npm run drift` separate because truth-source drift is still CONTENT-side debt. |
| ROUT-1 | skill-router | HIGH | `scripts/skill-graph-route.js` | Boundary-excluded skills could still leak their own co-load partners into the result because `verify_with` / `broader` / `depends_on` expansion ran before boundary exclusions. | Live query `node scripts/skill-graph-route.js --query "which skill should handle a routing coverage gap?"` selected `skill-router`, excluded `middleware-patterns`, but still co-loaded `security-fundamentals` as a `verify_with` partner of the excluded `middleware-patterns`. | Moved boundary exclusion before co-load expansion, skipped co-load targets already boundary-excluded, added `scripts/__tests__/test-router-boundary-co-load.js`, and wired it into `test:unit`. Targeted router tests passed; the live query no longer leaks `security-fundamentals` through the excluded skill. |
| ROUT-2 | skill-router | MEDIUM | `examples/evals/skill-router*.json` | Routing eval fixtures still contain retired `scope` / `type` tiebreaker language. This is now content/truth-source drift for the `skill-router` skill rather than a router-code defect. | `examples/evals/skill-router.json` still asks about a `type: workflow` vs `type: capability` tiebreaker; `examples/evals/skill-router.routing.json` describes scope/type tiebreakers and expected reasons. The `skill-router` CONTENT audit recorded truth DRIFT for those fixtures. | Recorded as `skill-router` CONTENT audit follow-up F2. Not patched in the SYSTEM commit to avoid mixing per-skill eval content with router infrastructure. |
| SCF-1 | skill-scaffold | HIGH | `examples/skill-metadata-template.md` | The active new-skill authoring template still taught retired v7-era scaffold concepts: adapting to an archetype, archetype-aware body layout, `Skill Metadata Protocol v7` keywords, capability/workflow examples, old protocol paths, and sidecar-owned state in the frontmatter narrative. This is the highest-risk place for stale authoring guidance because authors copy it. | The template's description and Coverage used "archetype"; keywords included `Skill Metadata Protocol v7`; examples asked how to choose capability vs workflow; truth sources pointed at `docs/skill-metadata-protocol.md`; the checklist still treated `eval_artifacts`, `eval_state`, `routing_eval`, and `drift_check` as frontmatter checks. | Updated the template to the current v8 + sidecar model: `subject` / `deployment_target` / free-text `scope`, body structure by intended use, current protocol paths, current sidecar checklist, and no retired `type` archetype guidance. `node scripts/skill-lint.js examples/skill-metadata-template.md` now passes with 0 warnings. |
| SCF-2 | skill-scaffold | MEDIUM | Companion authoring docs | Companion active references around the template still had stale scaffold signals: the sidecar template pointed at old field-reference paths and the direct routing-eval script, the canonical specimen row omitted `deployment_target`, and the repo guide used stale "archetypes" / "v6+" shorthand in active quick-reference text. | `examples/skill-audit-state-template.md` referenced `docs/SKILL_METADATA_PROTOCOL_field-reference.md` and `node scripts/skill-graph-routing-eval.js --skill <name>`; `SKILL_GRAPH.md` said the template demonstrated only `subject` / `scope`; `AGENTS.md` described design rationale as "archetypes" and Understanding fields as "v6+". | Updated the companion template paths and command, corrected the specimen row to `subject` / `deployment_target` / `scope`, and replaced active quick-reference shorthand with current classification/history wording. |
| AEV-1 | agent-eval-design | HIGH | `AGENTS.md § Evaluation Discipline` | The active eval guidance still described one old per-skill eval surface (`evals/evals.json`) and did not separate comprehension evals from application evals, even though application evals are the primary behavior-certification layer. This made the Behavior Gate look like one generic eval checklist rather than two different contracts. | `AGENTS.md` table listed "Per-skill comprehension" at `evals/evals.json`; the anti-patterns still referenced a single-scenario eval against a `≥7` threshold; the audit-loop summary listed `evals/evals.json` with ≥7 scenarios as the eval-coverage artifact. | Updated `AGENTS.md` to distinguish `evals/comprehension.json` from `evals/application.json`, add application-eval expectations (`cases[]`, red herrings, neutral prompts, `expected_flags`, `expected_fix_hints`, `absent_signals`), and correct the 5-case hard floor / practitioner-default language. |
| AEV-2 | agent-eval-design | MEDIUM | Comprehension eval spec/schema and audit runbook | Comprehension-eval docs/schema still described `skill_type` as mapping to the retired SKILL.md `type` axis and hard-coded "7 rubric dimensions" despite the live enum also reserving `misconception`. The audit-loop runbook also named retired eval filenames in its commit-path checklist. | `docs/comprehension-eval-spec.md` and `schemas/comprehension.schema.json` said `skill_type` maps to SKILL.md `type`; `skill-audit-loop/SKILL_AUDIT_LOOP.md` said cases cover 5 of 7 dimensions and listed `skills/<skill>/evals/evals.json` / `eval-set.json` in commit paths. | Reworded `skill_type` as eval-case metadata, updated the rubric-dimension wording to include `misconception`, changed runbook checks to the current comprehension/application artifacts, and changed "Health Block" schema wording to "Audit Status." |
| ADR-1 | architecture-decision-records | HIGH | ADR 0019 status | The sidecar ADR still described itself as accepted but not implemented, even though sidecar schema, manifest joins, audit/lint readers and writers, templates, and per-skill `audit-state.json` paths are live. Future agents could read the ADR status and wrongly treat the implemented two-file contract as a pending plan. | `docs/adr/0019-audit-state-sidecar-separation.md` said implementation was scoped in a plan, the schema was unchanged until that work landed, and the status section said "Not yet implemented." | Updated ADR 0019's status banner and status section to "Accepted and implemented," named the live surfaces, and clarified that corpus migration continues per-skill through `/audit:*`. |
| ADR-2 | architecture-decision-records | MEDIUM | Older ADR status/update notes | Older ADRs carried stale current-state statements after the v8 and sidecar work landed: ADR 0017 said the current required axes were only `subject` + `scope`, ADR 0005 still looked like an accepted-but-deferred freshness implementation path, and relation ADR notes still used deference wording for `relations.boundary`. | `docs/adr/0017-five-axis-classification-model.md` omitted required `deployment_target`; `docs/adr/0005-freshness-consolidation.md` still targeted a v4 `asserted_at` / `stale_after` cut; `docs/adr/0001-predicate-set.md` and `docs/adr/0006-revise-predicate-rename.md` used routing-layer deference wording. | Corrected ADR 0017's current v8 required fields, marked ADR 0005 superseded for the live v8 sidecar implementation by ADR 0019, and reworded the active relation-decision annotations around routing-layer exclusion. |
| BPR-1 | best-practice | HIGH | Audit-status tooling and schema descriptions | Active executable surfaces still described audit verdicts as a SKILL.md/frontmatter "Health Block," and one verifier still read behavior verdicts from SKILL.md instead of the live `audit-state.json` sidecar. This could make honest-downgrade checks miss migrated skills in the nested canonical library. | `scripts/check-audit-manifest.js` read `workspace/skills/<skill>/SKILL.md`; `schemas/manifest.schema.json` described audit fields as pass-through from authored frontmatter; `scripts/backfill-field-purpose-comments.js`, `scripts/generate-manifest.js`, `scripts/skill-graph-route.js`, `scripts/check-schema-constants.js`, and `schemas/skill.context.jsonld` still used old Health Block/frontmatter wording. | Updated `check-audit-manifest.js` to resolve flat and nested skill dirs, read `audit-state.json` first, and keep SKILL.md frontmatter only as a legacy fallback. Updated active comments/schema descriptions to "Audit Status" and "joined from audit-state.json." |
| BPR-2 | best-practice | MEDIUM | Active protocol docs and fixtures | Several active docs and fixtures still taught `relations.boundary` as a handoff/deference relation, after the runtime and protocol had moved to same-domain routing exclusion language. | `docs/glossary.md`, `docs/AUTHORING-QUICKSTART.md`, `docs/CONFORMANCE.md`, `skill-metadata-protocol/field-reference.md`, `skill-metadata-protocol/design-rationale.md`, `examples/tests/v3-1-skos-fixture/SKILL.md`, `examples/fixture-skills/with-relations/SKILL.md`, and `examples/fixture-skills/comprehension-full/SKILL.md` used "handoff" wording. | Reworded those active SYSTEM references to routing exclusion / ownership suppression, preserving historical ADR bodies and CONTENT-classified seed audit artifacts separately. |
| BPR-3 | best-practice | MEDIUM | Fixture portability | The grounded-skill fixture pointed readers to private/unavailable example skills even though the current public bundle has project-grounded Skill Graph skills available. | `examples/fixture-skills/with-grounding/SKILL.md` told readers to use `nextauth-patterns` or `query-tier-safety` as production project-grounded examples. | Replaced those examples with current available project-grounded skills: `skill-router` and `graph-audit`. |
| CLT-1 | cognitive-load-theory | HIGH | Protocol first screen | The protocol's first screen forced authors to hold the two-file contract, required fields, activation fields, relation vocabulary, schema pointers, and authoring shortcut in long dense sentences. This creates extraneous load at the exact point where a cold-start author needs chunked orientation. | `skill-metadata-protocol/SKILL_METADATA_PROTOCOL.md` had a single dense "Currently enforced" block and a single dense "Author shortcut" sentence listing every field group before the reader had formed the schema. | Replaced the current-contract sentence with a two-row file/owner/required-fields table and replaced the author shortcut with a four-step field-group table. |
| CLT-2 | cognitive-load-theory | MEDIUM | Audit-loop operation table | The audit-loop operations table mixed the reader's task classification question with long field lists and still described writes as "flat fields" instead of chunking by sidecar vs skill body. | `skill-audit-loop/SKILL_AUDIT_LOOP.md § The Four Operations` opened with "flat fields in the skill's Audit Status" and the `audit`/`evolve` rows carried long multi-concept cells. | Reworded the intro around "inspecting, editing, grading, or walking the corpus" and changed the writes column to group `audit-state.json` writes separately from `SKILL.md` instructional edits. |
| EPI-1 | epistemic-grounding | HIGH | Drift and truth-verdict docs | Active docs implied that running drift simply feeds `truth_verdict`, without labeling the evidence boundary that `UNGROUNDED` means no local truth-source baseline exists. This can make a portable externally sourced skill look truth-certified when the hash sentinel had nothing to compare. | `node scripts/skill-graph-drift.js --json ../skills/skills/meta-methods/epistemic-grounding` returned `status: UNGROUNDED`, while `SKILL_GRAPH.md`, `skill-audit-loop/SKILL_AUDIT_LOOP.md`, `docs/skill-audit-loop-executable-map.md`, and `docs/field-state-matrix.md` used unqualified "feeds truth_verdict" / "rolled up from drift_status" wording. | Reworded those docs so hash-based drift evidence only supports truth roll-up when declared truth sources are present and hashable; `UNGROUNDED` is now documented as missing hash coverage, not certification. |
| EPI-2 | epistemic-grounding | MEDIUM | Stub-audit evidence labels | The audit-runner role summary described stub mode as "lint-seeded TODO findings" without explicitly saying the artifact remains incomplete until human or graded review replaces the TODOs. | `SKILL_GRAPH.md § Audit` described stub mode tersely; `docs/skill-audit-loop-executable-map.md` warned that stub audits are easy to overread as audited but did not name the required completion action. | Added "incomplete until human review" to the audit-runner role summary and made the executable-map warning say the human/graded review must replace TODO sections. |
| EDD-1 | eval-driven-development | HIGH | Comprehension eval spec | The binding comprehension-eval spec listed eight rubric dimensions but still said the practitioner default was seven cases "one per rubric dimension." That under-samples the eval surface and makes the newly recommended `misconception` dimension easy to omit. | `docs/comprehension-eval-spec.md` had `dimension` values for `definition`, `mental_model`, `purpose`, `boundary`, `taxonomy`, `analogy`, `application`, and `misconception`, while the top-level field table and minimum/recommended section still said 7 cases = one per dimension. `AGENTS.md § Evaluation Discipline` repeated the 7-case default without explaining full-dimension coverage. | Updated the binding spec and AGENTS summary to say 7-8 cases is the practitioner default: seven covers core dimensions for most skills, eight gives one case per rubric dimension including `misconception`. |
| EVA-1 | evaluation | HIGH | Audit-loop completion rule and wrap contract | The audit loop had explicit Integrity and Behavior verdict requirements, but the runbook did not require a final 1-5 completion score, score ceilings, evidence packet, or residual-risk/unrun-check note before releasing a skill audit as completed. That leaves room for "lint passed, artifacts exist, done" optimism. | `skill-audit-loop/SKILL_AUDIT_LOOP.md § Completion Rule` listed checklist/verdict requirements but no audit-report score or score ceilings; Part 3 Step 7 scorecard rows omitted an audit report completion score; Step 10 allowed `--status completed` without a score-ceiling gate; Step 13 `/wrap` did not ask for evidence packet, residual risks, or unrun checks. | Added a 1-5 audit-report completion score, mechanical score ceilings, a required scorecard row, a Step 10 completion gate, and Step 13 `/wrap` fields for evidence packet, score ceilings, residual risks, and unrun checks. |
| GRA-1 | graph-audit | HIGH | Repo-grounded graph-audit eval fixture | The active graph-audit eval fixture still tested retired v3/v5-era schema concepts and old skill paths, so the repository's own audit fixture could train or grade agents on the wrong graph contract. | `examples/evals/graph-audit.json` asked about `schema_version: const 3`, `type: capability`, `browse_category`, `scope: portable`, `scope: codebase`, `skills/quality/graph-audit/SKILL.md`, and `docs/skill-metadata-protocol.md`, while the live contract is v8 with `subject`, `deployment_target`, free-text `scope`, sidecar `audit-state.json`, and current protocol/schema paths. | Rewrote `examples/evals/graph-audit.json` around current v8 schema checks, sidecar ownership, manifest joins, relation-target integrity, project grounding, and gradeable-artifact evidence. |
| IA-1 | information-architecture | MEDIUM | Docs findability | The repository had a strong root README and many active docs, but no `docs/` front-door index. A cold-start reader who knew the task type but not the owning file had to scan the root README, search, or infer from filenames across dozens of active docs. | `find docs -maxdepth 2 -type f -name '*.md'` lists many active docs across adoption, specs, evals, plans, ADRs, research, marketplace, and generated status; `docs/README.md` did not exist. | Added `docs/README.md` as a task-based docs map and linked it from the root README's "Pick the right doc" table. |
| KEY-1 | keywords | MEDIUM | Activation-field docs | Active docs still overclaimed or blurred `keywords` as conditionally required for routable skills, contradicting the v8 schema and the current protocol summary where `keywords` are recommended fuzzy activation terms capped at 10. One reference also described browse taxonomy as `category`, a retired field name. | `docs/concept-map.md`, `docs/field-state-matrix.md`, `docs/AUTHORING-QUICKSTART.md`, `skill-metadata-protocol/SKILL_METADATA_PROTOCOL.md`, `skill-metadata-protocol/field-reference.md`, and `skill-metadata-protocol/design-rationale.md` used required/condition-required/expected-for-routable language for `keywords`; `schemas/SKILL_METADATA_PROTOCOL_schema.json` requires only `name`, `description`, `subject`, `deployment_target`, and `scope`; the schema's `keywords` property is optional. | Reworded active docs so `keywords` are recommended semantic phrases for fuzzy routing, capped at 10, and not schema-required; updated the field reference to point browse taxonomy to `subject` / `taxonomy_domain` instead of retired `category`. |

## Novelty Memo

The useful new observation from the `semantics` lens is that ADR-0019 was implemented in code and partially documented at the top of the protocol, but the old word “frontmatter” persisted in operator-facing surfaces below the fold. That is a semantic drift pattern: the system behavior was right, but the signs users read still pointed at the pre-sidecar model.

The useful new observation from the `semantic-relations` lens is that the precise `relations.boundary` warning had been added in the protocol, but summary layers kept compressing it back into the word "handoff." That compression matters because the runtime mechanic is exclusion, not deference, and the wrong word changes how authors write relation reasons.

The useful new observation from the `ontology-modeling` lens is that the repo's RDF/OWL projection has to distinguish operational routing claims from formal class-theory claims everywhere, including comments and ADR update notes. `sg:disjointOwnership` is not `owl:disjointWith`; the difference is the whole ontology-level safety property.

The useful new observation from the `taxonomy-design` lens is that current-vs-retired classification drift is easiest to spot where docs show category tables. A table titled "Current Domain Paths" carried more false authority than scattered prose would have; replacing it with subject-local `taxonomy_domain` guidance makes the organizing principle visible.

The useful new observation from the `knowledge-modeling` lens is that ADR-0019 is not just a storage change; it changes the representation paradigm. `SKILL.md` is now the authored frame, `audit-state.json` is loop-owned state, and `skills.manifest.json` is the compiled joined view. Diagrams that collapse those artifacts back into one node make future docs drift more likely.

The useful new observation from the `context-graph` lens is that stale topology survives longest in diagrams and worked examples. They are compressed graph views, so one old edge name (`adjacent`) or old source node (`schema_version` in frontmatter) can teach the wrong traversal path more forcefully than a paragraph of prose.

The useful new observation from the `context-engineering` lens is that "load the relevant skills" is not enough as an instruction in a repo with many generated, historical, and audit-artifact surfaces. The instruction also needs selection rules: which file owns the fact, which context is current, which surfaces are generated or historical, and when to compact raw search output into a verified evidence list.

The useful new observation from the `skill-infrastructure` lens is that deterministic gates can be correct in `package.json` while active docs still teach an old gate topology. Health-tooling claims need to name both the command wiring and the latest command result; otherwise agents keep treating a repaired gate as intentionally separate debt.

The useful new observation from the `skill-router` lens is that boundary exclusion must happen before co-loading, not after it. Otherwise the router can correctly exclude an adjacent skill and still import that excluded skill's verification partner, reintroducing wrong-domain context through a side door.

The useful new observation from the `skill-scaffold` lens is that the authoring template is more dangerous than ordinary stale prose because it is copied into future skills. A single retired word such as "archetype" in the template can become new CONTENT debt across the library; the scaffold has to model both the current frontmatter fields and the sibling `audit-state.json` split.

The useful new observation from the `agent-eval-design` lens is that eval docs drift when they talk about "evals" as one thing. Comprehension evals test understanding, application evals test behavior change, routing evals test retrieval, and manifest/drift gates test integrity. A single checklist cannot represent all four without hiding the real acceptance thresholds.

The useful new observation from the `architecture-decision-records` lens is that ADRs decay most dangerously in their status banners and current-state update notes. The historical decision body can remain useful, but the first screen of an ADR has to say whether the decision is pending, implemented, or superseded; otherwise future agents make plans from old temporal facts.

The useful new observation from the `best-practice` lens is that cross-cutting quality debt often lives in generators, schemas, and fixtures rather than the main prose docs. If a comment backfiller or fixture still teaches old wording, the next CONTENT audit can recreate stale comments even after the protocol itself is fixed.

The useful new observation from the `cognitive-load-theory` lens is that first-screen docs need the strongest segmentation. Dense correctness is still a failure mode when the reader has not yet formed the schema; tables that separate file ownership and authoring steps preserve the same facts with lower extraneous load.

The useful new observation from the `epistemic-grounding` lens is that a command result and the claim people make from it are different artifacts. `UNGROUNDED` is a real drift result, but the warrant it supports is only "no local hash baseline exists"; it does not support "truth is verified." The docs now name that warrant boundary.

The useful new observation from the `eval-driven-development` lens is that eval specs need their own regression budget: when a new rubric dimension is added, the recommended dataset size has to move with it. Otherwise the docs quietly invite a suite that cannot cover the full behavior surface it claims to evaluate.

The useful new observation from the `evaluation` lens is that a verdict contract is not the same as a completion decision. The Skill Audit Loop already required Integrity and Behavior Gate states, but it still needed an explicit audit-report score, score ceilings, evidence packet, and residual-risk note so agents cannot treat passing checks or generated artifacts as equivalent to a complete audit.

The useful new observation from the `graph-audit` lens is that audit fixtures are part of the graph contract, not just tests. A fixture that still asks about retired schema fields can make a future grader reward the wrong answer even after the docs and tooling have moved on.

The useful new observation from the `information-architecture` lens is that the root README was not enough as a wayfinding surface once the docs folder reached dozens of active documents. Readers need a task-based `docs/` front door so they can start from intent, not filename memory.

The useful new observation from the `keywords` lens is that activation fields drift when "recommended for routing quality" gets compressed into "required." That distinction matters because `keywords` are a fuzzy recall aid, while `examples`, `anti_examples`, `triggers`, and `relations.boundary` carry different activation and boundary evidence.

## Dissent / Abstain

No dissent on the `semantics`, `semantic-relations`, `ontology-modeling`, `taxonomy-design`, `knowledge-modeling`, `context-graph`, `context-engineering`, `skill-infrastructure`, `skill-router`, `skill-scaffold`, `agent-eval-design`, `architecture-decision-records`, `best-practice`, `cognitive-load-theory`, `epistemic-grounding`, `eval-driven-development`, `evaluation`, `graph-audit`, `information-architecture`, or `keywords` findings. I abstained from rewriting historical ADR bodies wholesale; instead, I updated status banners, current-state notes, active decision annotations, executable comments, schemas, fixtures, and first-screen docs where current readers or generators need the corrected implementation state. I also abstained from editing historical research, archived docs, old ADR rationale text where stale wording is preserved as historical record rather than current operational guidance, the stale generated `docs/images/system-model.png` image body because the source Mermaid now lives inline and the rendered copy encoded the old representation, the runner prompt bodies because this pass found an agent-context guidance issue rather than a prompt-template defect, adding `npm run drift` to full `verify` because truth-source drift remains CONTENT-side debt, editing the stale `skill-router` example eval fixtures inside the SYSTEM commit because they are per-skill truth-source drift, rewriting the explicitly historical starter-skill archetype table in `SKILL_GRAPH.md` because it is labeled as historical rather than current authoring guidance, renaming the comprehension schema's `skill_type` field because that would be a breaking schema migration rather than a documentation repair, the deprecated `relations.adjacent` alias because removing that alias is a broader schema/content migration, old ADR 0011/0015/0016 wording where "Health Block" is the historical term being referenced, test receipt comments that intentionally describe past breakages, older research/planning notes that correctly preserve the drift sentinel's prior `UNGROUNDED` counts as dated evidence, the multi-model benchmark protocol because it already has held-out/oracle/regression gates and did not share the comprehension-dimension mismatch, `docs/recommended-skills.md` candidate names because that file records demand-gap candidates rather than current bundle availability, the full long-form protocol body because the CLT issue was the first-screen orientation rather than the detailed reference sections, adding a new machine-enforced release flag because the `evaluation` pass repaired the binding documentation first, rewriting every legacy example eval because the `graph-audit` pass found one active fixture that directly grades current Skill Graph audit behavior, restructuring the root README more broadly because the `information-architecture` pass found a missing docs-folder front door rather than a broken root page, and editing generated marketplace/publication outputs because the `keywords` pass found an active protocol-doc contract mismatch rather than a generated marketplace-search defect.
